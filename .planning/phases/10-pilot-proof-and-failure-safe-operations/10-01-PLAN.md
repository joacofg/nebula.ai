---
phase: 10-pilot-proof-and-failure-safe-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_phase10_outage_safety.py
  - tests/test_remote_management_service.py
autonomous: true
requirements:
  - INVT-03

must_haves:
  truths:
    - "Hosted-control-plane transport failures do not break POST /v1/chat/completions for a linked deployment"
    - "Hosted-control-plane transport failures do not turn /health/ready into a serving outage when only hosted visibility is degraded"
    - "Hosted deployment records can become stale or offline while the local gateway still serves requests successfully"
    - "Hosted heartbeat and remote-management loops fail closed with warnings instead of propagating fatal exceptions into the gateway"
  artifacts:
    - path: "tests/test_phase10_outage_safety.py"
      provides: "Dedicated outage-safety integration coverage for serving continuity, readiness, and stale/offline hosted visibility"
    - path: "tests/test_remote_management_service.py"
      provides: "Mixed hosted/local failure regression coverage for remote-management no-op and fail-closed behavior"
  key_links:
    - from: "tests/test_phase10_outage_safety.py"
      to: "src/nebula/services/heartbeat_service.py"
      via: "MockTransport raising ConnectError or ReadTimeout during hosted heartbeat calls"
      pattern: "Heartbeat failed:|_send_once"
    - from: "tests/test_phase10_outage_safety.py"
      to: "src/nebula/main.py"
      via: "Assertions against /v1/chat/completions and /health/ready on the same app instance"
      pattern: "/v1/chat/completions|/health/ready"
    - from: "tests/test_remote_management_service.py"
      to: "src/nebula/services/remote_management_service.py"
      via: "poll_and_apply_once remains non-fatal when hosted transport fails"
      pattern: "Remote management poll/apply failed|poll_and_apply_once"
---

<objective>
Create hard proof for the Phase 10 runtime claim: hosted-control-plane outages degrade hosted visibility only, while local serving and readiness remain authoritative and available.

Purpose: INVT-03 is already implied by the architecture, but pilot credibility requires deterministic regression coverage that couples hosted failure injection with successful serving-path assertions on the same app instance.
Output: A dedicated outage-safety integration suite plus mixed-failure remote-management tests that lock the hosted-outage contract in CI.
</objective>

<execution_context>
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-pilot-proof-and-failure-safe-operations/10-RESEARCH.md
@.planning/phases/10-pilot-proof-and-failure-safe-operations/10-VALIDATION.md
@.planning/phases/08-fleet-inventory-and-freshness-visibility/08-02-SUMMARY.md
@.planning/phases/09-audited-remote-management/09-02-SUMMARY.md
@src/nebula/main.py
@src/nebula/services/heartbeat_service.py
@src/nebula/services/remote_management_service.py
@tests/support.py
@tests/test_remote_management_service.py
@tests/test_chat_completions.py
@tests/test_health.py

<interfaces>
From tests/support.py:
```python
def configured_app(**env_overrides: str) -> Iterator: ...
def auth_headers(api_key: str = "nebula-dev-key", tenant_id: str = "default") -> dict[str, str]: ...
def admin_headers(admin_api_key: str = "nebula-admin-key") -> dict[str, str]: ...
class StubProvider: ...
class FakeCacheService: ...
```

From src/nebula/main.py:
```python
@app.get("/health/ready")
async def readiness() -> JSONResponse: ...
```

From src/nebula/services/heartbeat_service.py:
```python
class HeartbeatService:
    async def _send_once(self) -> None: ...
```

From src/nebula/services/remote_management_service.py:
```python
class RemoteManagementService:
    async def poll_and_apply_once(self) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add outage-safety integration coverage for serving continuity and stale hosted visibility</name>
  <files>
    tests/test_phase10_outage_safety.py
  </files>
  <read_first>
    tests/test_phase10_outage_safety.py,
    tests/support.py,
    tests/test_chat_completions.py,
    tests/test_health.py,
    tests/test_remote_management_service.py,
    src/nebula/main.py,
    src/nebula/services/heartbeat_service.py,
    src/nebula/services/enrollment_service.py,
    src/nebula/services/gateway_enrollment_service.py
  </read_first>
  <behavior>
    - Test 1: after a deployment links successfully, forcing hosted heartbeat and poll requests to raise `httpx.ConnectError` or `httpx.ReadTimeout` still allows `POST /v1/chat/completions` to return 200 with normal `X-Nebula-*` headers
    - Test 2: under the same hosted outage, `GET /health/ready` still returns 200 with `status` equal to `ready` or `degraded`, never `not_ready`
    - Test 3: when a linked deployment's `last_seen_at` is moved older than the stale and offline windows, `GET /v1/admin/deployments/{id}` reports `freshness_status` `"stale"` or `"offline"` while `POST /v1/chat/completions` still returns 200
    - Test 4: heartbeat and remote-management outage paths only emit warnings containing `Heartbeat failed:` or `Remote management poll/apply failed:` and do not raise exceptions into the test
  </behavior>
  <action>
    Create `tests/test_phase10_outage_safety.py` as an async integration module built on the existing `configured_app()` + `httpx.ASGITransport` pattern from `tests/test_remote_management_service.py`.

    In that file, define an async context helper that:
    1. creates the app with these exact env overrides:
       `NEBULA_HOSTED_PLANE_URL="http://hosted.invalid/v1"`,
       `NEBULA_REMOTE_MANAGEMENT_ENABLED="true"`,
       `NEBULA_REMOTE_MANAGEMENT_ALLOWED_ACTIONS='["rotate_deployment_credential"]'`,
       `NEBULA_PREMIUM_PROVIDER="mock"`;
    2. enters `app.router.lifespan_context(app)`;
    3. attaches an `httpx.ASGITransport(app=app)` client for normal in-process requests;
    4. after creating and enrolling an active deployment, swaps `app.state.container.heartbeat_service._http_transport` and `app.state.container.remote_management_service._http_transport` to an `httpx.MockTransport` that raises `httpx.ConnectError("hosted outage")` for every request.

    In the same file, add exact local test helpers to:
    - create an active deployment by calling `/v1/admin/deployments`, `/v1/admin/deployments/{deployment_id}/enrollment-token`, and `/v1/enrollment/exchange`;
    - persist the returned `EnrollmentExchangeResponse` into `gateway_enrollment_service._store_local_identity(exchange)`;
    - inject `StubProvider("ollama", completion_result=CompletionResult(...))` plus `FakeCacheService()` so `/v1/chat/completions` always has a deterministic successful serving path;
    - update `DeploymentModel.last_seen_at` through a SQLAlchemy session created from `app.state.container.settings.database_url`.

    Add at least these exact tests:
    `test_hosted_outage_keeps_chat_completion_serving_and_readiness_green`,
    `test_stale_and_offline_hosted_visibility_do_not_imply_serving_failure`.

    `test_hosted_outage_keeps_chat_completion_serving_and_readiness_green` must:
    - call `await app.state.container.heartbeat_service._send_once()` and `await app.state.container.remote_management_service.poll_and_apply_once()`;
    - assert neither call raises;
    - POST `"/v1/chat/completions"` with `auth_headers()` and the message payload `{"model": "nebula-auto", "messages": [{"role": "user", "content": "Hosted outage should not block local serving"}]}`;
    - assert status 200, `X-Nebula-Route-Target == "local"`, and `X-Nebula-Fallback-Used == "false"`;
    - GET `"/health/ready"` and assert status 200.

    `test_stale_and_offline_hosted_visibility_do_not_imply_serving_failure` must:
    - set the linked deployment `last_seen_at` to `datetime.now(UTC) - timedelta(minutes=45)` and assert `/v1/admin/deployments/{id}` returns `"freshness_status": "stale"`;
    - set `last_seen_at` to `datetime.now(UTC) - timedelta(hours=2)` and assert the same route returns `"freshness_status": "offline"`;
    - POST `"/v1/chat/completions"` after each visibility change and assert status 200 both times.

    Capture warnings with `caplog` and assert the emitted messages include the exact substrings `Heartbeat failed:` and `Remote management poll/apply failed:`. Do not add new retry layers, queues, or runtime status modes.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_phase10_outage_safety.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_phase10_outage_safety.py` contains `def test_hosted_outage_keeps_chat_completion_serving_and_readiness_green`
    - `tests/test_phase10_outage_safety.py` contains `def test_stale_and_offline_hosted_visibility_do_not_imply_serving_failure`
    - `tests/test_phase10_outage_safety.py` contains `Heartbeat failed:`
    - `tests/test_phase10_outage_safety.py` contains `Remote management poll/apply failed:`
    - `.venv/bin/pytest tests/test_phase10_outage_safety.py -x` exits 0
  </acceptance_criteria>
  <done>
    The repo has deterministic integration proof that hosted outages only degrade hosted visibility while the self-hosted serving path and readiness stay available.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend mixed-failure remote-management regression coverage for hosted outage no-op behavior</name>
  <files>
    tests/test_remote_management_service.py
  </files>
  <read_first>
    tests/test_remote_management_service.py,
    tests/test_phase10_outage_safety.py,
    tests/support.py,
    src/nebula/services/remote_management_service.py,
    src/nebula/services/enrollment_service.py,
    src/nebula/services/gateway_enrollment_service.py,
    src/nebula/models/deployment.py
  </read_first>
  <behavior>
    - Test 1: when a queued rotate action exists but the hosted plane is unreachable, `poll_and_apply_once()` returns without raising and the action remains `queued`
    - Test 2: when remote management is enabled but no active local identity exists, `_authorize()` fails closed with `failure_reason` `"invalid_state"` and `failure_detail` `"No active local hosted identity exists."`
    - Test 3: hosted-outage regressions are discoverable with `-k "hosted or outage or stale"` as declared in `10-VALIDATION.md`
  </behavior>
  <action>
    Extend `tests/test_remote_management_service.py` with two exact new tests named:
    `test_hosted_outage_leaves_remote_action_queued_and_serving_unaffected`
    and
    `test_missing_local_identity_reports_invalid_state_failure_reason`.

    In `test_hosted_outage_leaves_remote_action_queued_and_serving_unaffected`:
    - use `configured_async_client()` with `NEBULA_HOSTED_PLANE_URL="http://hosted.invalid/v1"`, `NEBULA_REMOTE_MANAGEMENT_ENABLED="true"`, and `NEBULA_REMOTE_MANAGEMENT_ALLOWED_ACTIONS='["rotate_deployment_credential"]'`;
    - create an active deployment and queue one rotation action with `_queue_rotation_action(...)`;
    - replace `app.state.container.remote_management_service._http_transport` with `httpx.MockTransport` that raises `httpx.ConnectError("hosted outage")`;
    - call `await app.state.container.remote_management_service.poll_and_apply_once()`;
    - assert the action row in `DeploymentRemoteActionModel` still has `status == "queued"`, `started_at is None`, and `finished_at is None`;
    - POST `"/v1/chat/completions"` with `auth_headers()` and assert status 200 in the same scenario.

    In `test_missing_local_identity_reports_invalid_state_failure_reason`:
    - instantiate or reuse `RemoteManagementService` through `app.state.container.remote_management_service`;
    - clear the local identity with `app.state.container.gateway_enrollment_service.clear_local_identity()`;
    - construct an in-memory `RemoteActionRecord` with `action_type="rotate_deployment_credential"` and `status="queued"`;
    - call `_authorize(action)` and assert it returns `(False, "invalid_state", "No active local hosted identity exists.")`.

    Keep the existing helper structure. Do not modify production code in this task unless a failing test reveals a real regression in the current fail-closed behavior.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_remote_management_service.py -k "hosted or outage or stale or invalid_state" -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_remote_management_service.py` contains `def test_hosted_outage_leaves_remote_action_queued_and_serving_unaffected`
    - `tests/test_remote_management_service.py` contains `def test_missing_local_identity_reports_invalid_state_failure_reason`
    - `tests/test_remote_management_service.py` contains `No active local hosted identity exists.`
    - `.venv/bin/pytest tests/test_remote_management_service.py -k "hosted or outage or stale or invalid_state" -x` exits 0
  </acceptance_criteria>
  <done>
    Hosted-outage and mixed local-state regressions are locked into the existing remote-management suite without broadening the runtime subsystem.
  </done>
</task>

</tasks>

<verification>
Run `.venv/bin/pytest tests/test_phase10_outage_safety.py -x` and `.venv/bin/pytest tests/test_remote_management_service.py -k "hosted or outage or stale or invalid_state" -x` to confirm serving continuity, stale/offline visibility semantics, and fail-closed remote-management behavior.
</verification>

<success_criteria>
- Hosted-transport failures leave `/v1/chat/completions` and `/health/ready` successful on the same linked app instance
- Admin deployment records can be stale or offline while local serving still succeeds
- Heartbeat and remote-management hosted calls remain warning-only and non-fatal
</success_criteria>

<output>
After completion, create `.planning/phases/10-pilot-proof-and-failure-safe-operations/10-01-SUMMARY.md`.
</output>
