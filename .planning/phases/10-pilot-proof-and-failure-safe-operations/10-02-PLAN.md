---
phase: 10-pilot-proof-and-failure-safe-operations
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - console/src/lib/hosted-contract.ts
  - console/src/lib/hosted-contract.test.ts
  - console/src/app/trust-boundary/page.tsx
  - console/src/app/trust-boundary/page.test.tsx
  - docs/self-hosting.md
  - docs/demo-script.md
  - docs/pilot-checklist.md
autonomous: true
requirements:
  - TRST-03

must_haves:
  truths:
    - "Pilot-facing docs explain the hosted onboarding flow as outbound-only enrollment plus deployment-scoped steady-state credentials"
    - "Pilot-facing docs and the public trust-boundary page state that hosted outage degrades visibility only and does not break local serving"
    - "Pilot-facing docs state that hosted remote management is limited to one audited rotate_deployment_credential action and fails closed"
    - "The trust-boundary page and the repo docs repeat the same metadata-only contract without drift"
  artifacts:
    - path: "console/src/lib/hosted-contract.ts"
      provides: "Canonical pilot trust-boundary copy for onboarding, outage behavior, and remote-management limits"
      exports: ["trustBoundaryCopy", "getHostedContractContent"]
    - path: "console/src/app/trust-boundary/page.tsx"
      provides: "Public trust-boundary page with pilot-ready hosted narrative"
    - path: "docs/self-hosting.md"
      provides: "Self-hosted onboarding and hosted-outage expectations"
    - path: "docs/demo-script.md"
      provides: "Pilot walkthrough and talk track for hosted onboarding, safety limits, and outage behavior"
    - path: "docs/pilot-checklist.md"
      provides: "Presenter checklist for the exact hosted trust-boundary narrative"
  key_links:
    - from: "console/src/app/trust-boundary/page.tsx"
      to: "console/src/lib/hosted-contract.ts"
      via: "schema-backed copy rendered on the public trust page"
      pattern: "getHostedContractContent|trustBoundaryCopy"
    - from: "docs/self-hosting.md"
      to: "docs/hosted-default-export.schema.json"
      via: "default-export contract reference"
      pattern: "hosted-default-export.schema.json"
    - from: "docs/demo-script.md"
      to: "docs/pilot-checklist.md"
      via: "shared pilot wording for outage-safe behavior and remote-management limits"
      pattern: "visibility only|rotate_deployment_credential|metadata-only"
---

<objective>
Turn the outage-safe backend proof into a pilot-ready narrative across the public trust-boundary page and the canonical docs used in demos, onboarding, and pilot reviews.

Purpose: TRST-03 is not satisfied by code alone. The hosted-control-plane story has to explain onboarding, trust boundaries, the one safe remote-management limit, and outage behavior in the exact terms the product now proves.
Output: Updated schema-backed trust-boundary copy, page-level regression tests, and pilot docs that repeat one consistent hosted narrative.
</objective>

<execution_context>
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-pilot-proof-and-failure-safe-operations/10-RESEARCH.md
@.planning/phases/10-pilot-proof-and-failure-safe-operations/10-VALIDATION.md
@.planning/phases/06-trust-boundary-and-hosted-contract/06-03-SUMMARY.md
@docs/hosted-default-export.schema.json
@console/src/lib/hosted-contract.ts
@console/src/lib/hosted-contract.test.ts
@console/src/app/trust-boundary/page.tsx
@console/src/app/trust-boundary/page.test.tsx
@docs/self-hosting.md
@docs/demo-script.md
@docs/pilot-checklist.md

<interfaces>
From console/src/lib/hosted-contract.ts:
```typescript
export const trustBoundaryCopy = {
  heading: "What this deployment shares",
  metadataOnly: "Metadata-only by default",
  freshnessWarning: "Hosted freshness is not local runtime authority.",
  notInPath: "Nebula's hosted control plane is not in the request-serving path.",
  excludedHeading: "Excluded by default",
  footnote: "Richer diagnostics must be operator-initiated exceptions to this default contract.",
} as const;
```

From docs/hosted-default-export.schema.json:
```json
{
  "title": "HostedDeploymentMetadata",
  "properties": {
    "freshness_status": { "enum": ["connected", "degraded", "stale", "offline"] }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend schema-backed trust-boundary copy with onboarding, outage, and safety-limit narrative</name>
  <files>
    console/src/lib/hosted-contract.ts,
    console/src/lib/hosted-contract.test.ts,
    console/src/app/trust-boundary/page.tsx,
    console/src/app/trust-boundary/page.test.tsx
  </files>
  <read_first>
    console/src/lib/hosted-contract.ts,
    console/src/lib/hosted-contract.test.ts,
    console/src/app/trust-boundary/page.tsx,
    console/src/app/trust-boundary/page.test.tsx,
    docs/hosted-default-export.schema.json,
    .planning/phases/10-pilot-proof-and-failure-safe-operations/10-RESEARCH.md,
    .planning/phases/10-pilot-proof-and-failure-safe-operations/10-01-PLAN.md
  </read_first>
  <behavior>
    - Test 1: `getHostedContractContent()` returns exact pilot copy strings for outbound-only onboarding, visibility-only outage behavior, and the one allowed remote-management action
    - Test 2: the public trust-boundary page renders those exact strings plus the existing metadata-only and not-in-serving-path statements
    - Test 3: freshness-state rendering remains aligned with the schema enum `["connected", "degraded", "stale", "offline"]`
  </behavior>
  <action>
    Extend `console/src/lib/hosted-contract.ts` so `trustBoundaryCopy` includes these exact new keys and values:

    - `pilotIntro: "Hosted onboarding creates deployment identity and fleet visibility without moving request serving or runtime policy into the hosted plane."`
    - `onboardingHeading: "Pilot onboarding flow"`
    - `onboardingBody: "Create a deployment slot in the hosted plane, exchange a short-lived enrollment token from the self-hosted gateway, then continue with a deployment-scoped hosted-link credential."`
    - `outageHeading: "Hosted outage behavior"`
    - `outageBody: "If the hosted control plane is unreachable, Nebula keeps serving with local policy and local provider access. Hosted freshness simply ages toward stale or offline until heartbeats resume."`
    - `remoteLimitsHeading: "Remote-management safety limits"`
    - `remoteLimitsBody: "v2.0 allows one audited rotate_deployment_credential action only. It never changes tenant policy, provider credentials, prompts, or responses, and it fails closed when the deployment is stale, offline, unlinked, revoked, unsupported, or disallowed by local policy."`

    Keep the existing `heading`, `metadataOnly`, `freshnessWarning`, `notInPath`, `excludedHeading`, and `footnote` values unchanged. Do not add new schema fields or a second content source.

    Update `console/src/lib/hosted-contract.test.ts` to assert every new copy key and its exact string value. Keep the existing schema-parity and excluded-data tests intact.

    Update `console/src/app/trust-boundary/page.tsx` to render three new prose sections below the existing intro and above the public-page footnote using the exact headings:
    - `Pilot onboarding flow`
    - `Hosted outage behavior`
    - `Remote-management safety limits`

    Each section must render the matching text from `getHostedContractContent().copy`. The page intro paragraph must also include the exact sentence `Hosted-control-plane outages degrade visibility only; they do not break the self-hosted serving path.`.

    Update `console/src/app/trust-boundary/page.test.tsx` so it asserts the page renders:
    - `Hosted-control-plane outages degrade visibility only; they do not break the self-hosted serving path.`
    - `Create a deployment slot in the hosted plane, exchange a short-lived enrollment token from the self-hosted gateway, then continue with a deployment-scoped hosted-link credential.`
    - `v2.0 allows one audited rotate_deployment_credential action only.`
  </action>
  <verify>
    <automated>npm --prefix console run test -- --run src/lib/hosted-contract.test.ts src/app/trust-boundary/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `console/src/lib/hosted-contract.ts` contains `pilotIntro`
    - `console/src/lib/hosted-contract.ts` contains `Hosted outage behavior`
    - `console/src/lib/hosted-contract.ts` contains `rotate_deployment_credential`
    - `console/src/app/trust-boundary/page.tsx` contains `Hosted-control-plane outages degrade visibility only; they do not break the self-hosted serving path.`
    - `npm --prefix console run test -- --run src/lib/hosted-contract.test.ts src/app/trust-boundary/page.test.tsx` exits 0
  </acceptance_criteria>
  <done>
    The public trust-boundary page exposes the pilot-ready hosted narrative from the existing schema-backed content module with regression coverage.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update canonical pilot docs and demo materials to match the shipped hosted-control-plane story</name>
  <files>
    docs/self-hosting.md,
    docs/demo-script.md,
    docs/pilot-checklist.md
  </files>
  <read_first>
    docs/self-hosting.md,
    docs/demo-script.md,
    docs/pilot-checklist.md,
    console/src/lib/hosted-contract.ts,
    docs/hosted-default-export.schema.json,
    .planning/phases/10-pilot-proof-and-failure-safe-operations/10-RESEARCH.md,
    .planning/phases/10-pilot-proof-and-failure-safe-operations/10-01-PLAN.md
  </read_first>
  <action>
    Update `docs/self-hosting.md` by adding a new section titled `Hosted pilot workflow` immediately after `Outbound-only hosted linking`. That section must state these exact points:
    - `Operators create a deployment slot in the hosted plane, generate a short-lived enrollment token, and pass that token to the self-hosted gateway for one outbound exchange.`
    - `After enrollment, steady-state hosted communication uses a deployment-scoped credential instead of the enrollment token.`
    - `If the hosted plane is unreachable, Nebula keeps serving traffic locally; hosted inventory simply becomes stale or offline until heartbeat visibility returns.`
    - `The only hosted remote-management action in v2.0 is rotate_deployment_credential, and it fails closed when local policy or deployment state does not allow it.`
    - `The hosted plane is metadata-and-intent only; local runtime policy and request serving remain authoritative inside the self-hosted gateway.`
    - `The default hosted export contract is defined in docs/hosted-default-export.schema.json.`

    Update `docs/demo-script.md` so the old sentence `Do not demonstrate enrollment, inventory, or any hosted UI that is not yet shipped.` is removed and replaced with a new hosted walkthrough section after `Explain the hosted control plane story` titled `Hosted pilot proof`. That section must instruct the presenter to:
    - show the public trust-boundary page first;
    - explain the outbound-only enrollment flow with short-lived token plus deployment-scoped credential;
    - show hosted deployment freshness as visibility only, not runtime authority;
    - mention the one audited `rotate_deployment_credential` action and say it never changes tenant policy or provider credentials;
    - state exactly `If the hosted plane disappears, the gateway keeps serving and the hosted view simply ages toward stale or offline.`
    - state exactly `The hosted plane is metadata-and-intent only; local runtime policy and request serving remain authoritative inside the self-hosted gateway.`

    Update `docs/pilot-checklist.md` so the `Trust boundary prep` section includes these exact checklist items:
    - `Be ready to say: hosted onboarding is outbound-only and starts with a short-lived enrollment token.`
    - `Be ready to say: after enrollment, the gateway uses a deployment-scoped hosted-link credential for steady-state communication.`
    - `Be ready to say: if the hosted plane disappears, the gateway keeps serving and hosted visibility simply becomes stale or offline.`
    - `Be ready to say: v2.0 hosted remote management is limited to one audited rotate_deployment_credential action.`
    - `Be ready to say: the hosted plane is metadata-and-intent only; local runtime policy and request serving remain authoritative inside the self-hosted gateway.`

    Also update the out-of-scope reminder in `docs/pilot-checklist.md` from `Do not improvise a hosted onboarding story` to `Do not improvise hosted powers beyond outbound-only enrollment, fleet visibility, and the audited rotate_deployment_credential action.`.

    Keep all wording aligned with `console/src/lib/hosted-contract.ts`. Do not introduce claims about shell access, generic remote execution, automatic hosted recovery, or hosted authority over local runtime policy.
  </action>
  <verify>
    <automated>grep -n "metadata-and-intent only; local runtime policy and request serving remain authoritative inside the self-hosted gateway\\|deployment-scoped credential\\|stale or offline\\|rotate_deployment_credential\\|visibility only\\|hosted-default-export.schema.json" docs/self-hosting.md docs/demo-script.md docs/pilot-checklist.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/self-hosting.md` contains `Hosted pilot workflow`
    - `docs/self-hosting.md` contains `steady-state hosted communication uses a deployment-scoped credential`
    - `docs/self-hosting.md` contains `The hosted plane is metadata-and-intent only; local runtime policy and request serving remain authoritative inside the self-hosted gateway.`
    - `docs/self-hosting.md` contains `docs/hosted-default-export.schema.json`
    - `docs/demo-script.md` contains `Hosted pilot proof`
    - `docs/demo-script.md` contains `If the hosted plane disappears, the gateway keeps serving and the hosted view simply ages toward stale or offline.`
    - `docs/demo-script.md` contains `The hosted plane is metadata-and-intent only; local runtime policy and request serving remain authoritative inside the self-hosted gateway.`
    - `docs/pilot-checklist.md` contains `v2.0 hosted remote management is limited to one audited rotate_deployment_credential action`
    - `docs/pilot-checklist.md` contains `the hosted plane is metadata-and-intent only; local runtime policy and request serving remain authoritative inside the self-hosted gateway`
    - `grep -n "metadata-and-intent only; local runtime policy and request serving remain authoritative inside the self-hosted gateway\\|deployment-scoped credential\\|stale or offline\\|rotate_deployment_credential\\|visibility only\\|hosted-default-export.schema.json" docs/self-hosting.md docs/demo-script.md docs/pilot-checklist.md` exits 0
  </acceptance_criteria>
  <done>
    The repo’s canonical pilot docs and demo flow tell one consistent hosted narrative that matches the shipped product boundary and outage-safe behavior.
  </done>
</task>

</tasks>

<verification>
Run `npm --prefix console run test -- --run src/lib/hosted-contract.test.ts src/app/trust-boundary/page.test.tsx` and `grep -n "metadata-and-intent only; local runtime policy and request serving remain authoritative inside the self-hosted gateway\|deployment-scoped credential\|stale or offline\|rotate_deployment_credential\|visibility only\|hosted-default-export.schema.json" docs/self-hosting.md docs/demo-script.md docs/pilot-checklist.md` to confirm the pilot narrative is both regression-tested and textually present in the docs.
</verification>

<success_criteria>
- The public trust-boundary page explains onboarding, outage behavior, and remote-management limits with exact tested copy
- `docs/self-hosting.md`, `docs/demo-script.md`, and `docs/pilot-checklist.md` repeat the same visibility-only and metadata-only narrative
- Pilot materials explicitly bound hosted scope to outbound-only enrollment, fleet visibility, and one audited rotate_deployment_credential action
</success_criteria>

<output>
After completion, create `.planning/phases/10-pilot-proof-and-failure-safe-operations/10-02-SUMMARY.md`.
</output>
