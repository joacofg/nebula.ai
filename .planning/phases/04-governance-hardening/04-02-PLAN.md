---
phase: 04-governance-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/nebula/services/auth_service.py
  - src/nebula/services/policy_service.py
  - src/nebula/services/chat_service.py
  - src/nebula/api/routes/chat.py
  - src/nebula/api/routes/admin.py
  - tests/test_governance_api.py
  - tests/test_response_headers.py
  - tests/test_admin_playground_api.py
  - tests/test_service_flows.py
autonomous: true
requirements:
  - GOV-02
  - ROUT-01
user_setup: []
must_haves:
  truths:
    - "Invalid credentials, inactive tenants, and disallowed routing choices return stable HTTP status codes plus predictable `detail` strings."
    - "Denied requests and fallback-blocked provider failures carry `X-Nebula-*` metadata that explains the attempted route and the policy outcome."
    - "Usage-ledger rows for `policy_denied` and `provider_error` no longer lose `route_reason` or policy context."
  artifacts:
    - path: src/nebula/services/policy_service.py
      provides: Denial exceptions with route-aware metadata
      contains: "_policy_denied"
    - path: src/nebula/services/chat_service.py
      provides: Consistent denied and fallback-blocked ledger recording
      contains: "local_provider_error_fallback_blocked"
    - path: tests/test_response_headers.py
      provides: Response-header coverage for denied and fallback-blocked paths
      contains: "policy_denied"
    - path: tests/test_admin_playground_api.py
      provides: Admin-playground error and metadata coverage for inactive/disallowed requests
      contains: "Tenant is inactive."
    - path: src/nebula/services/auth_service.py
      provides: Stable admin and tenant auth detail strings used by representative `/v1/admin/*` and `/v1/chat/*` tests
      contains: "Missing or invalid admin API key."
  key_links:
    - from: src/nebula/services/policy_service.py
      to: src/nebula/services/chat_service.py
      via: `HTTPException.headers` carrying `X-Nebula-*` denial metadata into ledger recording
      pattern: "X-Nebula-Route-Reason"
    - from: src/nebula/services/chat_service.py
      to: src/nebula/api/routes/chat.py
      via: route-level responses preserving metadata on 403 and 502 paths
      pattern: "X-Nebula-Policy-Outcome"
    - from: src/nebula/api/routes/admin.py
      to: tests/test_admin_playground_api.py
      via: admin-playground requests using the same metadata/error contract as normal chat traffic
      pattern: "/v1/admin/playground/completions"
---

<objective>
Harden backend-only denied-path metadata, error semantics, and governance verification coverage.

Purpose: Make Nebula explain routing outcomes consistently even when a request is rejected or a provider failure cannot recover because policy blocked fallback.
Output: Stable denial/error details, response-header metadata for non-happy paths, non-null ledger explanations, and backend test coverage for GOV-02 and ROUT-01.
</objective>

<execution_context>
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-governance-hardening/04-RESEARCH.md
@.planning/phases/04-governance-hardening/04-VALIDATION.md
@.planning/phases/04-governance-hardening/04-01-PLAN.md
@src/nebula/services/auth_service.py
@src/nebula/services/policy_service.py
@src/nebula/services/chat_service.py
@src/nebula/api/routes/chat.py
@src/nebula/api/routes/admin.py
@src/nebula/services/auth_service.py
@tests/test_governance_api.py
@tests/test_response_headers.py
@tests/test_admin_playground_api.py
@tests/test_service_flows.py

<interfaces>
From src/nebula/api/routes/chat.py:
```python
def _nebula_headers(metadata) -> dict[str, str]:
    return {
        "X-Nebula-Tenant-ID": metadata.tenant_id,
        "X-Nebula-Route-Target": metadata.route_target,
        "X-Nebula-Route-Reason": metadata.route_reason,
        "X-Nebula-Provider": metadata.provider,
        "X-Nebula-Cache-Hit": str(metadata.cache_hit).lower(),
        "X-Nebula-Fallback-Used": str(metadata.fallback_used).lower(),
        "X-Nebula-Policy-Mode": metadata.policy_mode,
        "X-Nebula-Policy-Outcome": metadata.policy_outcome,
    }
```

From src/nebula/services/chat_service.py:
```python
UsageLedgerRecord(
    terminal_status="policy_denied",
    route_reason=None,
    policy_outcome=exc.detail,
)
```

From src/nebula/services/auth_service.py:
```python
detail="Resolved tenant is inactive or does not exist."
detail="Tenant is inactive."
detail="API key is not authorized for the requested tenant."
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Attach route-aware denial metadata to policy rejections and fallback-blocked provider errors</name>
  <files>src/nebula/services/policy_service.py, src/nebula/services/chat_service.py, src/nebula/api/routes/chat.py, src/nebula/api/routes/admin.py, tests/test_response_headers.py, tests/test_service_flows.py</files>
  <read_first>src/nebula/services/policy_service.py, src/nebula/services/chat_service.py, src/nebula/api/routes/chat.py, src/nebula/api/routes/admin.py, tests/test_response_headers.py, tests/test_service_flows.py, .planning/phases/04-governance-hardening/04-RESEARCH.md, .planning/phases/04-governance-hardening/04-VALIDATION.md</read_first>
  <behavior>
    - Test 1: A disallowed premium-model request returns `403` with `detail` `Premium model '<model>' is not allowed for this tenant.` and response headers including `X-Nebula-Route-Target: premium`, a non-empty `X-Nebula-Route-Reason`, and `X-Nebula-Policy-Outcome`.
    - Test 2: A `premium_only` policy blocking an explicit local model returns `403` with `X-Nebula-Route-Target: local` and `X-Nebula-Route-Reason: explicit_local_model`, asserted in response-header coverage rather than broadening the routing-matrix file for this plan.
    - Test 3: A local-provider failure with `fallback_enabled=false` returns `502` with exact `detail` `Local provider failed and tenant policy disabled premium fallback.` and header `X-Nebula-Route-Reason: local_provider_error_fallback_blocked`.
    - Test 4: The corresponding `policy_denied` and `provider_error` ledger rows persist non-null `route_reason` and `policy_outcome` values.
  </behavior>
  <action>Create the missing red tests first in `tests/test_response_headers.py` and `tests/test_service_flows.py`; keep the broader routing matrix isolated to Plan `04-01` instead of expanding this plan back across that file. In `src/nebula/services/policy_service.py`, add a private helper named `_policy_denied(...)` that raises `HTTPException(status_code=403, detail=..., headers=...)` with exact `X-Nebula-*` header keys. Use the attempted route target and reason already available during `resolve()` so denials keep the original routing explanation instead of collapsing to `route_reason=None`. In `src/nebula/services/chat_service.py`, widen `CompletionMetadata.route_target` support so denied metadata can still be serialized, and when `route_decision.target == "local"` fails while `policy_resolution.fallback_enabled` is `False`, record `route_reason="local_provider_error_fallback_blocked"` and raise `HTTPException(status_code=502, detail="Local provider failed and tenant policy disabled premium fallback.", headers=...)`. Update the denied-path ledger recording in `_resolve_policy()` so it reads `route_reason`, `route_target`, and `policy_outcome` from `exc.headers` instead of writing `None`. Keep `src/nebula/api/routes/chat.py` and `src/nebula/api/routes/admin.py` on the existing `_nebula_headers` contract; do not invent a second response format.</action>
  <verify>
    <automated>.venv/bin/pytest tests/test_response_headers.py tests/test_service_flows.py tests/test_governance_runtime_hardening.py -q</automated>
  </verify>
  <acceptance_criteria>
    - src/nebula/services/policy_service.py contains 'def _policy_denied'
    - src/nebula/services/policy_service.py contains '"X-Nebula-Route-Reason"'
    - src/nebula/services/chat_service.py contains 'local_provider_error_fallback_blocked'
    - src/nebula/services/chat_service.py contains 'Local provider failed and tenant policy disabled premium fallback.'
    - src/nebula/services/chat_service.py does not contain 'route_reason=None' inside the `terminal_status="policy_denied"` record path
    - tests/test_response_headers.py contains 'local_provider_error_fallback_blocked'
    - tests/test_response_headers.py contains 'X-Nebula-Policy-Outcome'
  </acceptance_criteria>
  <done>Denied and fallback-blocked requests expose the same explanatory metadata shape as successful requests, and the ledger keeps that explanation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock down the GOV-02 and ROUT-01 error matrix across chat and admin-playground entrypoints</name>
  <files>src/nebula/services/auth_service.py, tests/test_governance_api.py, tests/test_admin_playground_api.py, tests/test_response_headers.py</files>
  <read_first>src/nebula/services/auth_service.py, tests/test_governance_api.py, tests/test_admin_playground_api.py, tests/test_response_headers.py, src/nebula/api/routes/admin.py, src/nebula/api/routes/chat.py, .planning/phases/04-governance-hardening/04-VALIDATION.md</read_first>
  <behavior>
    - Test 1: Missing client API key returns `401` with exact `detail` `Missing client API key.` and invalid client API key returns `401` with exact `detail` `Invalid client API key.`.
    - Test 2: Missing admin API key on `GET /v1/admin/session` returns `401` with exact `detail` `Missing or invalid admin API key.`, and an invalid admin API key on `GET /v1/admin/policy/options` returns the same `401` detail.
    - Test 3: An inactive tenant request through `/v1/chat/completions` returns `403` with exact `detail` `Resolved tenant is inactive or does not exist.`, and an inactive tenant request through `/v1/admin/playground/completions` returns `403` with exact `detail` `Tenant is inactive.`.
    - Test 4: Disallowed routing choices and denied premium models return matching `detail` strings plus correlated ledger rows retrievable by `request_id`.
  </behavior>
  <action>Expand `tests/test_governance_api.py` to assert the exact error JSON for missing client key, invalid client key, missing admin key on `GET /v1/admin/session`, invalid admin key on `GET /v1/admin/policy/options`, unauthorized tenant, inactive tenant, spend-guardrail denial, and premium-allowlist denial. If any admin detail strings drift, normalize `src/nebula/services/auth_service.py` so admin failures keep the exact detail `Missing or invalid admin API key.` and tenant failures retain their existing exact details. Extend `tests/test_admin_playground_api.py` with a case that deactivates tenant `default`, posts to `/v1/admin/playground/completions`, and asserts `403`, exact `detail` `Tenant is inactive.`, and no successful usage record for that request id. Add denied-path request-id correlation assertions wherever the API returns `X-Request-ID`, so ROUT-01 is proven through both response metadata and ledger retrieval. Keep `tests/test_response_headers.py` focused on header shape and keep the broader routing-matrix assertions owned by Plan `04-01` so this plan stays within a narrower backend-hardening slice. Do not add manual-only checks to satisfy these requirements; every required path in this task must stay under automated pytest coverage.</action>
  <verify>
    <automated>.venv/bin/pytest tests/test_governance_api.py tests/test_admin_playground_api.py tests/test_response_headers.py tests/test_governance_runtime_hardening.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_governance_api.py contains 'Missing client API key.'
    - tests/test_governance_api.py contains 'Invalid client API key.'
    - tests/test_governance_api.py contains 'Missing or invalid admin API key.'
    - tests/test_governance_api.py contains 'Resolved tenant is inactive or does not exist.'
    - tests/test_admin_playground_api.py contains 'Tenant is inactive.'
    - tests/test_admin_playground_api.py contains '/v1/admin/playground/completions'
    - tests/test_governance_api.py contains 'Request exceeds the tenant premium spend guardrail.'
    - src/nebula/services/auth_service.py contains 'Missing or invalid admin API key.'
  </acceptance_criteria>
  <done>The full Phase 4 backend error matrix is pinned down with automated tests across representative admin endpoints, normal chat traffic, and admin-playground entrypoints.</done>
</task>

</tasks>

<verification>
- [ ] `.venv/bin/pytest tests/test_response_headers.py tests/test_service_flows.py tests/test_governance_runtime_hardening.py -q` succeeds
- [ ] `.venv/bin/pytest tests/test_governance_api.py tests/test_admin_playground_api.py tests/test_response_headers.py tests/test_governance_runtime_hardening.py -q` succeeds
- [ ] `rg -n "_policy_denied|local_provider_error_fallback_blocked|Missing client API key.|Tenant is inactive.|X-Nebula-Route-Reason" src/nebula/services/policy_service.py src/nebula/services/chat_service.py tests/test_governance_api.py tests/test_admin_playground_api.py tests/test_response_headers.py` returns the hardened backend metadata and error-contract markers
</verification>

<success_criteria>
- GOV-02 is satisfied by exact, stable backend error details for invalid credentials, inactive tenants, and forbidden routing choices.
- ROUT-01 is satisfied for denied and fallback-blocked backend requests, not only successful completions.
- The request metadata contract is consistent across normal chat traffic, admin playground traffic, and persisted usage-ledger rows before any console work begins.
</success_criteria>

<output>
After completion, create `.planning/phases/04-governance-hardening/04-02-SUMMARY.md`
</output>
