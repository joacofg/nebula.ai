---
phase: 04-governance-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/nebula/models/governance.py
  - src/nebula/services/governance_store.py
  - src/nebula/services/policy_service.py
  - src/nebula/api/routes/admin.py
  - tests/test_governance_api.py
  - tests/test_service_flows.py
  - tests/test_governance_runtime_hardening.py
autonomous: true
requirements:
  - GOV-01
user_setup: []
must_haves:
  truths:
    - "The policy contract distinguishes runtime-enforced controls, soft-signal controls, and stored-only advisory settings."
    - "Changing routing mode, fallback, semantic cache, premium-model allowlists, and hard cost guardrails changes runtime resolution behavior in tests, while `soft_budget_usd` only annotates metadata when exceeded."
  artifacts:
    - path: src/nebula/models/governance.py
      provides: Authoritative policy contract metadata for runtime-enforced, soft-signal, and advisory fields
      contains: "soft_signal_fields"
    - path: src/nebula/services/policy_service.py
      provides: Runtime enforcement of the Phase 4 governance matrix
      contains: "soft_budget=exceeded"
    - path: tests/test_governance_runtime_hardening.py
      provides: Requirement-level routing and policy enforcement matrix
      contains: "test_routing_mode_matrix"
  key_links:
    - from: src/nebula/api/routes/admin.py
      to: src/nebula/models/governance.py
      via: `PolicyOptionsResponse` returned by `/v1/admin/policy/options`
      pattern: "runtime_enforced_fields"
    - from: src/nebula/services/policy_service.py
      to: tests/test_governance_runtime_hardening.py
      via: routing-mode, allowlist, cache, fallback, and spend-guardrail assertions
      pattern: "routing_mode_default"
---

<objective>
Align the policy surface with the backend behavior Nebula actually enforces and signals.

Purpose: Remove policy-contract drift so operators can trust that visible controls are classified correctly as runtime-enforced, soft-signal, or stored-only advisory settings.
Output: An authoritative policy-options contract and requirement-level runtime/soft-signal coverage that downstream surfaces can consume.
</objective>

<execution_context>
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-governance-hardening/04-RESEARCH.md
@.planning/phases/04-governance-hardening/04-VALIDATION.md
@.planning/phases/02-operator-console/02-03-SUMMARY.md
@.planning/phases/03-playground-observability/03-02-SUMMARY.md
@src/nebula/models/governance.py
@src/nebula/services/governance_store.py
@src/nebula/services/policy_service.py
@src/nebula/api/routes/admin.py
@tests/test_governance_api.py
@tests/test_service_flows.py

<interfaces>
From src/nebula/models/governance.py:
```python
class TenantPolicy(BaseModel):
    routing_mode_default: RoutingMode = "auto"
    allowed_premium_models: list[str] = Field(default_factory=list)
    semantic_cache_enabled: bool = True
    fallback_enabled: bool = True
    max_premium_cost_per_request: float | None = Field(default=None, ge=0)
    soft_budget_usd: float | None = Field(default=None, ge=0)
    prompt_capture_enabled: bool = False
    response_capture_enabled: bool = False
```

From src/nebula/services/policy_service.py:
```python
return PolicyResolution(
    route_decision=route_decision,
    policy_mode=policy.routing_mode_default,
    cache_enabled=policy.semantic_cache_enabled,
    fallback_enabled=policy.fallback_enabled,
    policy_outcome=";".join(outcome_parts),
    soft_budget_exceeded=soft_budget_exceeded,
    projected_premium_cost=projected_premium_cost,
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the backend policy contract explicitly authoritative about runtime-enforced, soft-signal, and advisory fields</name>
  <files>src/nebula/models/governance.py, src/nebula/services/governance_store.py, src/nebula/services/policy_service.py, src/nebula/api/routes/admin.py, tests/test_governance_api.py, tests/test_service_flows.py, tests/test_governance_runtime_hardening.py</files>
  <read_first>src/nebula/models/governance.py, src/nebula/services/governance_store.py, src/nebula/services/policy_service.py, src/nebula/api/routes/admin.py, tests/test_governance_api.py, tests/test_service_flows.py, .planning/phases/04-governance-hardening/04-RESEARCH.md, .planning/phases/04-governance-hardening/04-VALIDATION.md</read_first>
  <behavior>
    - Test 1: `GET /v1/admin/policy/options` returns `runtime_enforced_fields` containing exactly `routing_mode_default`, `allowed_premium_models`, `semantic_cache_enabled`, `fallback_enabled`, and `max_premium_cost_per_request`.
    - Test 2: The same response returns `soft_signal_fields` containing exactly `soft_budget_usd`, and `advisory_fields` containing exactly `prompt_capture_enabled` and `response_capture_enabled`.
    - Test 3: A new runtime hardening test matrix proves `routing_mode_default` values `auto`, `local_only`, and `premium_only` change route selection or raise the exact Phase 4 denial details for explicit-model conflicts.
    - Test 4: Service-flow coverage proves `semantic_cache_enabled=false`, `fallback_enabled=false`, `allowed_premium_models`, and `max_premium_cost_per_request` affect denial behavior, while `soft_budget_usd` appears in `policy_outcome` only as a soft-signal annotation when the budget threshold is exercised.
  </behavior>
  <action>Start by adding `tests/test_governance_runtime_hardening.py` and extend `tests/test_governance_api.py` / `tests/test_service_flows.py` so the suite fails on the missing Phase 4 contract. In `src/nebula/models/governance.py`, extend `PolicyOptionsResponse` with exact fields `runtime_enforced_fields: list[str]`, `soft_signal_fields: list[str]`, and `advisory_fields: list[str]`; keep `TenantPolicy` as the persisted payload, but make the enforcement boundary machine-readable through the options endpoint. In `src/nebula/api/routes/admin.py`, return those exact lists from `GET /admin/policy/options`, using `runtime_enforced_fields=["routing_mode_default","allowed_premium_models","semantic_cache_enabled","fallback_enabled","max_premium_cost_per_request"]`, `soft_signal_fields=["soft_budget_usd"]`, and `advisory_fields=["prompt_capture_enabled","response_capture_enabled"]`. In `src/nebula/services/policy_service.py`, keep enforcement centralized and make the runtime matrix explicit: preserve routing-mode denials, keep `fallback_enabled` and `semantic_cache_enabled` as effective toggles, append `soft_budget=exceeded` to `policy_outcome` once spend crosses `soft_budget_usd`, and raise `403` with exact detail `Request exceeds the tenant premium spend guardrail.` when `projected_premium_cost` exceeds `max_premium_cost_per_request`. Add `tests/test_governance_runtime_hardening.py` with named cases for `auto`, `local_only`, `premium_only`, explicit local-model rejection under premium-only, explicit premium-model rejection under local-only, premium-model allowlist / spend-guardrail denials, and soft-budget annotations that do not block routing by themselves. Do not move policy checks into routes or providers; use `PolicyService.resolve()` as the only authority.</action>
  <verify>
    <automated>.venv/bin/pytest tests/test_governance_api.py tests/test_service_flows.py tests/test_governance_runtime_hardening.py -q</automated>
  </verify>
  <acceptance_criteria>
    - src/nebula/models/governance.py contains 'runtime_enforced_fields: list[str]'
    - src/nebula/models/governance.py contains 'soft_signal_fields: list[str]'
    - src/nebula/models/governance.py contains 'advisory_fields: list[str]'
    - src/nebula/api/routes/admin.py contains 'runtime_enforced_fields=['
    - src/nebula/api/routes/admin.py contains 'soft_signal_fields=['
    - src/nebula/api/routes/admin.py contains 'advisory_fields=['
    - src/nebula/api/routes/admin.py contains '"prompt_capture_enabled"' and '"response_capture_enabled"'
    - src/nebula/services/policy_service.py contains 'soft_budget=exceeded'
    - src/nebula/services/policy_service.py contains 'Request exceeds the tenant premium spend guardrail.'
    - tests/test_governance_runtime_hardening.py contains 'test_routing_mode_matrix'
    - tests/test_governance_runtime_hardening.py contains 'premium_only'
    - tests/test_governance_runtime_hardening.py contains 'local_only'
  </acceptance_criteria>
  <done>The backend exposes an authoritative runtime-vs-soft-signal-vs-advisory policy boundary and proves the GOV-01 enforcement matrix with direct tests.</done>
</task>

</tasks>

<verification>
- [ ] `.venv/bin/pytest tests/test_governance_api.py tests/test_service_flows.py tests/test_governance_runtime_hardening.py -q` succeeds
- [ ] `rg -n "runtime_enforced_fields|soft_signal_fields|advisory_fields|soft_budget=exceeded|Request exceeds the tenant premium spend guardrail." src/nebula/models/governance.py src/nebula/api/routes/admin.py src/nebula/services/policy_service.py tests/test_governance_runtime_hardening.py` returns the backend contract and enforcement markers
</verification>

<success_criteria>
- The Phase 4 policy surface has explicit runtime-enforced, soft-signal, and advisory buckets.
- GOV-01 is proven through backend tests, not only through editable form fields.
- The backend policy contract is ready for downstream consumers without embedding console work into the backend-hardening plan.
</success_criteria>

<output>
After completion, create `.planning/phases/04-governance-hardening/04-01-SUMMARY.md`
</output>
