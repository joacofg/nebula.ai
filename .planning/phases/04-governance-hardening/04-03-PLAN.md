---
phase: 04-governance-hardening
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - console/src/lib/admin-api.ts
  - console/src/components/policy/policy-form.tsx
  - console/src/components/policy/policy-advanced-section.tsx
  - console/src/components/policy/policy-form.test.tsx
  - console/src/components/policy/policy-page.test.tsx
  - console/e2e/policy.spec.ts
autonomous: true
requirements:
  - GOV-01
user_setup: []
must_haves:
  truths:
    - "The operator policy screen presents only runtime-enforced controls as editable governance inputs instead of implying all saved settings are enforced."
    - "The operator policy screen treats `soft_budget_usd` as a soft-signal policy field rather than a runtime-enforced control."
    - "The console keeps the existing policy save flow while removing prompt/response capture flags from the visible editable policy surface for Phase 4."
    - "Browser coverage proves the visible policy copy stays aligned with the backend-authoritative contract after the backend hardening lands."
  artifacts:
    - path: console/src/lib/admin-api.ts
      provides: Typed policy-options contract for runtime-enforced, soft-signal, and advisory fields
      contains: "soft_signal_fields"
    - path: console/src/components/policy/policy-form.tsx
      provides: Runtime-enforced controls section driven by backend metadata
      contains: "Runtime-enforced controls"
    - path: console/src/components/policy/policy-advanced-section.tsx
      provides: Deferred/non-operator-facing explanatory copy for stored-but-not-enforced capture settings without rendering editable controls
      contains: "Capture settings are deferred for a future governance/privacy phase and are not editable in Phase 4."
    - path: console/e2e/policy.spec.ts
      provides: Browser coverage for the updated policy contract wording
      contains: "Capture settings are not editable in Phase 4."
  key_links:
    - from: console/src/lib/admin-api.ts
      to: console/src/components/policy/policy-form.tsx
      via: typed `PolicyOptionsResponse` fields used to decide which controls remain editable runtime controls and which stay soft-signal only
      pattern: "soft_signal_fields"
    - from: console/src/components/policy/policy-form.tsx
      to: console/src/components/policy/policy-advanced-section.tsx
      via: UI grouping that keeps runtime controls editable while relegating capture settings to non-editable deferred copy only
      pattern: "Runtime-enforced controls"
    - from: console/src/components/policy/policy-page.test.tsx
      to: console/e2e/policy.spec.ts
      via: component and browser assertions sharing the same contract wording
      pattern: "Capture settings are not editable in Phase 4."
---

<objective>
Align the console policy contract, copy, and browser coverage with the backend-authoritative governance boundary.

Purpose: Make the operator surface reflect the exact runtime-authoritative contract exposed by Phase 4 backend hardening without reintroducing policy/runtime drift in the UI.
Output: Typed console policy options, a runtime-enforced-only visible policy surface for Phase 4, component tests, and `policy.spec.ts` browser coverage.
</objective>

<execution_context>
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-governance-hardening/04-RESEARCH.md
@.planning/phases/04-governance-hardening/04-VALIDATION.md
@.planning/phases/04-governance-hardening/04-01-PLAN.md
@.planning/phases/04-governance-hardening/04-02-PLAN.md
@.planning/phases/02-operator-console/02-03-SUMMARY.md
@console/src/lib/admin-api.ts
@console/src/components/policy/policy-form.tsx
@console/src/components/policy/policy-advanced-section.tsx
@console/src/components/policy/policy-form.test.tsx
@console/src/components/policy/policy-page.test.tsx
@console/e2e/policy.spec.ts
@src/nebula/models/governance.py
@src/nebula/api/routes/admin.py

<interfaces>
From console/src/lib/admin-api.ts:
```typescript
export type PolicyOptionsResponse = {
  routing_modes: RoutingMode[];
  known_premium_models: string[];
  default_premium_model: string;
};
```

From src/nebula/models/governance.py:
```python
class PolicyOptionsResponse(BaseModel):
    routing_modes: list[RoutingMode]
    known_premium_models: list[str]
    default_premium_model: str
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Type the console policy contract and align runtime-vs-advisory UI copy</name>
  <files>console/src/lib/admin-api.ts, console/src/components/policy/policy-form.tsx, console/src/components/policy/policy-advanced-section.tsx, console/src/components/policy/policy-form.test.tsx, console/src/components/policy/policy-page.test.tsx</files>
  <read_first>console/src/lib/admin-api.ts, console/src/components/policy/policy-form.tsx, console/src/components/policy/policy-advanced-section.tsx, console/src/components/policy/policy-form.test.tsx, console/src/components/policy/policy-page.test.tsx, src/nebula/models/governance.py, src/nebula/api/routes/admin.py, .planning/phases/02-operator-console/02-03-SUMMARY.md, .planning/phases/04-governance-hardening/04-01-PLAN.md, .planning/phases/04-governance-hardening/04-02-PLAN.md</read_first>
  <behavior>
    - Test 1: The typed `PolicyOptionsResponse` in the console includes `runtime_enforced_fields`, `soft_signal_fields`, and `advisory_fields`.
    - Test 2: The policy page renders the exact label `Runtime-enforced controls` for the routing, cache, fallback, premium allowlist, and hard spend-guardrail surface that the backend marks runtime-enforced, and does not place `soft_budget_usd` in that section.
    - Test 3: Prompt and response capture settings are not rendered as editable controls, inline field labels, or operator-tweakable advanced toggles on the Phase 4 policy screen, and the page instead shows exact copy `Capture settings are deferred for a future governance/privacy phase and are not editable in Phase 4.`.
    - Test 4: The visible policy controls are derived from the typed options response instead of hardcoded assumptions, and `soft_budget_usd` is labeled with exact copy `Soft budget signal only. Adds policy outcome metadata when exceeded but does not block routing in Phase 4.`.
  </behavior>
  <action>Update `console/src/lib/admin-api.ts` so `PolicyOptionsResponse` matches the backend contract exactly and exports `runtime_enforced_fields`, `soft_signal_fields`, and `advisory_fields`. In `console/src/components/policy/policy-form.tsx`, add the exact section heading `Runtime-enforced controls` above the controls whose field names are present in `runtime_enforced_fields`, and derive the visible editable controls from that typed runtime list rather than static assumptions. Do not render `advisory_fields` as editable controls, badges, or a second operator-facing settings group. Keep `soft_budget_usd` editable only if the backend continues to expose it as a policy field, but render it outside the `Runtime-enforced controls` section with exact explanatory copy `Soft budget signal only. Adds policy outcome metadata when exceeded but does not block routing in Phase 4.`. Remove prompt and response capture settings from the visible editable policy surface for Phase 4; if `console/src/components/policy/policy-advanced-section.tsx` remains in use, repurpose it to show only exact non-editable copy `Capture settings are deferred for a future governance/privacy phase and are not editable in Phase 4.` instead of editable inputs. Update `console/src/components/policy/policy-form.test.tsx` and `console/src/components/policy/policy-page.test.tsx` to assert the new contract, the exact deferred copy, the soft-budget explanatory copy, and the absence of editable prompt/response capture controls. Do not add new policy controls, new routes, unrelated design changes, or any visible editable treatment for prompt/response capture flags.</action>
  <verify>
    <automated>npm --prefix console run test -- --run policy-form policy-page</automated>
  </verify>
  <acceptance_criteria>
    - console/src/lib/admin-api.ts contains 'runtime_enforced_fields: string[]'
    - console/src/lib/admin-api.ts contains 'soft_signal_fields: string[]'
    - console/src/lib/admin-api.ts contains 'advisory_fields: string[]'
    - console/src/components/policy/policy-form.tsx contains 'Runtime-enforced controls'
    - console/src/components/policy/policy-form.tsx contains 'Soft budget signal only. Adds policy outcome metadata when exceeded but does not block routing in Phase 4.'
    - console/src/components/policy/policy-advanced-section.tsx contains 'Capture settings are deferred for a future governance/privacy phase and are not editable in Phase 4.'
    - console/src/components/policy/policy-form.test.tsx contains 'Runtime-enforced controls'
    - console/src/components/policy/policy-page.test.tsx contains 'Capture settings are deferred for a future governance/privacy phase and are not editable in Phase 4.'
    - console/src/components/policy/policy-page.test.tsx contains 'Soft budget signal only. Adds policy outcome metadata when exceeded but does not block routing in Phase 4.'
    - console/src/components/policy/policy-page.test.tsx contains 'not.toBeInTheDocument'
    - console/src/components/policy/policy-form.tsx does not contain 'prompt_capture_enabled'
    - console/src/components/policy/policy-form.tsx does not contain 'response_capture_enabled'
  </acceptance_criteria>
  <done>The operator-facing policy editor matches the backend-authoritative governance boundary and no longer overstates advisory settings as runtime behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend browser coverage for the Phase 4 policy contract wording</name>
  <files>console/e2e/policy.spec.ts</files>
  <read_first>console/e2e/policy.spec.ts, console/src/components/policy/policy-form.tsx, console/src/components/policy/policy-advanced-section.tsx, console/src/lib/admin-api.ts, .planning/phases/04-governance-hardening/04-02-PLAN.md</read_first>
  <behavior>
    - Test 1: `policy.spec.ts` asserts the exact section label `Runtime-enforced controls` is visible on the policy screen.
    - Test 2: `policy.spec.ts` asserts the exact copy `Soft budget signal only. Adds policy outcome metadata when exceeded but does not block routing in Phase 4.` is visible outside the runtime-enforced section.
    - Test 3: `policy.spec.ts` asserts the exact deferred copy `Capture settings are deferred for a future governance/privacy phase and are not editable in Phase 4.` is visible and the capture inputs are absent from the editable policy form.
    - Test 4: Browser coverage still saves policy changes successfully after the wording update.
  </behavior>
  <action>Update `console/e2e/policy.spec.ts` so the browser flow checks the new runtime/deferred wording before or after the existing save assertions, explicitly proves `soft_budget_usd` is presented as a soft signal outside the runtime-enforced section, and proves prompt/response capture controls are not editable on the Phase 4 screen. Preserve the existing grouped explicit-save flow from Phase 2 and keep the test focused on copy alignment plus successful save behavior. Do not move browser work back into backend pytest coverage and do not weaken the exact-string assertions.</action>
  <verify>
    <automated>npm --prefix console run e2e -- policy.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - console/e2e/policy.spec.ts contains 'Runtime-enforced controls'
    - console/e2e/policy.spec.ts contains 'Soft budget signal only. Adds policy outcome metadata when exceeded but does not block routing in Phase 4.'
    - console/e2e/policy.spec.ts contains 'Capture settings are deferred for a future governance/privacy phase and are not editable in Phase 4.'
    - console/e2e/policy.spec.ts contains 'not.toBeVisible'
    - console/e2e/policy.spec.ts contains 'save'
  </acceptance_criteria>
  <done>Browser coverage proves the console wording stays aligned with the backend-authoritative policy contract while preserving the existing save workflow.</done>
</task>

</tasks>

<verification>
- [ ] `npm --prefix console run test -- --run policy-form policy-page` succeeds
- [ ] `npm --prefix console run e2e -- policy.spec.ts` succeeds
- [ ] `rg -n "runtime_enforced_fields|soft_signal_fields|advisory_fields|Runtime-enforced controls|Soft budget signal only\\. Adds policy outcome metadata when exceeded but does not block routing in Phase 4\\.|Capture settings are deferred for a future governance/privacy phase and are not editable in Phase 4\\." console/src/lib/admin-api.ts console/src/components/policy/policy-form.tsx console/src/components/policy/policy-advanced-section.tsx console/src/components/policy/policy-form.test.tsx console/src/components/policy/policy-page.test.tsx console/e2e/policy.spec.ts` returns the console contract and copy-alignment markers
</verification>

<success_criteria>
- GOV-01 remains reflected in the operator-facing contract without implying advisory fields are runtime-enforced.
- GOV-01 remains reflected in the operator-facing contract without keeping non-runtime capture flags as editable controls.
- The console uses backend-authored policy metadata instead of maintaining a second policy-truth table.
- Component and browser coverage will fail if the policy UI wording drifts away from the Phase 4 backend contract.
</success_criteria>

<output>
After completion, create `.planning/phases/04-governance-hardening/04-03-SUMMARY.md`
</output>
