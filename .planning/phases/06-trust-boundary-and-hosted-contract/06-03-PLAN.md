---
phase: 06-trust-boundary-and-hosted-contract
plan: "03"
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - README.md
  - docs/architecture.md
  - docs/self-hosting.md
  - docs/demo-script.md
  - docs/pilot-checklist.md
autonomous: true
requirements:
  - TRST-01
  - TRST-02
must_haves:
  truths:
    - "The repo narrative says Nebula is self-hosted with an optional hosted control plane."
    - "Architecture and self-hosting docs list exactly what leaves the environment by default."
    - "Docs explicitly say the hosted control plane is not in the serving path and not authoritative for local runtime enforcement."
  artifacts:
    - path: "docs/architecture.md"
      provides: "Canonical hybrid-boundary architecture narrative"
      contains: "optional hosted control plane"
    - path: "docs/self-hosting.md"
      provides: "Self-hosting runbook with outbound-only and metadata-only hosted contract disclosure"
      contains: "Outbound-only hosted linking"
    - path: "README.md"
      provides: "Top-level product framing that preserves the self-hosted-first message"
      contains: "optional hosted control plane"
  key_links:
    - from: "README.md"
      to: "docs/architecture.md"
      via: "documentation map and product framing"
      pattern: "trust boundary|optional hosted control plane"
    - from: "README.md"
      to: "docs/hosted-default-export.schema.json"
      via: "explicit reference to the canonical hosted export artifact"
      pattern: "hosted-default-export.schema.json"
    - from: "docs/self-hosting.md"
      to: "docs/architecture.md"
      via: "shared hosted/local authority language"
      pattern: "not in the request-serving path|metadata-only"
---

<objective>
Publish the Phase 6 hosted/self-hosted boundary in the canonical docs and pilot narrative without expanding into enrollment or live hosted operations.

Purpose: Make the hybrid model credible in writing before later phases add linking, inventory, or remote actions.
Output: Updated README, architecture doc, self-hosting runbook, and pilot/demo docs with exact boundary language.
</objective>

<execution_context>
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-trust-boundary-and-hosted-contract/06-CONTEXT.md
@.planning/phases/06-trust-boundary-and-hosted-contract/06-RESEARCH.md
@.planning/phases/05-product-proof-delivery/05-02-SUMMARY.md
@.planning/phases/05-product-proof-delivery/05-03-SUMMARY.md
@README.md
@docs/architecture.md
@docs/self-hosting.md
@docs/demo-script.md
@docs/pilot-checklist.md
@docs/hosted-default-export.schema.json

<interfaces>
Use these exact disclosure values everywhere in docs:
```text
Nebula is self-hosted with an optional hosted control plane.
The hosted control plane is recommended for pilots, but it is not required for serving traffic.
Nebula's hosted control plane is not in the request-serving path.
Local runtime enforcement remains authoritative.
Default hosted export is metadata-only.
Excluded by default: raw prompts, raw responses, provider credentials, raw usage-ledger rows, tenant secrets, authoritative runtime policy state.
Hosted freshness states are connected, degraded, stale, and offline.
Richer diagnostics are operator-initiated exceptions to the default contract.
```

Do not introduce any Phase 7-9 details such as enrollment tokens, hosted inventory pages, hosted auth, webhook flows, or the concrete remote-management action.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the canonical product and architecture docs around the Phase 6 trust boundary</name>
  <read_first>
    README.md
    docs/architecture.md
    docs/self-hosting.md
    docs/hosted-default-export.schema.json
    .planning/phases/05-product-proof-delivery/05-02-SUMMARY.md
    .planning/phases/06-trust-boundary-and-hosted-contract/06-01-PLAN.md
  </read_first>
  <files>README.md, docs/architecture.md, docs/self-hosting.md</files>
  <action>Update `README.md` so Nebula is described as `self-hosted with an optional hosted control plane`, not as a cloud product with an on-prem agent. Add a short hosted-control-plane section that says it is recommended for pilots because it improves onboarding and fleet visibility while remaining optional, and explicitly reference `docs/hosted-default-export.schema.json` as the canonical machine-readable contract for the default hosted export. Update `docs/architecture.md` to add a dedicated hybrid-boundary section with these exact statements: `Nebula's hosted control plane is not in the request-serving path.`, `Local runtime enforcement remains authoritative.`, `Default hosted export is metadata-only.`, and `Hosted freshness states are connected, degraded, stale, and offline.` Include the exact excluded-by-default list `raw prompts, raw responses, provider credentials, raw usage-ledger rows, tenant secrets, authoritative runtime policy state.` Add a short sentence that this section is derived from `docs/hosted-default-export.schema.json` so future edits must update the contract artifact first. Update `docs/self-hosting.md` with a new section titled `Outbound-only hosted linking` that says any later hosted linking is outbound-only, optional for self-hosted deployments, and limited by the same metadata-only default export plus operator-initiated richer diagnostics, and point readers to `docs/hosted-default-export.schema.json` for the authoritative default field list. Do not add enrollment steps, API endpoints, or hosted setup commands in this phase.</action>
  <acceptance_criteria>
    `README.md` contains `optional hosted control plane`.
    `README.md` contains `docs/hosted-default-export.schema.json`.
    `docs/architecture.md` contains `Nebula's hosted control plane is not in the request-serving path.`.
    `docs/architecture.md` contains `derived from docs/hosted-default-export.schema.json`.
    `docs/architecture.md` contains `Local runtime enforcement remains authoritative.`.
    `docs/architecture.md` contains `raw usage-ledger rows`.
    `docs/self-hosting.md` contains `Outbound-only hosted linking`.
    `docs/self-hosting.md` contains `docs/hosted-default-export.schema.json`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "optional hosted control plane|docs/hosted-default-export.schema.json|derived from docs/hosted-default-export.schema.json|not in the request-serving path|Local runtime enforcement remains authoritative|raw usage-ledger rows|Outbound-only hosted linking" README.md docs/architecture.md docs/self-hosting.md</automated>
  </verify>
  <done>The canonical docs use one exact hosted/self-hosted boundary narrative, explicitly list the default export exclusions, and point back to the schema artifact that owns the vocabulary.</done>
</task>

<task type="auto">
  <name>Task 2: Align the pilot and demo narrative to the hosted trust boundary without overselling hosted scope</name>
  <read_first>
    docs/demo-script.md
    docs/pilot-checklist.md
    docs/architecture.md
    .planning/phases/05-product-proof-delivery/05-03-SUMMARY.md
  </read_first>
  <files>docs/demo-script.md, docs/pilot-checklist.md</files>
  <action>Update `docs/demo-script.md` so the presenter can explain the hosted-control-plane story without implying that Phase 6 already ships enrollment or inventory. Add a short callout that Nebula is `self-hosted with an optional hosted control plane`, that the hosted plane is `recommended for pilots`, that it is `not in the request-serving path`, and that default export is metadata-only. Update `docs/pilot-checklist.md` with a trust-boundary prep item that requires the presenter to say the excluded-by-default list exactly: `raw prompts`, `raw responses`, `provider credentials`, `raw usage-ledger rows`, `tenant secrets`, and `authoritative runtime policy state`. Preserve the existing benchmark-led demo order and keep the existing reminder not to improvise hosted onboarding or enterprise scope.</action>
  <acceptance_criteria>
    `docs/demo-script.md` contains `recommended for pilots`.
    `docs/demo-script.md` contains `metadata-only`.
    `docs/pilot-checklist.md` contains `authoritative runtime policy state`.
    `docs/pilot-checklist.md` contains `Do not improvise a hosted onboarding story`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "recommended for pilots|metadata-only|authoritative runtime policy state|Do not improvise a hosted onboarding story" docs/demo-script.md docs/pilot-checklist.md</automated>
  </verify>
  <done>The demo and pilot docs can explain the hosted trust boundary accurately without leaking into later-phase implementation details.</done>
</task>

</tasks>

<verification>
Use `rg` to confirm the exact trust-boundary phrases appear in the canonical docs and that demo/pilot guidance still avoids scope creep.
</verification>

<success_criteria>
Nebula's top-level docs, architecture narrative, and demo materials all describe the same optional hosted control plane, the same default export allowlist/exclusions, and the same non-authoritative hosted boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/06-trust-boundary-and-hosted-contract/06-03-SUMMARY.md`
</output>
