---
phase: 02-operator-console
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - src/nebula/api/routes/admin.py
  - src/nebula/models/governance.py
  - src/nebula/services/governance_store.py
  - tests/test_governance_api.py
  - console/src/app/(console)/policy/page.tsx
  - console/src/components/policy/policy-form.tsx
  - console/src/components/policy/model-allowlist-input.tsx
  - console/src/components/policy/policy-advanced-section.tsx
  - console/src/components/policy/policy-form.test.tsx
  - console/src/components/policy/model-allowlist-input.test.tsx
  - console/src/lib/admin-api.ts
  - console/src/lib/query-keys.ts
  - console/e2e/policy.spec.ts
autonomous: true
requirements:
  - CONS-04
user_setup: []
must_haves:
    truths:
      - "Operators can inspect and update tenant routing policy from the console through a grouped form instead of raw JSON."
      - "Policy editing foregrounds routing mode, fallback, cache, and premium-model allowlist, while budgets and capture flags live in advanced sections."
      - "Policy changes use explicit save semantics with unsaved-change cues and do not pretend prompt/response capture is already enforced at runtime."
    artifacts:
      - path: src/nebula/api/routes/admin.py
        provides: Policy options metadata endpoint for the console
        contains: "@router.get(\"/policy/options\""
      - path: console/src/app/(console)/policy/page.tsx
        provides: Tenant policy management screen
        contains: "Routing mode"
      - path: console/src/components/policy/model-allowlist-input.tsx
        provides: Multi-select plus manual model entry UX
        contains: "Add model"
      - path: console/src/components/policy/policy-advanced-section.tsx
        provides: Advanced controls disclosure
        contains: "Stored for future governance hardening; not yet enforced at runtime."
    key_links:
      - from: console/src/app/(console)/policy/page.tsx
        to: src/nebula/api/routes/admin.py
        via: Policy read/write and options metadata
        pattern: "/policy/options"
      - from: console/src/components/policy/policy-form.tsx
        to: src/nebula/models/governance.py
        via: Policy field names and payload shape
        pattern: "routing_mode_default"
---

<objective>
Implement tenant policy management workflows in the operator console.

Purpose: Complete the Phase 2 governance surface by making tenant policy visible and editable through a structured operator form.
Output: Backend policy metadata support, a grouped policy page with advanced sections, explicit save/reset behavior, and automated coverage for policy editing.
</objective>

<execution_context>
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-operator-console/02-CONTEXT.md
@.planning/phases/02-operator-console/02-RESEARCH.md
@.planning/phases/02-operator-console/02-VALIDATION.md
@.planning/phases/02-operator-console/02-02-SUMMARY.md
@src/nebula/api/routes/admin.py
@src/nebula/models/governance.py
@src/nebula/services/governance_store.py
@tests/test_governance_api.py
@console/src/app/api/admin/[...path]/route.ts
@console/src/app/(console)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend policy-options metadata for the console form</name>
  <files>src/nebula/models/governance.py, src/nebula/services/governance_store.py, src/nebula/api/routes/admin.py, tests/test_governance_api.py</files>
  <read_first>src/nebula/models/governance.py, src/nebula/services/governance_store.py, src/nebula/api/routes/admin.py, tests/test_governance_api.py, .planning/phases/02-operator-console/02-RESEARCH.md</read_first>
  <action>Add a `PolicyOptionsResponse` model in `src/nebula/models/governance.py` with exact fields `routing_modes`, `known_premium_models`, and `default_premium_model`. In `src/nebula/services/governance_store.py`, add a method that returns the distinct union of the configured default premium model and all `allowed_premium_models` values currently stored in tenant policies. In `src/nebula/api/routes/admin.py`, add `GET /admin/policy/options` that returns `routing_modes` as `["auto", "local_only", "premium_only"]`, `default_premium_model` from settings, and the sorted `known_premium_models` list from the store. Extend `tests/test_governance_api.py` with coverage that the endpoint is admin-protected and returns at least the configured default premium model.</action>
  <verify>./.venv/bin/pytest tests/test_governance_api.py -q</verify>
  <acceptance_criteria>
    - src/nebula/models/governance.py contains "class PolicyOptionsResponse"
    - src/nebula/api/routes/admin.py contains '@router.get("/policy/options"'
    - src/nebula/api/routes/admin.py contains '"local_only"' and '"premium_only"'
    - src/nebula/services/governance_store.py contains "allowed_premium_models"
    - tests/test_governance_api.py contains "policy_options"
  </acceptance_criteria>
  <done>The console has a concrete backend source for routing-mode and premium-model options.</done>
</task>

<task type="auto">
  <name>Task 2: Build the policy page with grouped controls, advanced sections, and explicit save/reset flow</name>
  <files>console/src/app/(console)/policy/page.tsx, console/src/components/policy/policy-form.tsx, console/src/components/policy/model-allowlist-input.tsx, console/src/components/policy/policy-advanced-section.tsx, console/src/lib/admin-api.ts, console/src/lib/query-keys.ts</files>
  <read_first>console/src/app/(console)/layout.tsx, console/src/app/api/admin/[...path]/route.ts, src/nebula/models/governance.py, src/nebula/api/routes/admin.py, .planning/phases/02-operator-console/02-CONTEXT.md, .planning/phases/02-operator-console/02-RESEARCH.md</read_first>
  <action>Implement `console/src/app/(console)/policy/page.tsx` as a tenant-scoped policy management screen that first loads the tenant list, then the selected tenant policy, then `GET /api/admin/policy/options`. Build `console/src/components/policy/policy-form.tsx` with grouped sections `Routing mode`, `Execution controls`, `Premium model allowlist`, and `Advanced settings`. In the main sections, expose `routing_mode_default`, `fallback_enabled`, `semantic_cache_enabled`, and `allowed_premium_models`. Implement `console/src/components/policy/model-allowlist-input.tsx` as a multi-select chip input seeded from `known_premium_models`, with manual addition via an `Add model` affordance. Put `max_premium_cost_per_request`, `soft_budget_usd`, `prompt_capture_enabled`, and `response_capture_enabled` inside `console/src/components/policy/policy-advanced-section.tsx`. For the capture flags, render helper text that says exactly `Stored for future governance hardening; not yet enforced at runtime.` Track dirty state so the screen shows unsaved-change cues, enables an explicit `Save policy` action, and offers `Reset changes` to restore the last fetched policy payload.</action>
  <verify>npm --prefix console run test -- --run policy-page policy-form model-allowlist-input</verify>
  <acceptance_criteria>
    - console/src/app/(console)/policy/page.tsx contains "/api/admin/policy/options"
    - console/src/components/policy/policy-form.tsx contains "Routing mode", "Execution controls", and "Advanced settings"
    - console/src/components/policy/model-allowlist-input.tsx contains "Add model"
    - console/src/components/policy/policy-advanced-section.tsx contains "Stored for future governance hardening; not yet enforced at runtime."
    - console/src/components/policy/policy-form.tsx contains "Save policy" and "Reset changes"
  </acceptance_criteria>
  <done>The console exposes a clear, structured policy editor that matches the locked UX decisions and runtime caveats.</done>
</task>

<task type="auto">
  <name>Task 3: Add policy regression coverage at backend, component, and browser levels</name>
  <files>tests/test_governance_api.py, console/e2e/policy.spec.ts, console/src/components/policy/policy-form.test.tsx, console/src/components/policy/model-allowlist-input.test.tsx</files>
  <read_first>.planning/phases/02-operator-console/02-VALIDATION.md, tests/test_governance_api.py, console/playwright.config.ts, console/vitest.config.ts, console/src/app/(console)/policy/page.tsx</read_first>
  <action>Extend `tests/test_governance_api.py` so policy PUT coverage still proves tenant policy updates round-trip correctly after the new options endpoint lands. Add `console/src/components/policy/policy-form.test.tsx` and `console/src/components/policy/model-allowlist-input.test.tsx` to cover tenant switching, dirty-state detection, model chip addition/removal, and reset behavior. Add `console/e2e/policy.spec.ts` that signs into the console, selects a tenant, changes `routing_mode_default`, toggles `fallback_enabled`, adds a premium model through the manual-entry affordance, saves, reloads the page, and confirms the changed values persist. Include an assertion that the advanced section shows the capture-flags runtime caveat text.</action>
  <verify>./.venv/bin/pytest tests/test_governance_api.py -q && npm --prefix console run e2e -- policy.spec.ts</verify>
  <acceptance_criteria>
    - console/e2e/policy.spec.ts contains "Save policy"
    - console/e2e/policy.spec.ts contains "Stored for future governance hardening; not yet enforced at runtime."
    - console/src/components/policy/policy-form.test.tsx contains "Reset changes"
    - tests/test_governance_api.py exits 0
  </acceptance_criteria>
  <done>Policy management has repeatable coverage from backend API behavior through browser-level operator workflows.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./.venv/bin/pytest tests/test_governance_api.py -q` succeeds
- [ ] `npm --prefix console run test -- --run` succeeds
- [ ] `npm --prefix console run e2e -- policy.spec.ts` succeeds
- [ ] `rg -n "/policy/options|Save policy|Stored for future governance hardening" src/nebula/api/routes/admin.py console/src/app/(console)/policy/page.tsx console/src/components/policy/*` finds the expected policy workflow wiring
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Operators can inspect and update tenant policy from the console without falling back to raw API payload edits
  </success_criteria>

<output>
After completion, create `.planning/phases/02-operator-console/02-03-SUMMARY.md`
</output>
