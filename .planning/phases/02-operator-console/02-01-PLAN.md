---
phase: 02-operator-console
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - console/package.json
  - console/tsconfig.json
  - console/next.config.ts
  - console/tailwind.config.ts
  - console/postcss.config.mjs
  - console/src/app/layout.tsx
  - console/src/app/page.tsx
  - console/src/app/(console)/layout.tsx
  - console/src/app/(console)/tenants/page.tsx
  - console/src/app/api/admin/[...path]/route.ts
  - console/src/components/auth/admin-login-form.tsx
  - console/src/components/auth/admin-login-form.test.tsx
  - console/src/components/shell/operator-shell.tsx
  - console/src/lib/admin-session-provider.tsx
  - console/src/lib/admin-session-provider.test.tsx
  - console/src/lib/query-provider.tsx
  - console/src/test/render.tsx
  - console/playwright.config.ts
  - console/vitest.config.ts
  - console/e2e/auth.spec.ts
  - console/.env.example
  - console/Dockerfile
  - src/nebula/api/routes/admin.py
  - tests/test_governance_api.py
  - docker-compose.selfhosted.yml
  - Makefile
  - README.md
  - docs/self-hosting.md
autonomous: true
requirements:
  - CONS-01
user_setup: []
must_haves:
    truths:
      - "Operators can open a dedicated console UI, paste the Nebula admin key, and enter the operator shell without using raw API tooling."
      - "The admin key stays in browser memory only; refresh or tab close forces the operator back to login."
      - "The supported self-hosted deployment path includes the console service and documents how it reaches the FastAPI admin API."
    artifacts:
      - path: console/package.json
        provides: Console workspace and frontend command surface
        contains: "\"next\""
      - path: console/src/components/auth/admin-login-form.tsx
        provides: Session-entry login form for pasted admin key
        contains: "Nebula admin key"
      - path: console/src/app/(console)/layout.tsx
        provides: Sidebar shell for the operator console
        contains: "Tenants"
      - path: src/nebula/api/routes/admin.py
        provides: Dedicated admin session validation endpoint
        contains: "@router.get(\"/session\""
      - path: docker-compose.selfhosted.yml
        provides: Self-hosted runtime with the console service
        contains: "console:"
    key_links:
      - from: console/src/app/api/admin/[...path]/route.ts
        to: src/nebula/api/routes/admin.py
        via: Proxy forwarding to `/v1/admin/*`
        pattern: "/session"
      - from: docker-compose.selfhosted.yml
        to: console/Dockerfile
        via: Console container build
        pattern: "dockerfile: console/Dockerfile"
      - from: README.md
        to: docs/self-hosting.md
        via: Self-hosted console instructions
        pattern: "http://localhost:3000"
---

<objective>
Stand up the frontend shell and admin authentication model for the Operator Console phase.

Purpose: Create the first real browser-facing control plane for Nebula and wrap the existing admin-key trust model in a focused operator UX.
Output: A `console/` app, a compact control-plane shell, a login flow with memory-only session state, a proxy boundary to the FastAPI admin API, deployment wiring, and baseline frontend tests.
</objective>

<execution_context>
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-operator-console/02-CONTEXT.md
@.planning/phases/02-operator-console/02-RESEARCH.md
@.planning/phases/02-operator-console/02-VALIDATION.md
@README.md
@docs/self-hosting.md
@docker-compose.selfhosted.yml
@Makefile
@src/nebula/main.py
@src/nebula/api/routes/admin.py
@src/nebula/api/dependencies.py
@src/nebula/services/auth_service.py
@tests/test_governance_api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the `console/` workspace and the operator shell design system</name>
  <files>console/package.json, console/tsconfig.json, console/next.config.ts, console/tailwind.config.ts, console/postcss.config.mjs, console/src/app/layout.tsx, console/src/app/page.tsx, console/src/app/(console)/layout.tsx, console/src/app/(console)/tenants/page.tsx, console/src/components/shell/operator-shell.tsx, console/src/lib/query-provider.tsx, console/.env.example</files>
  <read_first>.planning/phases/02-operator-console/02-CONTEXT.md, .planning/phases/02-operator-console/02-RESEARCH.md, /Users/joaquinfernandezdegamboa/Proj/nebula/.codex/skills/ui-ux-pro-max/SKILL.md, README.md, docs/self-hosting.md, docker-compose.selfhosted.yml</read_first>
  <action>Create a top-level `console/` Next.js App Router workspace with TypeScript and Tailwind. In `console/package.json`, add runtime dependencies `next`, `react`, `react-dom`, `@tanstack/react-query`, and `lucide-react`; add dev dependencies `typescript`, `tailwindcss`, `postcss`, `autoprefixer`, `eslint`, `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `jsdom`, and `playwright`; and define scripts `dev`, `build`, `start`, `lint`, `test`, and `e2e`. Configure the app to use `src/` layout files, define `console/.env.example` with `NEBULA_API_BASE_URL=http://127.0.0.1:8000`, and set `next.config.ts` to emit a standalone production build. In `console/src/app/layout.tsx`, import `Fira Sans` and `Fira Code`, define CSS variables for the slate/navy control-plane palette (`#F8FAFC`, `#0F172A`, `#334155`, `#0369A1`, `#020617`), and wrap the app with a React Query provider. In `console/src/app/(console)/layout.tsx` and `console/src/components/shell/operator-shell.tsx`, build the compact shell with a left sidebar containing exact nav labels `Tenants`, `API Keys`, and `Policy`, a slim page-header slot, Lucide icons, row-hover states, and `overflow-x-auto` table wrappers for mobile safety. Seed `console/src/app/(console)/tenants/page.tsx` with a placeholder protected page so a successful login has a concrete first destination.</action>
  <verify>npm --prefix console run lint</verify>
  <acceptance_criteria>
    - console/package.json contains "\"dev\": \"next dev\"" and "\"@tanstack/react-query\""
    - console/.env.example contains "NEBULA_API_BASE_URL=http://127.0.0.1:8000"
    - console/src/app/(console)/layout.tsx contains "Tenants", "API Keys", and "Policy"
    - console/src/app/layout.tsx contains "Fira_Sans" and "Fira_Code"
    - console/src/components/shell/operator-shell.tsx contains "overflow-x-auto"
  </acceptance_criteria>
  <done>The repo has a real console workspace and a control-plane shell that matches the locked visual direction.</done>
</task>

<task type="auto">
  <name>Task 2: Implement memory-only admin authentication and the proxy boundary to the FastAPI admin API</name>
  <files>console/src/components/auth/admin-login-form.tsx, console/src/lib/admin-session-provider.tsx, console/src/app/page.tsx, console/src/app/api/admin/[...path]/route.ts, console/src/app/(console)/layout.tsx, src/nebula/api/routes/admin.py, tests/test_governance_api.py</files>
  <read_first>src/nebula/api/routes/admin.py, src/nebula/api/dependencies.py, src/nebula/services/auth_service.py, tests/test_governance_api.py, .planning/phases/02-operator-console/02-CONTEXT.md, .planning/phases/02-operator-console/02-RESEARCH.md</read_first>
  <action>Create `console/src/lib/admin-session-provider.tsx` as a client-side context that stores the admin key in React state only, exposes `signIn`, `signOut`, and `clearSession`, and never writes to `localStorage`, `sessionStorage`, or cookies. Build `console/src/components/auth/admin-login-form.tsx` with a labeled password-style input for the admin key, blur-time required validation, loading/error states, and a submit button that validates credentials by calling `GET /api/admin/session`. Use `console/src/app/page.tsx` as the login route and redirect successful sign-in to `/tenants`. Protect `console/src/app/(console)/layout.tsx` so missing session state redirects back to `/` with a message to enter the admin key again. Add `console/src/app/api/admin/[...path]/route.ts` that proxies all HTTP methods to `${NEBULA_API_BASE_URL}/v1/admin/${path}`, forwarding `X-Nebula-Admin-Key` from the incoming request header and returning upstream status/body unchanged. In `src/nebula/api/routes/admin.py`, add `GET /admin/session` guarded by `require_admin` that returns exactly `{\"status\": \"ok\"}`. Extend `tests/test_governance_api.py` with success and failure coverage for `/v1/admin/session`.</action>
  <verify>./.venv/bin/pytest tests/test_governance_api.py -q && npm --prefix console run test -- --run admin-login-form admin-session-provider</verify>
  <acceptance_criteria>
    - src/nebula/api/routes/admin.py contains '@router.get("/session"'
    - src/nebula/api/routes/admin.py contains '"status": "ok"'
    - console/src/lib/admin-session-provider.tsx does not contain "localStorage"
    - console/src/lib/admin-session-provider.tsx does not contain "sessionStorage"
    - console/src/components/auth/admin-login-form.tsx contains "Nebula admin key"
    - console/src/app/api/admin/[...path]/route.ts contains "X-Nebula-Admin-Key" and "/v1/admin/"
    - tests/test_governance_api.py contains "test_admin_session_endpoint"
  </acceptance_criteria>
  <done>The console can authenticate through the pasted admin key, validate it cleanly, and proxy admin requests without persisting browser secrets.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the console into local development, self-hosted deployment, and baseline browser automation</name>
  <files>console/Dockerfile, console/playwright.config.ts, console/vitest.config.ts, console/src/test/render.tsx, console/src/components/auth/admin-login-form.test.tsx, console/src/lib/admin-session-provider.test.tsx, console/e2e/auth.spec.ts, docker-compose.selfhosted.yml, Makefile, README.md, docs/self-hosting.md</files>
  <read_first>docker-compose.selfhosted.yml, Makefile, README.md, docs/self-hosting.md, .planning/phases/02-operator-console/02-VALIDATION.md, console/package.json</read_first>
  <action>Create `console/Dockerfile` using a Node 22 Alpine image that installs dependencies with `npm ci`, runs `npm run build`, and starts the console with `npm run start`. Add `console/vitest.config.ts`, `console/playwright.config.ts`, and shared test utilities under `console/src/test/` so the console has non-watch `test` and `e2e` entrypoints. Add a first component test for the login form and a Playwright smoke test that signs in and lands on `/tenants`. Update `docker-compose.selfhosted.yml` to add a `console` service exposing `3000:3000`, building from `console/Dockerfile`, and injecting `NEBULA_API_BASE_URL=http://nebula:8000`. Update `Makefile` with `console-install`, `console-dev`, `console-test`, and `console-e2e` targets. Update `README.md` and `docs/self-hosting.md` so the documented operator entrypoint is the console at `http://localhost:3000`, while the API remains available on `http://localhost:8000`.</action>
  <verify>docker compose -f docker-compose.selfhosted.yml config && npm --prefix console run lint && npm --prefix console run e2e -- auth.spec.ts</verify>
  <acceptance_criteria>
    - console/Dockerfile contains "npm run build" and "npm run start"
    - console/playwright.config.ts exists and contains "testDir"
    - docker-compose.selfhosted.yml contains "console:" and "3000:3000"
    - docker-compose.selfhosted.yml contains "NEBULA_API_BASE_URL=http://nebula:8000"
    - Makefile contains "console-install:", "console-dev:", "console-test:", and "console-e2e:"
    - README.md contains "http://localhost:3000"
    - docs/self-hosting.md contains "console" and "3000"
  </acceptance_criteria>
  <done>The console is part of the supported Nebula runtime story and has the baseline automation needed for later UI workflows.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./.venv/bin/pytest tests/test_governance_api.py -q` succeeds
- [ ] `npm --prefix console run lint` succeeds
- [ ] `npm --prefix console run test -- --run` succeeds
- [ ] `docker compose -f docker-compose.selfhosted.yml config` succeeds
- [ ] `rg -n "console:|3000:3000|NEBULA_API_BASE_URL=http://nebula:8000" docker-compose.selfhosted.yml README.md docs/self-hosting.md` finds the console deployment wiring
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Nebula has a dedicated operator console shell with a deployment-ready login flow around the existing admin key model
  </success_criteria>

<output>
After completion, create `.planning/phases/02-operator-console/02-01-SUMMARY.md`
</output>
