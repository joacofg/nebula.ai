---
phase: 03-playground-observability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/nebula/models/governance.py
  - src/nebula/services/auth_service.py
  - src/nebula/api/routes/admin.py
  - src/nebula/services/governance_store.py
  - tests/test_admin_playground_api.py
  - console/src/lib/admin-api.ts
  - console/src/lib/query-keys.ts
  - console/src/app/api/playground/completions/route.ts
  - console/src/app/(console)/playground/page.tsx
  - console/src/components/playground/playground-form.tsx
  - console/src/components/playground/playground-response.tsx
  - console/src/components/playground/playground-form.test.tsx
  - console/src/components/playground/playground-response.test.tsx
  - console/e2e/playground.spec.ts
autonomous: true
requirements:
  - PLAY-01
user_setup: []
must_haves:
  truths:
    - "Operator can open a Playground screen in the console, choose a tenant, and send a non-streaming test prompt without needing a tenant API key."
    - "Playground execution uses the real Nebula routing path through ChatService instead of a mock-only browser workflow."
    - "The first Playground response shows the completion text and request identifier so later plans can enrich it with recorded metadata."
  artifacts:
    - path: src/nebula/api/routes/admin.py
      provides: Admin-scoped playground completion endpoint plus exact request-id ledger lookup contract
      contains: "@router.post(\"/playground/completions\""
    - path: src/nebula/services/auth_service.py
      provides: Admin-side tenant context resolution for playground execution
      contains: "resolve_playground_context"
    - path: console/src/app/(console)/playground/page.tsx
      provides: Protected Playground route in the operator console
      contains: "Run prompt"
    - path: console/src/app/api/playground/completions/route.ts
      provides: Same-origin proxy from the console to the admin playground endpoint
      contains: "/v1/admin/playground/completions"
    - path: tests/test_admin_playground_api.py
      provides: Backend verification of admin playground execution and request-id correlation
      contains: "test_admin_playground_completion"
  key_links:
    - from: console/src/app/api/playground/completions/route.ts
      to: src/nebula/api/routes/admin.py
      via: POST proxy to /v1/admin/playground/completions
      pattern: "/v1/admin/playground/completions"
    - from: src/nebula/api/routes/admin.py
      to: src/nebula/services/auth_service.py
      via: Tenant context resolution before delegating to ChatService
      pattern: "resolve_playground_context"
    - from: console/src/app/(console)/playground/page.tsx
      to: console/src/lib/admin-api.ts
      via: Playground mutation and tenant query
      pattern: "createPlaygroundCompletion"
    - from: console/src/lib/admin-api.ts
      to: console/src/app/api/playground/completions/route.ts
      via: header-aware playground client result contract
      pattern: "PlaygroundCompletionResult"
---

<objective>
Build the playground request/response workflow for Phase 3.

Purpose: Give operators a real browser path for exercising Nebula routing without breaking the admin-key trust model or asking for unrecoverable tenant API keys.
Output: An admin-scoped backend bridge, shared console contracts for downstream plans, a protected Playground route, and backend/browser coverage for the basic non-streaming flow.
</objective>

<execution_context>
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-playground-observability/03-RESEARCH.md
@.planning/phases/03-playground-observability/03-VALIDATION.md
@README.md
@src/nebula/api/routes/admin.py
@src/nebula/api/routes/chat.py
@src/nebula/services/auth_service.py
@src/nebula/services/chat_service.py
@src/nebula/services/governance_store.py
@src/nebula/models/governance.py
@tests/test_governance_api.py
@console/src/lib/admin-api.ts
@console/src/lib/query-keys.ts
@console/src/app/api/admin/[...path]/route.ts
@console/src/app/(console)/layout.tsx

<interfaces>
From src/nebula/api/routes/chat.py:
```python
def _nebula_headers(metadata) -> dict[str, str]:
    return {
        "X-Nebula-Tenant-ID": metadata.tenant_id,
        "X-Nebula-Route-Target": metadata.route_target,
        "X-Nebula-Route-Reason": metadata.route_reason,
        "X-Nebula-Provider": metadata.provider,
        "X-Nebula-Cache-Hit": str(metadata.cache_hit).lower(),
        "X-Nebula-Fallback-Used": str(metadata.fallback_used).lower(),
        "X-Nebula-Policy-Mode": metadata.policy_mode,
        "X-Nebula-Policy-Outcome": metadata.policy_outcome,
    }
```

From src/nebula/services/auth_service.py:
```python
@dataclass(slots=True, frozen=True)
class AuthenticatedTenantContext:
    tenant: TenantRecord
    api_key: ApiKeyRecord
    policy: TenantPolicy
```

From src/nebula/models/governance.py:
```python
class UsageLedgerRecord(BaseModel):
    request_id: str
    tenant_id: str
    final_route_target: str
    final_provider: str | None = None
    fallback_used: bool
    cache_hit: bool
    estimated_cost: float | None = None
    latency_ms: float | None = None
    terminal_status: TerminalStatus
    route_reason: str | None = None
    policy_outcome: str | None = None
```

From console/src/lib/admin-api.ts:
```typescript
export function listTenants(adminKey: string) {
  return adminRequest<TenantRecord[]>(ADMIN_TENANTS_ENDPOINT, { adminKey });
}
```

Expected contract created in this plan:
```typescript
export type PlaygroundCompletionResult = {
  body: PlaygroundResponse;
  requestId: string;
  tenantId: string;
  routeTarget: string;
  routeReason: string;
  provider: string;
  cacheHit: boolean;
  fallbackUsed: boolean;
  policyMode: string;
  policyOutcome: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the admin playground backend contract and request-id lookup support</name>
  <files>src/nebula/models/governance.py, src/nebula/services/auth_service.py, src/nebula/api/routes/admin.py, src/nebula/services/governance_store.py, tests/test_admin_playground_api.py</files>
  <read_first>src/nebula/models/governance.py, src/nebula/services/auth_service.py, src/nebula/api/routes/admin.py, src/nebula/services/chat_service.py, src/nebula/services/governance_store.py, tests/test_governance_api.py, .planning/phases/03-playground-observability/03-RESEARCH.md, .planning/phases/03-playground-observability/03-VALIDATION.md</read_first>
  <behavior>
    - Test 1: `POST /v1/admin/playground/completions` with a valid admin key, tenant id `default`, and `stream=false` returns `200`, a normal completion body, and an `X-Request-ID` header.
    - Test 2: The same response exposes `X-Nebula-Route-Target`, `X-Nebula-Provider`, `X-Nebula-Cache-Hit`, `X-Nebula-Fallback-Used`, and `X-Nebula-Policy-Outcome`.
    - Test 3: `GET /v1/admin/usage/ledger?request_id=<id>` returns exactly one correlated usage row for the playground request.
    - Test 4: Unknown tenant id in the admin playground payload returns `404` with detail `Tenant not found.`.
  </behavior>
  <action>First create `tests/test_admin_playground_api.py` and make it fail against the missing contract. In `src/nebula/models/governance.py`, add explicit admin playground request/response models that accept `tenant_id` plus a normal `ChatCompletionRequest`, and keep the response body compatible with existing completion payloads while allowing a `request_id` field. In `src/nebula/services/auth_service.py`, add `resolve_playground_context(tenant_id: str) -> AuthenticatedTenantContext` that loads the tenant, rejects missing or inactive tenants with `404` or `403`, uses the bootstrap API key record as the synthetic `api_key`, and returns the stored tenant policy. In `src/nebula/api/routes/admin.py`, add `POST /admin/playground/completions` that requires admin auth, rejects `stream=true` with `400` detail `Playground only supports non-streaming requests in Phase 3.`, delegates to `container.chat_service.create_completion_with_metadata(...)`, mirrors the `X-Nebula-*` header contract from `chat.py`, and sets `X-Request-ID` from `request.state.request_id`. Extend `GET /admin/usage/ledger` to accept an exact-match `request_id` query parameter and pass it into the store. In `src/nebula/services/governance_store.py`, add the exact request-id filter path so `request_id` narrows to a single row before the normal `limit` ordering.</action>
  <verify>
    <automated>./.venv/bin/pytest tests/test_admin_playground_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - src/nebula/api/routes/admin.py contains '@router.post("/playground/completions"'
    - src/nebula/api/routes/admin.py contains 'Playground only supports non-streaming requests in Phase 3.'
    - src/nebula/api/routes/admin.py contains 'request_id: str | None = Query(default=None)'
    - src/nebula/services/auth_service.py contains 'def resolve_playground_context'
    - src/nebula/services/governance_store.py contains 'request_id: str | None = None'
    - tests/test_admin_playground_api.py contains 'test_admin_playground_completion'
    - tests/test_admin_playground_api.py contains 'test_usage_ledger_request_id_filter'
  </acceptance_criteria>
  <done>The backend exposes an admin-only non-streaming playground execution path and an exact request-id lookup contract that later plans can rely on.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Establish shared console contracts and the minimal Playground route</name>
  <files>console/src/lib/admin-api.ts, console/src/lib/query-keys.ts, console/src/app/api/playground/completions/route.ts, console/src/app/(console)/playground/page.tsx, console/src/components/playground/playground-form.tsx, console/src/components/playground/playground-response.tsx, console/src/components/playground/playground-form.test.tsx, console/src/components/playground/playground-response.test.tsx</files>
  <read_first>console/src/lib/admin-api.ts, console/src/lib/query-keys.ts, console/src/app/api/admin/[...path]/route.ts, console/src/app/(console)/layout.tsx, src/nebula/models/governance.py, src/nebula/api/routes/admin.py, .planning/phases/03-playground-observability/03-RESEARCH.md</read_first>
  <behavior>
    - Test 1: Playground page loads the tenant list, defaults to the first tenant, and blocks submission when the operator session is missing.
    - Test 2: Submitting a prompt calls `/api/playground/completions` and renders the returned assistant text plus the request id.
    - Test 3: `createPlaygroundCompletion` returns a typed result object that includes the response body plus parsed `X-Request-ID` and `X-Nebula-*` header values needed by `03-02`.
  </behavior>
  <action>Extend `console/src/lib/admin-api.ts` with exact exported types `PlaygroundInput`, `PlaygroundResponse`, `PlaygroundCompletionResult`, and `UsageLedgerFilters`. Make `UsageLedgerFilters` explicitly include `tenantId`, `routeTarget`, `terminalStatus`, `fromTimestamp`, `toTimestamp`, and `requestId`. Implement `createPlaygroundCompletion` without the generic `adminRequest` helper so it can parse response headers and return a `PlaygroundCompletionResult` containing `body`, `requestId`, `tenantId`, `routeTarget`, `routeReason`, `provider`, `cacheHit`, `fallbackUsed`, `policyMode`, and `policyOutcome`. Keep `listUsageLedger` and `getUsageLedgerEntry` on top of the shared admin request path so later plans reuse one client surface. Extend `console/src/lib/query-keys.ts` now with `playgroundResponse`, `usageLedger`, `usageLedgerEntry`, and `runtimeHealth` keys so downstream plans can stay off this shared file. Create `console/src/app/api/playground/completions/route.ts` as a same-origin proxy that forwards POSTs to `/v1/admin/playground/completions`, preserves `X-Nebula-Admin-Key`, and copies the `X-Request-ID` plus all `X-Nebula-*` headers back to the browser response. Build `console/src/app/(console)/playground/page.tsx` with a tenant select, model input, prompt textarea, `Run prompt` submit button, loading/error state, and a result card that shows the returned assistant text and request id only; do not add metadata, cost, or fallback badges in this plan. Put the form and basic response rendering into `console/src/components/playground/playground-form.tsx` and `console/src/components/playground/playground-response.tsx`, and add `console/src/components/playground/playground-form.test.tsx` plus `console/src/components/playground/playground-response.test.tsx` now so the task-level verification command is runnable as soon as this task finishes.</action>
  <verify>
    <automated>npm --prefix console run test -- --run playground-form playground-response</automated>
  </verify>
  <acceptance_criteria>
    - console/src/lib/admin-api.ts contains 'export type PlaygroundInput'
    - console/src/lib/admin-api.ts contains 'export type PlaygroundCompletionResult'
    - console/src/lib/admin-api.ts contains 'createPlaygroundCompletion'
    - console/src/lib/admin-api.ts contains 'getUsageLedgerEntry'
    - console/src/lib/admin-api.ts contains 'routeTarget:' and 'policyOutcome:'
    - console/src/lib/admin-api.ts contains 'terminalStatus'
    - console/src/lib/query-keys.ts contains 'usageLedgerEntry' and 'runtimeHealth'
    - console/src/app/api/playground/completions/route.ts contains '/v1/admin/playground/completions'
    - console/src/app/(console)/playground/page.tsx contains 'Run prompt'
    - console/src/components/playground/playground-response.tsx contains 'Request ID'
    - console/src/components/playground/playground-form.test.tsx contains 'Run prompt'
    - console/src/components/playground/playground-response.test.tsx contains 'Request ID'
  </acceptance_criteria>
  <done>The console has a stable Playground route and shared client/query contracts that the metadata and observability plans can build on without touching the fetch layer again.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add baseline component and browser coverage for the non-streaming Playground flow</name>
  <files>console/e2e/playground.spec.ts</files>
  <read_first>console/src/app/(console)/playground/page.tsx, console/src/components/playground/playground-form.tsx, console/src/components/playground/playground-response.tsx, console/playwright.config.ts, console/vitest.config.ts, .planning/phases/03-playground-observability/03-VALIDATION.md</read_first>
  <behavior>
    - Test 1: Browser flow signs in, opens `/playground`, submits one prompt, and sees a completion plus request id.
    - Test 2: The route continues to work when the shared client returns a header-aware `PlaygroundCompletionResult`.
  </behavior>
  <action>Add `console/e2e/playground.spec.ts` that signs in through the existing admin key flow, stubs the Playground proxy response including `X-Request-ID`, visits `/playground`, submits a prompt for tenant `default`, and asserts the page shows the returned content plus the `Request ID` label. Keep the E2E flow scoped to the minimal non-streaming round trip; metadata and ledger follow-up assertions belong to `03-02`.</action>
  <verify>
    <automated>npm --prefix console run e2e -- playground.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - console/e2e/playground.spec.ts contains '/playground'
    - console/e2e/playground.spec.ts contains 'Request ID'
    - console/e2e/playground.spec.ts contains 'X-Request-ID'
  </acceptance_criteria>
  <done>The base Playground request/response workflow has repeatable automated coverage before metadata enrichment begins.</done>
</task>

</tasks>

<verification>
- [ ] `./.venv/bin/pytest tests/test_admin_playground_api.py -q` succeeds
- [ ] `npm --prefix console run test -- --run playground-form playground-response` succeeds
- [ ] `npm --prefix console run e2e -- playground.spec.ts` succeeds
- [ ] `rg -n "playground/completions|resolve_playground_context|Request ID" src/nebula/api/routes/admin.py src/nebula/services/auth_service.py console/src/app/(console)/playground/page.tsx console/src/components/playground/*` finds the expected workflow wiring
</verification>

<success_criteria>
- Operators can send a non-streaming Playground request from the console without needing a tenant API key.
- The backend bridge delegates into `ChatService` and preserves the standard `X-Nebula-*` response metadata contract.
- Shared console contracts for Playground, ledger lookup, and runtime health exist so `03-02` and `03-03` can execute in parallel after this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/03-playground-observability/03-01-SUMMARY.md`
</output>
