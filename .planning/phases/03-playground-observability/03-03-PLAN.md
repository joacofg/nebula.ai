---
phase: 03-playground-observability
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/nebula/services/premium_provider_health_service.py
  - src/nebula/services/runtime_health_service.py
  - src/nebula/core/container.py
  - tests/test_health.py
  - console/src/app/api/runtime/health/route.ts
  - console/src/app/(console)/observability/page.tsx
  - console/src/components/ledger/ledger-filters.tsx
  - console/src/components/ledger/ledger-table.tsx
  - console/src/components/ledger/ledger-request-detail.tsx
  - console/src/components/ledger/ledger-filters.test.tsx
  - console/src/components/ledger/ledger-table.test.tsx
  - console/src/components/health/runtime-health-cards.tsx
  - console/src/components/health/runtime-health-cards.test.tsx
  - console/e2e/observability.spec.ts
autonomous: true
requirements:
  - OBS-01
  - OBS-02
user_setup: []
must_haves:
  truths:
    - "Operator can inspect usage-ledger entries in the console by tenant, route target, terminal status, and time window."
    - "Operator can open request detail for a specific ledger row and see the recorded route, provider, fallback, cache, and cost fields."
    - "Operator can tell whether semantic cache, local Ollama, and the premium provider are ready or degraded without confusing optional degradation for full outage."
  artifacts:
    - path: src/nebula/services/premium_provider_health_service.py
      provides: Backend-owned premium-provider health contract
      contains: "health_status"
    - path: src/nebula/services/runtime_health_service.py
      provides: Extended dependency payload including premium provider status
      contains: "\"premium_provider\""
    - path: console/src/app/(console)/observability/page.tsx
      provides: Operator-facing observability surface for ledger and runtime health
      contains: "Usage ledger"
    - path: console/src/components/health/runtime-health-cards.tsx
      provides: Ready/degraded/not_ready rendering for dependency cards
      contains: "premium_provider"
    - path: console/e2e/observability.spec.ts
      provides: Browser verification of ledger filters and health cards
      contains: "Dependency health"
  key_links:
    - from: src/nebula/services/runtime_health_service.py
      to: src/nebula/services/premium_provider_health_service.py
      via: runtime dependency aggregation
      pattern: "premium_provider"
    - from: console/src/app/api/runtime/health/route.ts
      to: src/nebula/services/runtime_health_service.py
      via: same-origin proxy to /health/dependencies
      pattern: "/health/dependencies"
    - from: console/src/app/(console)/observability/page.tsx
      to: console/src/lib/admin-api.ts
      via: ledger queries by tenant, route target, terminal status, time window, and request id
      pattern: "listUsageLedger"
---

<objective>
Build the observability views for usage ledger and dependency health.

Purpose: Give operators a dedicated console surface for recorded request outcomes and runtime dependency state, including premium-provider degradation that is currently missing from the backend contract.
Output: Premium-provider health aggregation, an observability page with ledger filters and request detail, dependency health cards, and automated verification for the end-to-end operator view.
</objective>

<execution_context>
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-playground-observability/03-RESEARCH.md
@.planning/phases/03-playground-observability/03-VALIDATION.md
@.planning/phases/03-playground-observability/03-01-SUMMARY.md
@src/nebula/core/container.py
@src/nebula/services/runtime_health_service.py
@src/nebula/services/governance_store.py
@tests/test_health.py
@console/src/lib/admin-api.ts
@console/src/lib/query-keys.ts

<interfaces>
From src/nebula/services/runtime_health_service.py:
```python
async def dependencies(self) -> dict[str, dict[str, object]]:
    return {
        "gateway": {...},
        "governance_store": self.governance_store.health_status(),
        "semantic_cache": await self.semantic_cache.health_status(),
        "local_ollama": await self.embeddings_service.health_status(),
    }
```

From console/src/lib/admin-api.ts after 03-01:
```typescript
export function listUsageLedger(adminKey: string, filters: UsageLedgerFilters)
export function getUsageLedgerEntry(adminKey: string, requestId: string)
```

From console/src/lib/query-keys.ts after 03-01:
```typescript
usageLedger: (filters: UsageLedgerFilters) => ...
runtimeHealth: ["runtime-health"] as const
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the backend runtime health contract with premium-provider status</name>
  <files>src/nebula/services/premium_provider_health_service.py, src/nebula/services/runtime_health_service.py, src/nebula/core/container.py, tests/test_health.py</files>
  <read_first>src/nebula/services/runtime_health_service.py, src/nebula/core/container.py, src/nebula/providers/openai_compatible.py, src/nebula/providers/mock_premium.py, tests/test_health.py, .planning/phases/03-playground-observability/03-RESEARCH.md</read_first>
  <behavior>
    - Test 1: `/health/dependencies` includes a `premium_provider` object with `status`, `required`, and `detail`.
    - Test 2: When the premium provider probe fails but required dependencies remain ready, `/health/ready` returns `200` with overall `degraded`.
    - Test 3: Mock premium configuration reports `ready` with a detail that it is a local mock backend, not a remote outage probe.
  </behavior>
  <action>Create `src/nebula/services/premium_provider_health_service.py` with a single async method `health_status() -> dict[str, object]`. Implement concrete logic: when `settings.premium_provider == "mock"`, return `{"status": "ready", "required": False, "detail": "Mock premium provider configured for local development."}`; when `settings.premium_provider == "openai_compatible"`, issue an authenticated `GET /models` against the configured premium base URL using a short timeout and return `ready` on HTTP 200, otherwise `degraded` with the exception text in `detail`. Wire that service through `src/nebula/core/container.py` and inject it into `src/nebula/services/runtime_health_service.py`, adding `"premium_provider": await self.premium_provider_health.health_status()` to the dependency payload. Extend `tests/test_health.py` with concrete coverage for the new key and for degraded optional-provider behavior; keep premium-provider degradation optional so the overall status only becomes `not_ready` when required dependencies fail.</action>
  <verify>
    <automated>./.venv/bin/pytest tests/test_health.py -q</automated>
  </verify>
  <acceptance_criteria>
    - src/nebula/services/premium_provider_health_service.py contains 'async def health_status'
    - src/nebula/services/premium_provider_health_service.py contains 'GET /models' or '"/models"'
    - src/nebula/services/runtime_health_service.py contains '"premium_provider"'
    - src/nebula/core/container.py contains 'premium_provider_health_service'
    - tests/test_health.py contains 'premium_provider'
  </acceptance_criteria>
  <done>The backend owns a complete dependency health contract that now includes premium-provider status under `OBS-02`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the observability ledger view with tenant, route-target, terminal-status, and time-window filters</name>
  <files>console/src/app/(console)/observability/page.tsx, console/src/components/ledger/ledger-filters.tsx, console/src/components/ledger/ledger-table.tsx, console/src/components/ledger/ledger-request-detail.tsx, console/src/components/ledger/ledger-filters.test.tsx, console/src/components/ledger/ledger-table.test.tsx</files>
  <read_first>console/src/lib/admin-api.ts, console/src/lib/query-keys.ts, src/nebula/models/governance.py, src/nebula/api/routes/admin.py, .planning/phases/03-playground-observability/03-RESEARCH.md, .planning/phases/03-playground-observability/03-VALIDATION.md</read_first>
  <behavior>
    - Test 1: Observability page renders tenant, route-target, terminal-status, and time-window filters before loading ledger rows.
    - Test 2: Changing a filter updates the `listUsageLedger` query key and refreshes the table.
    - Test 3: Selecting a row opens request detail showing request id, provider, fallback, cache, tokens, and estimated cost.
  </behavior>
  <action>Build `console/src/app/(console)/observability/page.tsx` as a single operator page with two sections: `Usage ledger` first and `Dependency health` second. For the ledger section, create `console/src/components/ledger/ledger-filters.tsx` with exact controls `Tenant`, `Route target`, `Terminal status`, `From`, and `To`, plus a `Refresh` action. Treat "route outcome" as the combination of where the request routed and how it ended, not `route_target` alone. Use `listUsageLedger` with filters mapped to `tenant_id`, `route_target`, `terminal_status`, `from_timestamp`, and `to_timestamp`. Render the results in `console/src/components/ledger/ledger-table.tsx` with columns `Timestamp`, `Tenant`, `Route target`, `Provider`, `Status`, `Latency`, and `Estimated cost`. Add `console/src/components/ledger/ledger-request-detail.tsx` as an adjacent detail panel fed by the selected row so the operator can inspect `request_id`, `route_reason`, `policy_outcome`, `fallback_used`, `cache_hit`, and token totals without leaving the page. Do not add charts or aggregate dashboards in this phase.</action>
  <verify>
    <automated>npm --prefix console run test -- --run ledger-filters ledger-table</automated>
  </verify>
  <acceptance_criteria>
    - console/src/app/(console)/observability/page.tsx contains 'Usage ledger'
    - console/src/components/ledger/ledger-filters.tsx contains 'Tenant', 'Route target', 'Terminal status', 'From', and 'To'
    - console/src/components/ledger/ledger-table.tsx contains 'Estimated cost'
    - console/src/components/ledger/ledger-table.tsx contains 'Route target' and 'Status'
    - console/src/components/ledger/ledger-request-detail.tsx contains 'policy_outcome' or 'Policy outcome'
    - console/src/components/ledger/ledger-table.test.tsx contains 'Latency'
    - console/src/app/(console)/observability/page.tsx contains 'terminal_status'
  </acceptance_criteria>
  <done>The console exposes an operator-friendly ledger workflow with the exact filters required by `OBS-01`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add dependency-health cards and observability browser coverage</name>
  <files>console/src/app/api/runtime/health/route.ts, console/src/app/(console)/observability/page.tsx, console/src/components/health/runtime-health-cards.tsx, console/src/components/health/runtime-health-cards.test.tsx, console/e2e/observability.spec.ts</files>
  <read_first>console/src/app/(console)/observability/page.tsx, console/src/lib/query-keys.ts, console/src/app/api/admin/[...path]/route.ts, src/nebula/services/runtime_health_service.py, tests/test_health.py, .planning/phases/03-playground-observability/03-RESEARCH.md, .planning/phases/03-playground-observability/03-VALIDATION.md</read_first>
  <behavior>
    - Test 1: The health section fetches `/api/runtime/health` and renders cards for `gateway`, `governance_store`, `semantic_cache`, `local_ollama`, and `premium_provider`.
    - Test 2: Cards distinguish `ready`, `degraded`, and `not_ready` with explicit text instead of relying on color alone.
    - Test 3: Browser coverage confirms the operator can filter the ledger and also see premium-provider degradation messaging on the same page.
  </behavior>
  <action>Create `console/src/app/api/runtime/health/route.ts` as a same-origin proxy to `/health/dependencies`; do not add admin-key auth because the backend health endpoint is already public. In `console/src/app/(console)/observability/page.tsx`, add a second query keyed by `queryKeys.runtimeHealth` and render it through `console/src/components/health/runtime-health-cards.tsx`. Each card must show the component name, the literal status string, and the backend-provided detail message. Include copy that says `Optional dependency degradation does not block gateway readiness.` when at least one optional dependency is degraded. Add `console/src/components/health/runtime-health-cards.test.tsx` for ready/degraded/not_ready rendering and `console/e2e/observability.spec.ts` that signs in, visits `/observability`, applies a tenant filter, checks ledger rows, and verifies that a degraded `premium_provider` card still coexists with gateway availability messaging.</action>
  <verify>
    <automated>npm --prefix console run e2e -- observability.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - console/src/app/api/runtime/health/route.ts contains '/health/dependencies'
    - console/src/components/health/runtime-health-cards.tsx contains 'premium_provider'
    - console/src/components/health/runtime-health-cards.tsx contains 'Optional dependency degradation does not block gateway readiness.'
    - console/src/components/health/runtime-health-cards.test.tsx contains 'degraded'
    - console/e2e/observability.spec.ts contains '/observability'
    - console/e2e/observability.spec.ts contains 'Dependency health'
  </acceptance_criteria>
  <done>The observability page covers both recorded request outcomes and dependency health, including premium-provider degradation states.</done>
</task>

</tasks>

<verification>
- [ ] `./.venv/bin/pytest tests/test_health.py -q` succeeds
- [ ] `npm --prefix console run test -- --run ledger-filters ledger-table runtime-health-cards` succeeds
- [ ] `npm --prefix console run e2e -- observability.spec.ts` succeeds
- [ ] `rg -n "premium_provider|Usage ledger|Dependency health|Estimated cost" src/nebula/services/runtime_health_service.py console/src/app/(console)/observability/page.tsx console/src/components/ledger/* console/src/components/health/*` finds the expected observability wiring
</verification>

<success_criteria>
- Operators can inspect usage-ledger entries by tenant, route outcome, and time window from the console.
- Operators can inspect dependency health for cache, local Ollama, and premium providers through a backend-owned runtime contract.
- Optional dependency degradation is clearly communicated without implying that the whole gateway is down.
</success_criteria>

<output>
After completion, create `.planning/phases/03-playground-observability/03-03-SUMMARY.md`
</output>
