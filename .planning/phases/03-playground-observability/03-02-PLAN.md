---
phase: 03-playground-observability
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - console/src/components/shell/operator-shell.tsx
  - console/src/app/(console)/playground/page.tsx
  - console/src/components/playground/playground-form.tsx
  - console/src/components/playground/playground-response.tsx
  - console/src/components/playground/playground-metadata.tsx
  - console/src/components/playground/playground-recorded-outcome.tsx
  - console/src/components/playground/playground-page.test.tsx
  - console/src/components/playground/playground-metadata.test.tsx
  - console/src/components/playground/playground-recorded-outcome.test.tsx
  - console/e2e/playground.spec.ts
autonomous: true
requirements:
  - PLAY-02
user_setup: []
must_haves:
  truths:
    - "Each Playground request immediately shows route target, provider, cache-hit status, fallback status, latency, and policy outcome."
    - "The Playground follows up by request id to show the recorded terminal outcome, token totals, and estimated premium cost from the usage ledger."
    - "Operators can distinguish immediate response metadata from persisted ledger data instead of seeing one ambiguous summary block."
  artifacts:
    - path: console/src/components/playground/playground-metadata.tsx
      provides: Immediate response metadata panel sourced from headers and client timing
      contains: "Route target"
    - path: console/src/components/playground/playground-recorded-outcome.tsx
      provides: Request-id correlated ledger enrichment panel
      contains: "Estimated cost"
    - path: console/src/app/(console)/playground/page.tsx
      provides: Combined Playground flow with mutation plus follow-up query
      contains: "Recorded outcome"
    - path: console/e2e/playground.spec.ts
      provides: Browser verification of metadata and ledger follow-up rendering
      contains: "fallback"
  key_links:
    - from: console/src/app/(console)/playground/page.tsx
      to: console/src/lib/admin-api.ts
      via: `createPlaygroundCompletion` mutation and `getUsageLedgerEntry` follow-up query
      pattern: "getUsageLedgerEntry"
    - from: console/src/components/playground/playground-metadata.tsx
      to: console/src/app/api/playground/completions/route.ts
      via: Header-derived request metadata
      pattern: "X-Nebula-Route-Target"
    - from: console/src/components/playground/playground-recorded-outcome.tsx
      to: src/nebula/api/routes/admin.py
      via: exact request-id ledger lookup
      pattern: "request_id"
---

<objective>
Surface route and cost metadata in the Playground UI.

Purpose: Turn the basic Playground flow into the Phase 3 product proof by exposing the routing, cache, fallback, latency, and recorded cost behavior that Nebula claims to optimize.
Output: Header-driven metadata cards, request-id correlated ledger enrichment, and regression coverage for the combined immediate-plus-recorded workflow.
</objective>

<execution_context>
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-playground-observability/03-RESEARCH.md
@.planning/phases/03-playground-observability/03-VALIDATION.md
@.planning/phases/03-playground-observability/03-01-SUMMARY.md
@src/nebula/api/routes/chat.py
@src/nebula/models/governance.py
@console/src/lib/admin-api.ts
@console/src/lib/query-keys.ts
@console/src/app/api/playground/completions/route.ts
@console/src/app/(console)/playground/page.tsx
@console/src/components/playground/playground-form.tsx
@console/src/components/playground/playground-response.tsx

<interfaces>
From src/nebula/api/routes/chat.py:
```python
response.headers.update(_nebula_headers(completion_envelope.metadata))
```

From console/src/lib/admin-api.ts after 03-01:
```typescript
export type PlaygroundCompletionResult = {
  body: PlaygroundResponse;
  requestId: string;
  tenantId: string;
  routeTarget: string;
  routeReason: string;
  provider: string;
  cacheHit: boolean;
  fallbackUsed: boolean;
  policyMode: string;
  policyOutcome: string;
}
export function createPlaygroundCompletion(adminKey: string, payload: PlaygroundInput): Promise<PlaygroundCompletionResult>
export function getUsageLedgerEntry(adminKey: string, requestId: string)
```

From src/nebula/models/governance.py:
```python
class UsageLedgerRecord(BaseModel):
    request_id: str
    final_route_target: str
    final_provider: str | None = None
    fallback_used: bool
    cache_hit: bool
    prompt_tokens: int = 0
    completion_tokens: int = 0
    total_tokens: int = 0
    estimated_cost: float | None = None
    terminal_status: TerminalStatus
    policy_outcome: str | None = None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render immediate route metadata from response headers and client timing</name>
  <files>console/src/components/shell/operator-shell.tsx, console/src/app/(console)/playground/page.tsx, console/src/components/playground/playground-form.tsx, console/src/components/playground/playground-response.tsx, console/src/components/playground/playground-metadata.tsx, console/src/components/playground/playground-metadata.test.tsx</files>
  <read_first>console/src/components/shell/operator-shell.tsx, console/src/app/(console)/playground/page.tsx, console/src/components/playground/playground-form.tsx, console/src/components/playground/playground-response.tsx, console/src/lib/admin-api.ts, console/src/lib/query-keys.ts, src/nebula/api/routes/chat.py, .planning/phases/03-playground-observability/03-RESEARCH.md</read_first>
  <behavior>
    - Test 1: A successful Playground mutation records `performance.now()` latency and renders it as an integer millisecond value.
    - Test 2: The response metadata panel shows exact fields `Route target`, `Provider`, `Cache hit`, `Fallback`, and `Policy outcome`.
    - Test 3: Boolean metadata values render as explicit `Yes` or `No`, not raw `true` or `false`.
  </behavior>
  <action>Update `console/src/components/shell/operator-shell.tsx` to add the Phase 3 primary nav entries `Playground` and `Observability`. Split the basic response rendering so `console/src/components/playground/playground-response.tsx` stays focused on assistant content while a new `console/src/components/playground/playground-metadata.tsx` renders immediate request details. In `console/src/app/(console)/playground/page.tsx`, consume the explicit `PlaygroundCompletionResult` returned by `createPlaygroundCompletion`, compute `latencyMs` from `performance.now()`, and normalize the returned fields into a stable metadata object with exact keys `routeTarget`, `provider`, `cacheHit`, `fallbackUsed`, `policyOutcome`, and `requestId`. Render those values in the page with labels exactly `Route target`, `Provider`, `Cache hit`, `Fallback`, `Latency`, and `Policy outcome`. Keep the data source boundaries explicit: latency comes from client timing, the rest come from the typed client contract sourced from response headers. Do not fetch ledger data in this task.</action>
  <verify>
    <automated>npm --prefix console run test -- --run playground-metadata</automated>
  </verify>
  <acceptance_criteria>
    - console/src/components/shell/operator-shell.tsx contains 'Playground'
    - console/src/components/shell/operator-shell.tsx contains 'Observability'
    - console/src/components/playground/playground-metadata.tsx contains 'Route target'
    - console/src/components/playground/playground-metadata.tsx contains 'Policy outcome'
    - console/src/app/(console)/playground/page.tsx contains 'performance.now()'
    - console/src/app/(console)/playground/page.tsx contains 'routeTarget' and 'fallbackUsed'
    - console/src/app/(console)/playground/page.tsx contains 'PlaygroundCompletionResult'
    - console/src/components/playground/playground-metadata.test.tsx contains 'Latency'
  </acceptance_criteria>
  <done>The Playground immediately exposes the route and policy metadata that came back with the completion response.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enrich the Playground with recorded outcome and cost from the usage ledger</name>
  <files>console/src/app/(console)/playground/page.tsx, console/src/components/playground/playground-recorded-outcome.tsx, console/src/components/playground/playground-recorded-outcome.test.tsx, console/src/components/playground/playground-page.test.tsx</files>
  <read_first>console/src/app/(console)/playground/page.tsx, console/src/lib/admin-api.ts, console/src/lib/query-keys.ts, src/nebula/models/governance.py, tests/test_admin_playground_api.py, .planning/phases/03-playground-observability/03-RESEARCH.md, .planning/phases/03-playground-observability/03-VALIDATION.md</read_first>
  <behavior>
    - Test 1: After a successful Playground mutation with request id `req-123`, the UI fetches `getUsageLedgerEntry(adminKey, "req-123")`.
    - Test 2: The recorded outcome panel renders `Terminal status`, `Prompt tokens`, `Completion tokens`, `Total tokens`, and `Estimated cost`.
    - Test 3: While the ledger lookup is pending, the UI shows a distinct loading state instead of blanking the immediate response content.
  </behavior>
  <action>Use TanStack Query in `console/src/app/(console)/playground/page.tsx` to trigger a follow-up query keyed by `queryKeys.usageLedgerEntry(requestId)` once the mutation resolves with a non-empty request id. Create `console/src/components/playground/playground-recorded-outcome.tsx` to render the persisted usage row with labels exactly `Recorded outcome`, `Terminal status`, `Prompt tokens`, `Completion tokens`, `Total tokens`, and `Estimated cost`. Keep the immediate metadata panel and the recorded-outcome panel visually separate, with helper text that says `Recorded usage from the Nebula ledger.`. If the ledger lookup fails, show an inline error block that keeps the immediate completion content and metadata visible. Use the persisted `estimated_cost`, `total_tokens`, and `terminal_status` values from the ledger response; do not recompute them in the browser.</action>
  <verify>
    <automated>npm --prefix console run test -- --run playground-page playground-recorded-outcome</automated>
  </verify>
  <acceptance_criteria>
    - console/src/components/playground/playground-recorded-outcome.tsx contains 'Recorded usage from the Nebula ledger.'
    - console/src/components/playground/playground-recorded-outcome.tsx contains 'Estimated cost'
    - console/src/app/(console)/playground/page.tsx contains 'queryKeys.usageLedgerEntry'
    - console/src/app/(console)/playground/page.tsx contains 'getUsageLedgerEntry'
    - console/src/components/playground/playground-page.test.tsx contains 'Terminal status'
  </acceptance_criteria>
  <done>The Playground shows both the immediate routing result and the recorded ledger outcome for the same request id.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend Playground browser coverage to the full metadata-plus-ledger flow</name>
  <files>console/e2e/playground.spec.ts</files>
  <read_first>console/e2e/playground.spec.ts, console/src/app/(console)/playground/page.tsx, console/src/components/playground/playground-metadata.tsx, console/src/components/playground/playground-recorded-outcome.tsx, .planning/phases/03-playground-observability/03-VALIDATION.md</read_first>
  <behavior>
    - Test 1: The browser flow shows response metadata immediately after submission.
    - Test 2: The browser flow then renders the correlated ledger values without a second manual action from the operator.
    - Test 3: The test asserts a fallback scenario where `Fallback` is `Yes` and the recorded terminal status is `fallback_completed`.
  </behavior>
  <action>Replace the minimal `console/e2e/playground.spec.ts` assertions from `03-01` with the full product-proof flow. Stub the Playground proxy response headers so the browser sees concrete values such as `X-Nebula-Route-Target=premium`, `X-Nebula-Provider=openai-compatible`, `X-Nebula-Cache-Hit=false`, `X-Nebula-Fallback-Used=true`, `X-Nebula-Policy-Outcome=allowed`, and `X-Request-ID=req-play-001`. Stub the ledger lookup that follows to return `terminal_status=fallback_completed`, non-zero token counts, and an `estimated_cost` value. Assert that the page renders the immediate metadata block first and the `Recorded outcome` block after the follow-up query resolves.</action>
  <verify>
    <automated>npm --prefix console run e2e -- playground.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - console/e2e/playground.spec.ts contains 'X-Nebula-Fallback-Used'
    - console/e2e/playground.spec.ts contains 'Recorded outcome'
    - console/e2e/playground.spec.ts contains 'fallback_completed'
    - console/e2e/playground.spec.ts contains 'Estimated cost'
  </acceptance_criteria>
  <done>The key Phase 3 Playground story is regression-tested at the browser level: request runs, metadata appears, and ledger enrichment follows.</done>
</task>

</tasks>

<verification>
- [ ] `npm --prefix console run test -- --run playground-page playground-metadata playground-recorded-outcome` succeeds
- [ ] `npm --prefix console run e2e -- playground.spec.ts` succeeds
- [ ] `rg -n "Route target|Recorded outcome|Estimated cost|getUsageLedgerEntry" console/src/app/(console)/playground/page.tsx console/src/components/playground/*` finds the expected metadata and ledger enrichment wiring
</verification>

<success_criteria>
- Each Playground request shows route target, provider, cache-hit status, fallback status, latency, and policy outcome.
- The same request id drives a ledger lookup that surfaces persisted terminal status, token usage, and estimated premium cost.
- Operators can see the distinction between immediate response metadata and backend-recorded outcome without leaving the Playground page.
</success_criteria>

<output>
After completion, create `.planning/phases/03-playground-observability/03-02-SUMMARY.md`
</output>
