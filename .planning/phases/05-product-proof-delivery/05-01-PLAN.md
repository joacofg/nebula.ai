---
phase: 05-product-proof-delivery
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/nebula/benchmarking/run.py
  - src/nebula/benchmarking/dataset.py
  - benchmarks/v1/demo-scenarios.jsonl
  - Makefile
  - tests/test_benchmarking.py
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
user_setup: []
must_haves:
  truths:
    - "Operators can run one canonical benchmark suite plus a smaller live-demo subset from the repo without inventing extra tooling."
    - "Benchmark artifacts make Nebula's premium-cost avoidance, route outcomes, cache behavior, fallback behavior, and latency story legible without reading raw rows first."
    - "Benchmark reports stay repo-native and reproducible, with JSON and Markdown artifacts written under `artifacts/benchmarks/<timestamp>/`."
  artifacts:
    - path: src/nebula/benchmarking/run.py
      provides: Canonical report-building and Markdown rendering for the benchmark proof package
      contains: "_render_markdown"
    - path: benchmarks/v1/demo-scenarios.jsonl
      provides: Demo-sized benchmark subset covering the live proof story
      contains: "\"mode\":\"auto_fallback\""
    - path: tests/test_benchmarking.py
      provides: Automated coverage for summary fields, canonical group framing, and demo-subset behavior
      contains: "test_benchmark_runner_builds_report_and_markdown_shapes"
  key_links:
    - from: src/nebula/benchmarking/dataset.py
      to: src/nebula/benchmarking/run.py
      via: canonical scenario-group ordering and human-facing comparison framing used by the report builder
      pattern: "SCENARIO_MODE_ORDER"
    - from: Makefile
      to: benchmarks/v1/demo-scenarios.jsonl
      via: dedicated benchmark-demo command reusing the existing runner with the demo dataset
      pattern: "benchmark-demo"
    - from: tests/test_benchmarking.py
      to: src/nebula/benchmarking/run.py
      via: report-shape assertions for executive summary, comparison groups, and mismatch notes
      pattern: "_build_report"
---

<objective>
Strengthen Nebula's benchmark workflow so it proves the product story with repeatable artifacts instead of raw logs alone.

Purpose: Satisfy `EVAL-01` and `EVAL-02` by keeping the existing black-box benchmark runner, adding a stronger Markdown/JSON summary layer, and shipping a smaller live-demo subset without widening product scope.
Output: Updated benchmark report generation, a demo benchmark dataset/command, and tests that lock the Phase 5 proof contract.
</objective>

<execution_context>
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-product-proof-delivery/05-CONTEXT.md
@.planning/phases/05-product-proof-delivery/05-RESEARCH.md
@src/nebula/benchmarking/run.py
@src/nebula/benchmarking/dataset.py
@benchmarks/v1/scenarios.jsonl
@benchmarks/pricing.json
@artifacts/benchmarks/20260314T193127Z/report.md
@tests/test_benchmarking.py
@Makefile

<interfaces>
From src/nebula/benchmarking/dataset.py:
```python
ScenarioMode = Literal[
    "premium_direct",
    "local_direct",
    "auto_simple_cold",
    "auto_simple_warm",
    "auto_complex",
    "auto_fallback",
]

SCENARIO_MODE_ORDER: tuple[ScenarioMode, ...] = (
    "premium_direct",
    "local_direct",
    "auto_simple_cold",
    "auto_simple_warm",
    "auto_complex",
    "auto_fallback",
)
```

From src/nebula/benchmarking/run.py:
```python
class BenchmarkRunner:
    async def run(self) -> tuple[dict[str, object], Path]:
        ...

    def _build_report(self, results: list[BenchmarkResult]) -> dict[str, object]:
        ...

    def _render_markdown(self, report: dict[str, object]) -> str:
        ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a human-readable benchmark summary layer on top of the existing raw report rows</name>
  <files>src/nebula/benchmarking/run.py, src/nebula/benchmarking/dataset.py, tests/test_benchmarking.py</files>
  <read_first>src/nebula/benchmarking/run.py, src/nebula/benchmarking/dataset.py, tests/test_benchmarking.py, artifacts/benchmarks/20260314T193127Z/report.md, .planning/phases/05-product-proof-delivery/05-RESEARCH.md</read_first>
  <behavior>
    - Test 1: `tests/test_benchmarking.py` proves `BenchmarkRunner._build_report()` returns a summary block that surfaces top-line cost-avoidance, route-distribution, latency, token-usage, and expectation-mismatch information without dropping the existing per-result rows.
    - Test 2: The report exposes human-facing comparison groups for the locked Phase 5 story: premium control, local control, auto-routing cold, auto-routing warm-cache, and fallback resilience; `auto_complex` remains covered but is clearly treated as supporting premium-routed evidence rather than replacing the top-line five-group story.
    - Test 3: `_render_markdown()` writes a summary-first Markdown report with explicit key takeaways, concise comparison tables, route/cost highlights, and failure notes before the raw scenario table.
    - Test 4: Existing route-distribution, fallback-rate, and scenario-failure expectations stay intact so this remains a report-shape enhancement, not a new benchmark engine.
  </behavior>
  <action>Start by extending `tests/test_benchmarking.py` so the suite fails on the current raw-only report contract. In `src/nebula/benchmarking/dataset.py`, add the smallest helper structure needed to map scenario modes into the locked Phase 5 human-facing comparison story while keeping the existing versioned JSONL dataset and black-box runner architecture intact. In `src/nebula/benchmarking/run.py`, update `_build_report()` to emit a summary-first proof package that keeps the current totals but also adds: a concise executive/takeaway block, comparison-group summaries with latency and cost signals, route-distribution and cost-avoidance highlights, and explicit expectation-mismatch notes for any scenario whose observed result diverges from `scenario.expect`. Preserve the raw `results` rows and the current artifact directory lifecycle. In `_render_markdown()`, place the new summary material ahead of the scenario table and keep the output lightweight Markdown rather than charts, dashboards, or notebook dependencies. Do not introduce a new benchmark service, alternate storage backend, or non-repo reporting tool.</action>
  <verify>
    <automated>.venv/bin/pytest tests/test_benchmarking.py -q</automated>
  </verify>
  <acceptance_criteria>
    - src/nebula/benchmarking/run.py contains "## Key Takeaways"
    - src/nebula/benchmarking/run.py contains "comparison_groups"
    - src/nebula/benchmarking/run.py contains "expectation_mismatches"
    - src/nebula/benchmarking/dataset.py contains a human-facing comparison-group mapping for the canonical benchmark story
    - tests/test_benchmarking.py asserts summary-first report fields in both JSON and Markdown shapes
  </acceptance_criteria>
  <done>The canonical benchmark report proves savings and resilience in a summary-first JSON/Markdown package without abandoning the existing raw result rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship a live-demo benchmark subset and repo-native execution path without changing the canonical suite</name>
  <files>benchmarks/v1/demo-scenarios.jsonl, Makefile, tests/test_benchmarking.py</files>
  <read_first>benchmarks/v1/scenarios.jsonl, Makefile, tests/test_benchmarking.py, src/nebula/benchmarking/run.py, .planning/phases/05-product-proof-delivery/05-CONTEXT.md</read_first>
  <behavior>
    - Test 1: A new demo dataset exists under `benchmarks/v1/demo-scenarios.jsonl` and covers one concise scenario for each live-proof step: premium control, local control, auto-routing cold, auto-routing warm-cache, and fallback resilience.
    - Test 2: `Makefile` exposes a `benchmark-demo` entrypoint that reuses the existing benchmark runner with the demo dataset instead of adding a second runner implementation.
    - Test 3: The canonical dataset in `benchmarks/v1/scenarios.jsonl` remains the full repeatable suite, including `auto_complex`, so the product-proof package keeps its deeper premium-routed coverage.
    - Test 4: Automated benchmark tests prove the new demo subset is valid, ordered, and compatible with the existing dataset-loading/reporting path.
  </behavior>
  <action>Add `benchmarks/v1/demo-scenarios.jsonl` as a deliberate subset of the existing versioned scenario set: keep it small enough for a live demo, but include exactly the story beats locked in context rather than arbitrary prompts. Update `Makefile` with a `benchmark-demo` target that invokes the existing runner using `--dataset benchmarks/v1/demo-scenarios.jsonl`; do not add a new CLI, wrapper service, or special-case code path for the demo mode if `--dataset` already solves it. Extend `tests/test_benchmarking.py` to assert the subset loads successfully, preserves the intended mode ordering, and stays compatible with the full report generation flow. Keep the full benchmark suite as the primary proof path and treat the demo subset as a faster operator-facing slice of the same evidence model.</action>
  <verify>
    <automated>.venv/bin/pytest tests/test_benchmarking.py -q</automated>
  </verify>
  <acceptance_criteria>
    - benchmarks/v1/demo-scenarios.jsonl exists
    - benchmarks/v1/demo-scenarios.jsonl contains "\"mode\":\"premium_direct\""
    - benchmarks/v1/demo-scenarios.jsonl contains "\"mode\":\"local_direct\""
    - benchmarks/v1/demo-scenarios.jsonl contains "\"mode\":\"auto_simple_cold\""
    - benchmarks/v1/demo-scenarios.jsonl contains "\"mode\":\"auto_simple_warm\""
    - benchmarks/v1/demo-scenarios.jsonl contains "\"mode\":\"auto_fallback\""
    - Makefile contains "benchmark-demo:"
    - Makefile contains "--dataset benchmarks/v1/demo-scenarios.jsonl"
  </acceptance_criteria>
  <done>Nebula ships both a full benchmark proof suite and a smaller live-demo subset through the same runner and artifact lifecycle.</done>
</task>

</tasks>

<verification>
- [ ] `.venv/bin/pytest tests/test_benchmarking.py -q` succeeds
- [ ] `rg -n "comparison_groups|expectation_mismatches|benchmark-demo|demo-scenarios" src/nebula/benchmarking/run.py src/nebula/benchmarking/dataset.py Makefile tests/test_benchmarking.py benchmarks/v1/demo-scenarios.jsonl` returns the Phase 5 benchmark-proof contract markers
</verification>

<success_criteria>
- `EVAL-01` is satisfied with one repeatable canonical benchmark suite and one faster live-demo subset.
- `EVAL-02` is satisfied with JSON and Markdown artifacts that summarize route outcome, latency, token usage, estimated premium cost, avoided cost, and scenario expectation mismatches.
- The benchmark proof remains black-box, repo-native, and grounded in the existing runner/dataset architecture.
</success_criteria>

<output>
After completion, create `.planning/phases/05-product-proof-delivery/05-01-SUMMARY.md`
</output>
