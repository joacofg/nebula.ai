---
phase: 05-product-proof-delivery
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - README.md
  - docs/self-hosting.md
  - docs/architecture.md
  - docs/evaluation.md
  - docs/demo-script.md
autonomous: true
requirements:
  - DOCS-01
user_setup: []
must_haves:
  truths:
    - "An engineer can open the repo and understand what Nebula is, why it exists, and where to go for deployment, architecture, and evaluation details."
    - "The deployment guide stays aligned with the supported Compose/env path instead of drifting into a second unofficial deployment story."
    - "The docs explain Nebula's architecture and benchmark-evaluation method clearly enough for pilot onboarding and academic review."
  artifacts:
    - path: README.md
      provides: Product entrypoint with proof points and links into focused docs
      contains: "Product proof"
    - path: docs/architecture.md
      provides: Repo-local explanation of gateway, governance, cache, provider, console, and benchmark architecture
      contains: "Request flow"
    - path: docs/evaluation.md
      provides: Benchmark/evaluation guide anchored to the existing runner and artifact outputs
      contains: "benchmark-demo"
    - path: docs/demo-script.md
      provides: Benchmark-led live walkthrough that ties the artifact proof to Playground and Observability
      contains: "Observability"
    - path: docs/self-hosting.md
      provides: Canonical supported deployment path for pilot onboarding
      contains: "docker compose -f docker-compose.selfhosted.yml up -d"
  key_links:
    - from: README.md
      to: docs/self-hosting.md
      via: explicit deployment entrypoint link for the supported Compose path
      pattern: "self-hosting"
    - from: README.md
      to: docs/evaluation.md
      via: product-proof section linking to benchmark commands and artifact interpretation
      pattern: "benchmark"
    - from: docs/evaluation.md
      to: artifacts/benchmarks/20260314T193127Z/report.md
      via: explanation of how to read the summary-first benchmark artifact shape produced by Plan 05-01
      pattern: "report.md"
    - from: docs/demo-script.md
      to: Makefile
      via: scripted use of `make benchmark-demo` before the live console walkthrough
      pattern: "benchmark-demo"
---

<objective>
Produce the focused documentation layer Nebula needs for pilot onboarding and academic evaluation.

Purpose: Satisfy `DOCS-01` by turning the root README into a fast product entrypoint, preserving the canonical self-hosted deployment runbook, and adding dedicated architecture, evaluation, and demo docs grounded in the existing stack and benchmark artifacts.
Output: Updated root README plus focused deployment, architecture, evaluation, and demo documentation with repo-native verification commands.
</objective>

<execution_context>
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-product-proof-delivery/05-CONTEXT.md
@.planning/phases/05-product-proof-delivery/05-RESEARCH.md
@.planning/phases/05-product-proof-delivery/05-01-PLAN.md
@.planning/phases/05-product-proof-delivery/05-VALIDATION.md
@README.md
@docs/self-hosting.md
@docker-compose.selfhosted.yml
@deploy/selfhosted.env.example
@src/nebula/benchmarking/run.py
@artifacts/benchmarks/20260314T193127Z/report.md

<interfaces>
From README.md / Makefile:
```text
make benchmark
BASE_URL=http://127.0.0.1:8000 .venv/bin/python -m nebula.benchmarking.run
docker compose -f docker-compose.selfhosted.yml up -d
```

From docker-compose.selfhosted.yml:
```yaml
services:
  console:
    ports:
      - "3000:3000"
  nebula:
    command:
      - alembic upgrade head && uvicorn nebula.main:app --host 0.0.0.0 --port 8000
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the README into a product entrypoint and keep the self-hosted runbook canonical</name>
  <files>README.md, docs/self-hosting.md</files>
  <read_first>README.md, docs/self-hosting.md, docker-compose.selfhosted.yml, deploy/selfhosted.env.example, .planning/phases/05-product-proof-delivery/05-CONTEXT.md, .planning/phases/05-product-proof-delivery/05-RESEARCH.md</read_first>
  <action>Restructure `README.md` so it becomes the fast product entrypoint instead of a mixed local-dev dump. It should explain what Nebula is, who it is for, the concrete value claim around premium-cost avoidance plus visible fallback/degraded behavior, and where readers should go next for deployment, architecture, evaluation, and demo guidance. Keep `docs/self-hosting.md` as the single supported deployment runbook rooted in `docker-compose.selfhosted.yml` and `deploy/selfhosted.env.example`; tighten wording where needed so the docs clearly distinguish local development from the supported self-hosted pilot path. Do not introduce a second deployment topology, hosted-product framing, or enterprise packaging that Phase 5 explicitly defers.</action>
  <verify>
    <automated>test -f README.md && test -f docs/self-hosting.md && rg -n "Product proof|self-hosting|docker compose -f docker-compose.selfhosted.yml up -d|NEBULA_ADMIN_API_KEY" README.md docs/self-hosting.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains a product/value section for Nebula
    - README.md links to the focused docs instead of carrying every detail inline
    - docs/self-hosting.md remains the canonical supported deployment path
    - docs/self-hosting.md references docker-compose.selfhosted.yml and deploy/selfhosted.env.example consistently
  </acceptance_criteria>
  <done>The repo entrypoint is product-oriented and the supported self-hosted deployment path remains singular, explicit, and pilot-ready.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused architecture, evaluation, and demo docs tied to the existing runtime and benchmark artifacts</name>
  <files>docs/architecture.md, docs/evaluation.md, docs/demo-script.md, README.md</files>
  <read_first>src/nebula/benchmarking/run.py, artifacts/benchmarks/20260314T193127Z/report.md, README.md, .planning/PROJECT.md, .planning/phases/03-playground-observability/03-03-SUMMARY.md, .planning/phases/05-product-proof-delivery/05-01-PLAN.md</read_first>
  <action>Create `docs/architecture.md` to explain the deployed system in product terms: request flow through the FastAPI gateway, governance/policy enforcement, Qdrant semantic cache, premium/local providers, usage ledger, console, and benchmark harness. Create `docs/evaluation.md` to explain the canonical benchmark suite, the demo subset, the artifact outputs (`report.json` and `report.md`), how to interpret cost-avoidance and degraded/fallback evidence, and the limits of estimated premium cost versus real billing. Create `docs/demo-script.md` as the benchmark-led live walkthrough tied to the existing console and observability surfaces: briefly establish the operator console, run `make benchmark-demo`, show the generated proof artifact and its savings claim, then move into the Playground for one live request and finish on the Observability page with one deliberate degraded/fallback explanation. Update `README.md` links so these docs are discoverable from the entrypoint. Keep all three docs brownfield and source-backed: no imaginary services, dashboards, or new runtime features.</action>
  <verify>
    <automated>test -f docs/architecture.md && test -f docs/evaluation.md && test -f docs/demo-script.md && rg -n "Request flow|Qdrant|usage ledger|benchmark-demo|report.md|avoided cost|fallback|Observability" README.md docs/architecture.md docs/evaluation.md docs/demo-script.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/architecture.md exists
    - docs/architecture.md explains request flow and deployed components already present in the repo
    - docs/evaluation.md exists
    - docs/evaluation.md references both the full benchmark suite and the demo subset
    - docs/evaluation.md explains how to read report.json/report.md and the premium-cost estimates
    - docs/demo-script.md exists
    - docs/demo-script.md references `make benchmark-demo`, Playground, and Observability
  </acceptance_criteria>
  <done>The repo contains focused architecture, evaluation, and demo docs that are accurate to the implemented Nebula stack and benchmark proof path.</done>
</task>

</tasks>

<verification>
- [ ] `test -f README.md && test -f docs/self-hosting.md && test -f docs/architecture.md && test -f docs/evaluation.md && test -f docs/demo-script.md` succeeds
- [ ] `rg -n "Product proof|self-hosting|Request flow|benchmark-demo|report.md|avoided cost|fallback|Observability" README.md docs/self-hosting.md docs/architecture.md docs/evaluation.md docs/demo-script.md` returns the expected documentation markers
</verification>

<success_criteria>
- `DOCS-01` is satisfied for the deployment guide, architecture explanation, and evaluation guidance.
- `DOCS-01` includes the demo script in the same documentation wave as the roadmap's deployment and architecture docs.
- The docs stay anchored to the existing Compose/env path, benchmark runner, and current product surfaces instead of inventing new scope.
- README becomes a fast navigation layer and the focused docs absorb the deeper operational and evaluation detail.
</success_criteria>

<output>
After completion, create `.planning/phases/05-product-proof-delivery/05-02-SUMMARY.md`
</output>
