---
phase: 09-audited-remote-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/nebula/models/deployment.py
  - src/nebula/db/models.py
  - src/nebula/services/remote_management_service.py
  - src/nebula/api/routes/enrollment.py
  - src/nebula/core/container.py
  - migrations/versions/20260322_0005_remote_actions.py
  - console/src/lib/admin-api.ts
  - tests/test_remote_management_api.py
autonomous: true
requirements:
  - RMGT-01
  - RMGT-03

must_haves:
  truths:
    - "Operator can queue exactly one allowlisted remote action type named rotate_deployment_credential for an active linked deployment"
    - "Duplicate queue requests for the same deployment and action type return the existing live action record instead of inserting a second live row"
    - "Queued actions persist expires_at, reason, and queued/applied/failed timestamps in hosted storage"
    - "Hosted queue creation fails closed for pending, revoked, unlinked, stale-incompatible, or unsupported deployments"
  artifacts:
    - path: "src/nebula/models/deployment.py"
      provides: "Remote action Pydantic types used by API and services"
      exports: ["RemoteActionType", "RemoteActionStatus", "RemoteActionRecord", "RemoteActionQueueRequest"]
    - path: "src/nebula/db/models.py"
      provides: "Remote action ORM persistence model"
      contains: "class RemoteActionModel"
    - path: "src/nebula/services/remote_management_service.py"
      provides: "Hosted-side queue creation, dedupe, history listing, and expiry normalization"
      contains: "def queue_rotate_deployment_credential"
    - path: "src/nebula/api/routes/enrollment.py"
      provides: "Admin endpoints for queueing and viewing deployment remote actions"
      contains: "/remote-actions"
    - path: "migrations/versions/20260322_0005_remote_actions.py"
      provides: "Alembic migration adding remote_actions table"
    - path: "tests/test_remote_management_api.py"
      provides: "Hosted queue contract coverage for RMGT-01"
      contains: "test_queue_rotate_deployment_credential"
  key_links:
    - from: "src/nebula/api/routes/enrollment.py"
      to: "src/nebula/services/remote_management_service.py"
      via: "container.remote_management_service"
      pattern: "remote_management_service\\."
    - from: "src/nebula/services/remote_management_service.py"
      to: "src/nebula/db/models.py"
      via: "RemoteActionModel CRUD and DeploymentModel gating"
      pattern: "RemoteActionModel|DeploymentModel"
    - from: "console/src/lib/admin-api.ts"
      to: "/api/admin/deployments/{id}/remote-actions"
      via: "adminRequest remote action helpers"
      pattern: "remote-actions"
---

<objective>
Define the initial allowlisted remote action and the hosted queue contract.

Purpose: This plan establishes the durable hosted-side source of truth for `rotate_deployment_credential`, including queue creation, dedupe, expiry timestamps, and deployment-scoped history. It gives later plans a stable contract for polling, apply, and audit summary projection.

Output: Hosted remote-action types, DB table, admin queue/history endpoints, TypeScript client types, and backend integration tests for RMGT-01.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-audited-remote-management/09-CONTEXT.md
@.planning/phases/09-audited-remote-management/09-RESEARCH.md
@.planning/phases/09-audited-remote-management/09-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add remote-action models, migration, hosted queue service, and admin endpoints</name>
  <files>
    src/nebula/models/deployment.py,
    src/nebula/db/models.py,
    src/nebula/services/remote_management_service.py,
    src/nebula/api/routes/enrollment.py,
    src/nebula/core/container.py,
    migrations/versions/20260322_0005_remote_actions.py,
    console/src/lib/admin-api.ts,
    tests/test_remote_management_api.py
  </files>
  <read_first>
    src/nebula/models/deployment.py,
    src/nebula/db/models.py,
    src/nebula/services/enrollment_service.py,
    src/nebula/services/heartbeat_ingest_service.py,
    src/nebula/api/routes/enrollment.py,
    src/nebula/core/container.py,
    console/src/lib/admin-api.ts,
    tests/test_enrollment_api.py,
    tests/test_heartbeat_api.py,
    tests/support.py,
    .planning/phases/09-audited-remote-management/09-CONTEXT.md,
    .planning/phases/09-audited-remote-management/09-RESEARCH.md
  </read_first>
  <action>
    **1. Extend `src/nebula/models/deployment.py`** with the exact remote-action API contract:

    - Add `RemoteActionType = Literal["rotate_deployment_credential"]`
    - Add `RemoteActionStatus = Literal["queued", "applied", "failed"]`
    - Add `RemoteActionQueueRequest(BaseModel)` with `reason: str`
    - Add `RemoteActionRecord(BaseModel)` with these exact fields:
      `id`, `deployment_id`, `action_type`, `status`, `reason`, `expires_at`, `queued_at`, `claimed_at`, `applied_at`, `failed_at`, `failure_reason`, `failure_detail`, `created_at`, `updated_at`
    - Add `RemoteActionSummaryRecord(BaseModel)` with:
      `queued`, `applied`, `failed`, `last_action_at`

    **2. Extend `src/nebula/db/models.py`** with `RemoteActionModel` using these exact columns:
      `id` `String(255)` primary key,
      `deployment_id` foreign key to `deployments.id`,
      `action_type` `String(64)` non-null,
      `status` `String(32)` non-null,
      `reason` `Text()` non-null,
      `expires_at` `DateTime(timezone=True)` non-null,
      `queued_at` `DateTime(timezone=True)` non-null,
      `claimed_at` `DateTime(timezone=True)` nullable,
      `applied_at` `DateTime(timezone=True)` nullable,
      `failed_at` `DateTime(timezone=True)` nullable,
      `failure_reason` `String(64)` nullable,
      `failure_detail` `Text()` nullable,
      `pending_credential_hash` `String(64)` nullable,
      `pending_credential_prefix` `String(32)` nullable,
      `created_at` `DateTime(timezone=True)` non-null,
      `updated_at` `DateTime(timezone=True)` non-null

    **3. Create `migrations/versions/20260322_0005_remote_actions.py`**:
    - Add `remote_actions` table with the exact columns above
    - Add an index on `(deployment_id, action_type, status)`
    - Use the repo's idempotent inspector pattern before creating the table or index

    **4. Create `src/nebula/services/remote_management_service.py`** with a hosted-side `RemoteManagementService` that implements these exact methods:
    - `queue_rotate_deployment_credential(deployment_id: str, reason: str) -> RemoteActionRecord`
    - `list_remote_actions(deployment_id: str) -> list[RemoteActionRecord]`
    - `_expire_live_actions(now: datetime) -> None`
    - `_to_record(model: RemoteActionModel) -> RemoteActionRecord`

    In `queue_rotate_deployment_credential` implement these exact rules:
    - Load the deployment row
    - If deployment missing: raise `KeyError("Deployment not found")`
    - If `deployment.enrollment_state != "active"`: raise `ValueError("Remote actions require an active linked deployment.")`
    - If `deployment.last_seen_at is None` OR `compute_freshness(deployment.last_seen_at)[0] in {"stale", "offline"}`: raise `ValueError("Remote action unavailable for stale or offline deployment.")`
    - If `"remote_credential_rotation"` not in `deployment.capability_flags_json`: raise `ValueError("Deployment does not support rotate_deployment_credential.")`
    - Normalize expired queued rows first by setting `status="failed"`, `failed_at=now`, `failure_reason="expired"`, `failure_detail="Action expired before the deployment claimed it."`
    - Under `select(RemoteActionModel)...with_for_update()`, search for an existing live row where `deployment_id` matches, `action_type == "rotate_deployment_credential"`, and `status == "queued"`
    - If found, return that existing row without inserting a duplicate
    - If not found, insert a new row with:
      `status="queued"`, `reason=reason.strip()`, `expires_at = now + timedelta(minutes=15)`, `queued_at=now`, `created_at=now`, `updated_at=now`

    **5. Register `RemoteManagementService` in `src/nebula/core/container.py`** as `self.remote_management_service = RemoteManagementService(session_factory=create_session_factory(settings))`

    **6. Extend `src/nebula/api/routes/enrollment.py`** with these exact admin endpoints:
    - `POST /admin/deployments/{deployment_id}/remote-actions/rotate-credential`
      request body: `RemoteActionQueueRequest`
      response model: `RemoteActionRecord`
    - `GET /admin/deployments/{deployment_id}/remote-actions`
      response model: `list[RemoteActionRecord]`

    Endpoint behavior:
    - Require admin auth
    - Convert `KeyError` to `404`
    - Convert `ValueError` to `409`
    - Queue endpoint must call `container.remote_management_service.queue_rotate_deployment_credential`
    - History endpoint must call `container.remote_management_service.list_remote_actions`

    **7. Extend `console/src/lib/admin-api.ts`** with:
    - `RemoteActionStatus`, `RemoteActionRecord`, and `RemoteActionQueueInput`
    - `queueRotateDeploymentCredential(adminKey: string, deploymentId: string, payload: { reason: string })`
    - `listDeploymentRemoteActions(adminKey: string, deploymentId: string)`
    - Use the exact admin endpoint paths added above

    **8. Add `tests/test_remote_management_api.py`** with these exact tests:
    - `test_queue_rotate_deployment_credential`: active deployment with `last_seen_at` set and `capability_flags_json=["remote_credential_rotation"]` returns `200`, `status=="queued"`, `action_type=="rotate_deployment_credential"`, and non-null `expires_at`
    - `test_queue_duplicate_returns_existing_live_action`: same deployment + same request twice returns the same `id`
    - `test_queue_fails_for_pending_or_revoked_deployment`: returns `409`
    - `test_queue_fails_for_stale_deployment`: set `last_seen_at` older than one hour and assert `409`
    - `test_queue_fails_without_remote_rotation_capability`: returns `409`
    - `test_list_remote_actions_returns_deployment_history`: queue an action and assert the GET endpoint returns a one-item list with matching `id`
  </action>
  <verify>
    <automated>pytest tests/test_remote_management_api.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/nebula/models/deployment.py` contains `RemoteActionType = Literal["rotate_deployment_credential"]`
    - `src/nebula/db/models.py` contains `class RemoteActionModel`
    - `src/nebula/services/remote_management_service.py` contains `def queue_rotate_deployment_credential(` and `def list_remote_actions(`
    - `src/nebula/api/routes/enrollment.py` contains `/remote-actions/rotate-credential` and `async def list_remote_actions(`
    - `console/src/lib/admin-api.ts` contains `queueRotateDeploymentCredential` and `listDeploymentRemoteActions`
    - `tests/test_remote_management_api.py` contains `test_queue_duplicate_returns_existing_live_action`
    - `pytest tests/test_remote_management_api.py -x` exits 0
  </acceptance_criteria>
</task>

</tasks>
