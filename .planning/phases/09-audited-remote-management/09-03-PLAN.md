---
phase: 09-audited-remote-management
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - src/nebula/core/container.py
  - src/nebula/models/deployment.py
  - src/nebula/models/hosted_contract.py
  - src/nebula/services/remote_management_service.py
  - src/nebula/services/enrollment_service.py
  - src/nebula/api/routes/enrollment.py
  - console/src/lib/admin-api.ts
  - console/src/components/deployments/deployment-detail-drawer.tsx
  - console/src/components/deployments/remote-action-card.tsx
  - console/src/components/deployments/remote-action-card.test.tsx
  - console/src/components/deployments/deployment-detail-drawer.test.tsx
  - tests/test_remote_management_audit.py
  - tests/test_hosted_contract.py
autonomous: true
requirements:
  - RMGT-01
  - RMGT-02
  - RMGT-03

must_haves:
  truths:
    - "Expired queued actions become terminal failed audit records with failure_reason='expired' and remain visible in deployment history"
    - "Deployment APIs expose remote action summary and recent history needed by the hosted drawer"
    - "The hosted drawer offers one confirmation-gated rotate credential action with a required reason field and disabled states for unsupported deployments"
    - "remote_action_summary stays consistent with hosted_contract expectations"
  artifacts:
    - path: "src/nebula/services/remote_management_service.py"
      provides: "expiry normalization, history projection, and per-deployment summary"
      contains: "def summarize_remote_actions"
    - path: "src/nebula/services/enrollment_service.py"
      provides: "DeploymentRecord projection including remote action summary"
      contains: "remote_action_summary"
    - path: "console/src/components/deployments/remote-action-card.tsx"
      provides: "action trigger, reason input, confirmation, and history section"
      contains: "rotate_deployment_credential"
    - path: "tests/test_remote_management_audit.py"
      provides: "TTL, idempotency, and audit-history coverage"
      contains: "test_expired_action_is_preserved_in_history"
  key_links:
    - from: "src/nebula/services/enrollment_service.py"
      to: "src/nebula/services/remote_management_service.py"
      via: "remote action summary projection onto deployment responses"
      pattern: "remote_action_summary|summarize_remote_actions"
    - from: "console/src/components/deployments/deployment-detail-drawer.tsx"
      to: "console/src/components/deployments/remote-action-card.tsx"
      via: "deployment-scoped action UI composition"
      pattern: "RemoteActionCard"
    - from: "src/nebula/models/hosted_contract.py"
      to: "tests/test_hosted_contract.py"
      via: "remote_action_summary schema lock"
      pattern: "remote_action_summary"
---

<objective>
Add TTL, idempotency, and hosted/local audit history surfaces for the remote-management workflow.

Purpose: This plan finishes Phase 9 by making the workflow operator-visible and auditable. It projects summary/history into deployment responses, ensures expired or failed actions remain visible, and adds the hosted drawer action surface with confirmation and reason capture.

Output: Expiry-safe audit history, deployment-level remote action summary, hosted drawer action UI, and tests covering RMGT-03 plus final operator-facing behavior.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-audited-remote-management/09-CONTEXT.md
@.planning/phases/09-audited-remote-management/09-RESEARCH.md
@.planning/phases/09-audited-remote-management/09-VALIDATION.md
@.planning/phases/09-audited-remote-management/09-01-PLAN.md
@.planning/phases/09-audited-remote-management/09-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project remote-action summary/history and build the hosted drawer action surface</name>
  <files>
    src/nebula/core/container.py,
    src/nebula/models/deployment.py,
    src/nebula/models/hosted_contract.py,
    src/nebula/services/remote_management_service.py,
    src/nebula/services/enrollment_service.py,
    src/nebula/api/routes/enrollment.py,
    console/src/lib/admin-api.ts,
    console/src/components/deployments/deployment-detail-drawer.tsx,
    console/src/components/deployments/remote-action-card.tsx,
    console/src/components/deployments/remote-action-card.test.tsx,
    console/src/components/deployments/deployment-detail-drawer.test.tsx,
    tests/test_remote_management_audit.py,
    tests/test_hosted_contract.py
  </files>
  <read_first>
    src/nebula/core/container.py,
    src/nebula/models/deployment.py,
    src/nebula/models/hosted_contract.py,
    src/nebula/services/remote_management_service.py,
    src/nebula/services/enrollment_service.py,
    src/nebula/api/routes/enrollment.py,
    console/src/components/deployments/deployment-detail-drawer.tsx,
    console/src/lib/admin-api.ts,
    console/src/components/deployments/freshness-badge.tsx,
    console/src/components/deployments/deployment-status-badge.tsx,
    console/src/components/deployments/deployment-detail-drawer.test.tsx,
    tests/test_hosted_contract.py,
    .planning/phases/09-audited-remote-management/09-CONTEXT.md,
    .planning/phases/09-audited-remote-management/09-RESEARCH.md
  </read_first>
  <action>
    **1. Extend `src/nebula/models/deployment.py`**:
    - Add `remote_action_summary: RemoteActionSummaryRecord | None = None` to `DeploymentRecord`
    - Add `recent_remote_actions: list[RemoteActionRecord] = []` to `DeploymentRecord`

    **2. Extend `src/nebula/services/remote_management_service.py`** with these exact methods:
    - `summarize_remote_actions(deployment_id: str) -> RemoteActionSummaryRecord`
    - `list_recent_remote_actions(deployment_id: str, limit: int = 5) -> list[RemoteActionRecord]`
    - Ensure both methods call the same expiry-normalization path before reading history so queued-but-expired rows become failed rows with `failure_reason="expired"`

    `summarize_remote_actions` must compute:
    - `queued` = count of rows with `status=="queued"`
    - `applied` = count of rows with `status=="applied"`
    - `failed` = count of rows with `status=="failed"`
    - `last_action_at` = max of `applied_at`, `failed_at`, or `queued_at` for the deployment

    **3. Extend `src/nebula/services/enrollment_service.py`** `_to_record()` to populate:
    - `remote_action_summary=self._remote_management_service.summarize_remote_actions(model.id)`
    - `recent_remote_actions=self._remote_management_service.list_recent_remote_actions(model.id, limit=5)`

    **4. Update `src/nebula/core/container.py`** so `EnrollmentService` receives the same `remote_management_service` instance used by the hosted queue endpoints. The exact constructor wiring must pass `remote_management_service=self.remote_management_service` into `EnrollmentService(...)`.

    **5. Extend `src/nebula/api/routes/enrollment.py`**:
    - Keep `GET /admin/deployments/{id}` and `GET /admin/deployments` returning the extended `DeploymentRecord`
    - Keep `GET /admin/deployments/{id}/remote-actions` for full history

    **6. Keep `src/nebula/models/hosted_contract.py` and tests aligned**
    - Ensure `HostedRemoteActionSummary` remains the canonical hosted summary shape
    - Update `tests/test_hosted_contract.py` so `remote_action_summary` still matches the committed schema and remains present in the generated schema

    **7. Extend `console/src/lib/admin-api.ts`**:
    - Add `remote_action_summary` and `recent_remote_actions` to `DeploymentRecord`
    - Ensure `RemoteActionRecord` contains `failure_reason` and `failure_detail`

    **8. Create `console/src/components/deployments/remote-action-card.tsx`** with this exact behavior:
    - Render section title `Remote action`
    - Show one button labeled `Rotate hosted credential`
    - Disable the button when:
      `deployment.enrollment_state !== "active"` OR
      `deployment.freshness_status` is `"stale"` or `"offline"` OR
      `"remote_credential_rotation"` is not in `deployment.capability_flags`
    - Show an inline reason message for disabled state:
      `Deployment must be active, connected, and advertise remote_credential_rotation.`
    - Include a required text input or textarea labeled `Reason`
    - Require the reason to be non-empty before enabling confirmation
    - On confirm, call `queueRotateDeploymentCredential`
    - After success, show the returned queued record at the top of a `Recent remote actions` list
    - Render each history row with action label, status badge, timestamp, and failure reason/detail when present

    **9. Update `console/src/components/deployments/deployment-detail-drawer.tsx`**:
    - Import and render `<RemoteActionCard deployment={deployment} />` below the trust-boundary card and above lifecycle actions
    - Keep revoke/unlink buttons intact

    **10. Add tests**
    - `tests/test_remote_management_audit.py`
      - `test_expired_action_is_preserved_in_history`: create a queued row with past `expires_at`, call summary/history read, assert status becomes `failed` and `failure_reason=="expired"`
      - `test_duplicate_request_after_terminal_failure_creates_new_row`: fail a first action, queue again, assert new `id`
      - `test_summary_counts_match_history`: seed queued/applied/failed rows and assert `queued`, `applied`, `failed`, and `last_action_at`
    - `console/src/components/deployments/remote-action-card.test.tsx`
      - action button disabled without active+connected+capability state
      - reason required before confirm
    - `console/src/components/deployments/deployment-detail-drawer.test.tsx`
      - drawer renders `Remote action` section and recent history when deployment carries `recent_remote_actions`
  </action>
  <verify>
    <automated>pytest tests/test_remote_management_audit.py tests/test_hosted_contract.py -x && npm --prefix console run test -- --run src/components/deployments/remote-action-card.test.tsx src/components/deployments/deployment-detail-drawer.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/nebula/models/deployment.py` contains `remote_action_summary` and `recent_remote_actions`
    - `src/nebula/services/remote_management_service.py` contains `def summarize_remote_actions(` and `def list_recent_remote_actions(`
    - `src/nebula/core/container.py` contains `remote_management_service=self.remote_management_service`
    - `console/src/components/deployments/remote-action-card.tsx` contains `Rotate hosted credential`
    - `console/src/components/deployments/deployment-detail-drawer.tsx` contains `RemoteActionCard`
    - `tests/test_remote_management_audit.py` contains `test_expired_action_is_preserved_in_history`
    - `npm --prefix console run test -- --run src/components/deployments/remote-action-card.test.tsx src/components/deployments/deployment-detail-drawer.test.tsx` exits 0
    - `pytest tests/test_remote_management_audit.py tests/test_hosted_contract.py -x` exits 0
  </acceptance_criteria>
</task>

</tasks>
