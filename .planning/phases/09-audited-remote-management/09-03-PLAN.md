---
phase: 09-audited-remote-management
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - src/nebula/models/hosted_contract.py
  - src/nebula/models/deployment.py
  - src/nebula/services/enrollment_service.py
  - console/src/lib/admin-api.ts
  - console/src/components/deployments/remote-action-card.tsx
  - console/src/components/deployments/deployment-detail-drawer.tsx
  - console/src/components/deployments/remote-action-card.test.tsx
  - console/src/lib/admin-api.test.ts
  - tests/test_remote_management_audit.py
autonomous: true
requirements:
  - RMGT-01
  - RMGT-03

must_haves:
  truths:
    - "Expired queued or in_progress actions become failed with reason expired and remain visible in audit history"
    - "Deployment records expose queued/applied/failed summary counts plus last_action_at for hosted visibility"
    - "The deployment detail drawer shows a single rotate-credential action surface with required note entry and recent history"
    - "The hosted UI fails closed for revoked, unlinked, unsupported, stale, or offline deployments"
  artifacts:
    - path: "tests/test_remote_management_audit.py"
      provides: "Backend coverage for expiry, summary rollups, and duplicate-live-action behavior"
    - path: "console/src/components/deployments/remote-action-card.tsx"
      provides: "Hosted drawer action card and recent-history rendering"
      exports: ["RemoteActionCard"]
    - path: "console/src/lib/admin-api.ts"
      provides: "Remote-action types and admin client methods"
    - path: "src/nebula/models/hosted_contract.py"
      provides: "Hosted metadata contract populated with real remote_action_summary rollups"
  key_links:
    - from: "src/nebula/services/enrollment_service.py"
      to: "src/nebula/models/hosted_contract.py"
      via: "remote_action_summary projection into deployment responses"
      pattern: "remote_action_summary"
    - from: "console/src/components/deployments/deployment-detail-drawer.tsx"
      to: "console/src/components/deployments/remote-action-card.tsx"
      via: "drawer renders RemoteActionCard"
      pattern: "RemoteActionCard"
    - from: "console/src/components/deployments/remote-action-card.tsx"
      to: "console/src/lib/admin-api.ts"
      via: "queueRotateDeploymentCredential and listRemoteActions"
      pattern: "queueRotateDeploymentCredential|listRemoteActions"
---

<objective>
Finish the Phase 9 credibility layer: TTL expiry transitions, audit-summary projection, hosted deployment detail UI, and frontend/backend regression coverage for the operator workflow.

Purpose: Phase 9 is only pilot-credible if stale actions fail visibly, audit history stays inspectable, and the hosted drawer communicates the trust boundary around credential rotation without implying broader remote control.
Output: Expiry-aware audit behavior, deployment-level remote_action_summary projection, hosted drawer action/history UI, and tests for RMGT-01/RMGT-03.
</objective>

<execution_context>
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-audited-remote-management/09-CONTEXT.md
@.planning/phases/09-audited-remote-management/09-RESEARCH.md
@.planning/phases/09-audited-remote-management/09-VALIDATION.md
@.planning/phases/09-audited-remote-management/09-01-SUMMARY.md
@.planning/phases/09-audited-remote-management/09-02-SUMMARY.md
@src/nebula/models/hosted_contract.py
@console/src/components/deployments/deployment-detail-drawer.tsx
@console/src/lib/admin-api.ts

<interfaces>
From src/nebula/models/hosted_contract.py:
```python
class HostedRemoteActionSummary(BaseModel):
    queued: int = 0
    applied: int = 0
    failed: int = 0
    last_action_at: datetime | None = None
```

From console/src/components/deployments/deployment-detail-drawer.tsx:
```tsx
type DeploymentDetailDrawerProps = {
  deployment: DeploymentRecord;
  isGeneratingToken: boolean;
  onGenerateToken: (deploymentId: string) => void;
  onRequestRevoke: (deploymentId: string) => void;
  onRequestUnlink: (deploymentId: string) => void;
  onClose: () => void;
};
```

From console/src/lib/admin-api.ts:
```typescript
export type DeploymentRecord = {
  id: string;
  display_name: string;
  enrollment_state: EnrollmentState;
  capability_flags: string[];
  freshness_status: FreshnessStatus | null;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce expiry and project remote-action audit summary</name>
  <files>
    src/nebula/models/hosted_contract.py,
    src/nebula/models/deployment.py,
    src/nebula/services/enrollment_service.py,
    tests/test_remote_management_audit.py
  </files>
  <read_first>
    src/nebula/models/hosted_contract.py,
    src/nebula/models/deployment.py,
    src/nebula/services/enrollment_service.py,
    src/nebula/db/models.py,
    tests/test_hosted_contract.py,
    tests/test_remote_management_api.py,
    tests/test_remote_management_service.py
  </read_first>
  <behavior>
    - Test 1: queued and in_progress actions past expires_at become failed with failure_reason expired and a finished_at timestamp
    - Test 2: list_deployments and get_deployment expose queued/applied/failed counts and last_action_at derived from action history
    - Test 3: duplicate queue requests still return the same live action id after expiry processing removes expired live rows from the idempotency window
  </behavior>
  <action>
    In `src/nebula/services/enrollment_service.py`, add `_expire_remote_actions(now: datetime) -> None` that finds `DeploymentRemoteActionModel` rows with `status IN ("queued", "in_progress")` and `expires_at <= now`, then transitions them to:
    `status="failed"`,
    `failure_reason="expired"`,
    `failure_detail="Remote action expired before completion."`,
    `finished_at=now`,
    `updated_at=now`.
    Invoke `_expire_remote_actions()` before queue creation, before polling claims, and before list/get deployment projections so expired rows never remain live invisibly.

    Add `_build_remote_action_summary(deployment_id: str) -> HostedRemoteActionSummary` and project it into both `DeploymentRecord.remote_action_summary` and the hosted metadata shape already represented by `HostedRemoteActionSummary`. The summary keys must be exactly:
    `queued` = count of currently queued rows,
    `applied` = count of status=applied,
    `failed` = count of status=failed,
    `last_action_at` = most recent non-null `finished_at` or `requested_at`.

    Extend `tests/test_remote_management_audit.py` to cover expiry transition, summary rollup values, and duplicate queue behavior before and after expiry. Keep the tests backend-only and under the quick validation path defined in `09-VALIDATION.md`.
  </action>
  <verify>
    <automated>pytest tests/test_remote_management_audit.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/nebula/services/enrollment_service.py` contains `def _expire_remote_actions(`
    - `src/nebula/services/enrollment_service.py` contains `failure_reason="expired"` or equivalent exact expired transition
    - `src/nebula/models/hosted_contract.py` still contains `class HostedRemoteActionSummary`
    - `pytest tests/test_remote_management_audit.py -x` exits 0
  </acceptance_criteria>
  <done>
    Expired actions become visible failed audit records and deployment responses expose remote-action rollups for hosted visibility.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add hosted deployment-detail action card, note capture, and recent-history UI</name>
  <files>
    console/src/lib/admin-api.ts,
    console/src/components/deployments/remote-action-card.tsx,
    console/src/components/deployments/deployment-detail-drawer.tsx,
    console/src/components/deployments/remote-action-card.test.tsx,
    console/src/lib/admin-api.test.ts
  </files>
  <read_first>
    console/src/lib/admin-api.ts,
    console/src/lib/admin-api.test.ts,
    console/src/components/deployments/deployment-detail-drawer.tsx,
    console/src/components/deployments/deployment-status-badge.tsx,
    console/src/components/deployments/freshness-badge.tsx,
    console/src/lib/freshness.ts
  </read_first>
  <behavior>
    - Test 1: active compatible deployments render the action card, require a 1-280 character note, and call the queue mutation only after confirmation
    - Test 2: stale, offline, revoked, unlinked, or unsupported deployments render the card in a disabled fail-closed state with explanatory text
    - Test 3: recent history rows render status, note, timestamps, failure reason, and applied credential prefix when present
  </behavior>
  <action>
    Extend `console/src/lib/admin-api.ts` with exact client types and methods:
    `type RemoteActionRecord`,
    `type RemoteActionSummary`,
    `queueRotateDeploymentCredential(deploymentId: string, note: string)`,
    `listRemoteActions(deploymentId: string)`.
    Extend `DeploymentRecord` to include `remote_action_summary: RemoteActionSummary | null`.

    Create `console/src/components/deployments/remote-action-card.tsx` with this exact behavior:
    render the title `Rotate hosted-link credential`,
    render explanatory copy stating `This rotates the hosted-link credential only. It does not change serving traffic, tenant policy, or provider credentials.`,
    render a required note input with max length 280,
    render a confirmation CTA labeled `Queue rotation`,
    render recent history rows ordered newest first.
    Disable the CTA when `deployment.enrollment_state !== "active"`, when `deployment.freshness_status` is `"stale"` or `"offline"`, or when `deployment.capability_flags` does not include `remote_credential_rotation`.

    Update `console/src/components/deployments/deployment-detail-drawer.tsx` to render `<RemoteActionCard />` above the existing lifecycle actions and pass the current deployment id plus the deployment data needed for gating.

    Add `console/src/components/deployments/remote-action-card.test.tsx` and extend `console/src/lib/admin-api.test.ts` for note validation, disabled-state gating, queue mutation wiring, and history rendering.
  </action>
  <verify>
    <automated>npm --prefix console run test -- --run src/components/deployments/remote-action-card.test.tsx src/lib/admin-api.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `console/src/components/deployments/remote-action-card.tsx` contains `Rotate hosted-link credential`
    - `console/src/components/deployments/deployment-detail-drawer.tsx` contains `RemoteActionCard`
    - `console/src/lib/admin-api.ts` contains `queueRotateDeploymentCredential`
    - `npm --prefix console run test -- --run src/components/deployments/remote-action-card.test.tsx src/lib/admin-api.test.ts` exits 0
  </acceptance_criteria>
  <done>
    The hosted operator can queue and inspect the one safe remote action from the deployment detail drawer with fail-closed UI behavior.
  </done>
</task>

</tasks>

<verification>
Run `pytest tests/test_remote_management_audit.py -x` and `npm --prefix console run test -- --run src/components/deployments/remote-action-card.test.tsx src/lib/admin-api.test.ts` to confirm expiry, summary, note capture, and fail-closed UI states.
</verification>

<success_criteria>
- Expired actions become visible failed audit records with reason `expired`
- Deployment data exposes remote_action_summary rollups needed by hosted visibility surfaces
- The deployment detail drawer provides a single, narrow rotate-credential workflow with note capture and recent history
</success_criteria>

<output>
After completion, create `.planning/phases/09-audited-remote-management/09-03-SUMMARY.md`.
</output>
