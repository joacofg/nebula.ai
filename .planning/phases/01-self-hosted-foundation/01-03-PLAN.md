---
phase: 01-self-hosted-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - pyproject.toml
  - alembic.ini
  - migrations/env.py
  - migrations/versions/20260315_0001_governance_baseline.py
  - src/nebula/core/config.py
  - src/nebula/core/container.py
  - src/nebula/db/session.py
  - src/nebula/db/models.py
  - src/nebula/services/governance_store.py
  - tests/support.py
  - tests/test_governance_api.py
  - Makefile
  - docker-compose.selfhosted.yml
  - deploy/selfhosted.env.example
  - docs/self-hosting.md
  - README.md
autonomous: true
requirements:
  - PLAT-03
user_setup: []
must_haves:
    truths:
      - "Governance schema changes are applied through Alembic migrations instead of runtime CREATE TABLE bootstrap."
      - "Nebula can use PostgreSQL for the canonical self-hosted path while local tests and development still work through the same migration workflow."
      - "Governance-store readiness reflects migrated database connectivity before the app is trusted."
    artifacts:
      - path: alembic.ini
        provides: Alembic CLI configuration
        contains: "script_location = migrations"
      - path: migrations/versions/20260315_0001_governance_baseline.py
        provides: Baseline governance schema migration
        contains: "usage_ledger"
      - path: src/nebula/services/governance_store.py
        provides: Migration-backed governance repository
        contains: "sessionmaker"
    key_links:
      - from: src/nebula/services/governance_store.py
        to: src/nebula/db/session.py
        via: SQLAlchemy session factory
        pattern: "sessionmaker"
      - from: docker-compose.selfhosted.yml
        to: alembic.ini
        via: app startup migration command
        pattern: "alembic upgrade head"
---

<objective>
Introduce a durable persistence layer and explicit migration workflow for governance data.

Purpose: Replace implicit SQLite bootstrap schema creation with a migration-backed database story that operators can trust.
Output: Alembic scaffold, database URL configuration, migration-managed governance persistence, updated tests, and updated self-hosted docs.
</objective>

<execution_context>
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-self-hosted-foundation/01-CONTEXT.md
@.planning/phases/01-self-hosted-foundation/01-RESEARCH.md
@.planning/phases/01-self-hosted-foundation/01-VALIDATION.md
@.planning/phases/01-self-hosted-foundation/01-02-SUMMARY.md
@pyproject.toml
@src/nebula/core/config.py
@src/nebula/core/container.py
@src/nebula/services/governance_store.py
@tests/support.py
@tests/test_governance_api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the database URL and Alembic migration scaffold</name>
  <files>pyproject.toml, alembic.ini, migrations/env.py, migrations/versions/20260315_0001_governance_baseline.py, src/nebula/core/config.py, src/nebula/db/session.py, src/nebula/db/models.py</files>
  <read_first>pyproject.toml, src/nebula/core/config.py, src/nebula/services/governance_store.py, .planning/phases/01-self-hosted-foundation/01-RESEARCH.md, deploy/selfhosted.env.example</read_first>
  <action>Add `sqlalchemy>=2`, `alembic>=1`, and `psycopg[binary]>=3` to `pyproject.toml`. Add `NEBULA_DATABASE_URL` to `src/nebula/core/config.py` and make it the primary persistence setting for self-hosted runtime, while preserving a SQLite default for local dev. Create `alembic.ini`, `migrations/env.py`, and `migrations/versions/20260315_0001_governance_baseline.py` for the existing `tenants`, `tenant_policies`, `api_keys`, and `usage_ledger` tables. Add `src/nebula/db/session.py` for engine and `sessionmaker` creation and `src/nebula/db/models.py` for the SQLAlchemy table definitions that match the baseline migration.</action>
  <verify>rg -n "sqlalchemy|alembic|psycopg\\[binary\\]|NEBULA_DATABASE_URL|script_location = migrations" pyproject.toml src/nebula/core/config.py alembic.ini migrations/env.py</verify>
  <acceptance_criteria>
    - pyproject.toml contains "sqlalchemy>="
    - pyproject.toml contains "alembic>="
    - pyproject.toml contains "psycopg[binary]>="
    - src/nebula/core/config.py contains "NEBULA_DATABASE_URL"
    - alembic.ini contains "script_location = migrations"
    - migrations/versions/20260315_0001_governance_baseline.py contains "usage_ledger"
  </acceptance_criteria>
  <done>The repo has a real migration scaffold and a database URL configuration surface for durable persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor governance persistence to rely on migrated schema instead of runtime DDL</name>
  <files>src/nebula/core/container.py, src/nebula/services/governance_store.py, tests/support.py, tests/test_governance_api.py</files>
  <read_first>src/nebula/core/container.py, src/nebula/services/governance_store.py, tests/support.py, tests/test_governance_api.py, src/nebula/db/session.py, src/nebula/db/models.py</read_first>
  <action>Refactor `GovernanceStore` to use the SQLAlchemy engine and `sessionmaker` from `src/nebula/db/session.py` rather than a raw `sqlite3.connect`. Remove the `CREATE TABLE IF NOT EXISTS` script from `initialize()` and replace it with a migrated-schema assumption: `initialize()` should verify connectivity, confirm the schema is present, and then call `ensure_bootstrap()` only after the database is ready. Update `ServiceContainer` to pass the database-backed store. Update `tests/support.py` so temporary test databases are migrated before `create_app()` runs, and update `tests/test_governance_api.py` only where assertions or setup change because of the migration-backed startup path.</action>
  <verify>./.venv/bin/pytest tests/test_governance_api.py -q</verify>
  <acceptance_criteria>
    - src/nebula/services/governance_store.py does not contain "CREATE TABLE IF NOT EXISTS"
    - src/nebula/services/governance_store.py contains "sessionmaker"
    - tests/support.py contains "alembic" or "upgrade"
    - tests/test_governance_api.py exits 0
  </acceptance_criteria>
  <done>The governance store no longer owns schema creation and tests boot against migrated databases.</done>
</task>

<task type="auto">
  <name>Task 3: Wire migrations into the operator workflow and self-hosted runtime</name>
  <files>Makefile, docker-compose.selfhosted.yml, deploy/selfhosted.env.example, docs/self-hosting.md, README.md</files>
  <read_first>Makefile, docker-compose.selfhosted.yml, deploy/selfhosted.env.example, docs/self-hosting.md, README.md, alembic.ini</read_first>
  <action>Add `migrate` and `migrate-create` Makefile targets that run `./.venv/bin/alembic upgrade head` and `./.venv/bin/alembic revision --autogenerate -m "$(MSG)"`. Update `docker-compose.selfhosted.yml` so the `nebula` container runs `alembic upgrade head && uvicorn nebula.main:app --host 0.0.0.0 --port 8000`. Update `deploy/selfhosted.env.example` to use a concrete Postgres URL like `postgresql+psycopg://nebula:nebula@postgres:5432/nebula`. Update `docs/self-hosting.md` and `README.md` so the deployment instructions explicitly mention the migration step and call out Postgres as the canonical governance store for self-hosted environments.</action>
  <verify>rg -n "migrate:|migrate-create:|alembic upgrade head|postgresql\\+psycopg://nebula:nebula@postgres:5432/nebula" Makefile docker-compose.selfhosted.yml deploy/selfhosted.env.example docs/self-hosting.md README.md</verify>
  <acceptance_criteria>
    - Makefile contains "migrate:"
    - Makefile contains "migrate-create:"
    - docker-compose.selfhosted.yml contains "alembic upgrade head"
    - deploy/selfhosted.env.example contains "postgresql+psycopg://nebula:nebula@postgres:5432/nebula"
    - docs/self-hosting.md contains "alembic upgrade head"
  </acceptance_criteria>
  <done>Operators have an explicit migration workflow and the self-hosted runtime uses Postgres as the canonical durable store.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./.venv/bin/alembic upgrade head` succeeds
- [ ] `./.venv/bin/pytest tests/test_governance_api.py -q` succeeds
- [ ] `./.venv/bin/pytest -q` succeeds
- [ ] `rg -n "alembic upgrade head|NEBULA_DATABASE_URL|postgresql\\+psycopg" Makefile docker-compose.selfhosted.yml deploy/selfhosted.env.example docs/self-hosting.md README.md` finds the migration workflow
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Schema evolution no longer depends on runtime bootstrap DDL
  </success_criteria>

<output>
After completion, create `.planning/phases/01-self-hosted-foundation/01-03-SUMMARY.md`
</output>
