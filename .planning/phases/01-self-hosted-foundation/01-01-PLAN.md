---
phase: 01-self-hosted-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - .dockerignore
  - docker-compose.selfhosted.yml
  - deploy/selfhosted.env.example
  - docs/self-hosting.md
  - Makefile
  - README.md
autonomous: true
requirements:
  - PLAT-01
user_setup: []
must_haves:
    truths:
      - "Operators can bring up one documented Docker Compose stack for Nebula without editing repository files."
      - "The self-hosted path clearly documents Nebula, PostgreSQL, and Qdrant as the supported runtime topology."
    artifacts:
      - path: Dockerfile
        provides: Container image for the Nebula API
        contains: "uvicorn nebula.main:app"
      - path: docker-compose.selfhosted.yml
        provides: Canonical self-hosted service topology
        contains: "postgres:"
      - path: docs/self-hosting.md
        provides: Operator deployment guide
        contains: "docker compose -f docker-compose.selfhosted.yml up -d"
    key_links:
      - from: docker-compose.selfhosted.yml
        to: Dockerfile
        via: Compose build configuration
        pattern: "build:"
      - from: docs/self-hosting.md
        to: deploy/selfhosted.env.example
        via: Documented environment setup
        pattern: "selfhosted.env.example"
---

<objective>
Define the supported self-hosted packaging path for Phase 1.

Purpose: Turn the repo from a local-development gateway into a product-shaped deployment with one clear operator story.
Output: Container packaging, a self-hosted Compose stack, a dedicated env template, and deployment documentation.
</objective>

<execution_context>
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-self-hosted-foundation/01-CONTEXT.md
@.planning/phases/01-self-hosted-foundation/01-RESEARCH.md
@.planning/phases/01-self-hosted-foundation/01-VALIDATION.md
@README.md
@docker-compose.yml
@Makefile
@pyproject.toml
@src/nebula/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the canonical container and self-hosted compose topology</name>
  <files>Dockerfile, .dockerignore, docker-compose.selfhosted.yml</files>
  <read_first>docker-compose.yml, Makefile, README.md, pyproject.toml, src/nebula/main.py</read_first>
  <action>Create `Dockerfile` from `python:3.12-slim`, install the project with `pip install -e .`, expose port `8000`, and start Nebula with `uvicorn nebula.main:app --host 0.0.0.0 --port 8000`. Add `.dockerignore` entries for `.git`, `.venv`, `.planning`, `artifacts`, `__pycache__`, `.pytest_cache`, and `.ruff_cache`. Add `docker-compose.selfhosted.yml` with services named `nebula`, `postgres`, and `qdrant`; bind `8000:8000`, `5432:5432`, and `6333:6333`; mount named volumes `postgres_data` and `qdrant_data`; load env from `deploy/selfhosted.env.example`; define `postgres` healthcheck with `pg_isready`; define `qdrant` restart policy; make the `nebula` service build from the new `Dockerfile` and use `depends_on` for `postgres` and `qdrant`.</action>
  <verify>docker compose -f docker-compose.selfhosted.yml config</verify>
  <acceptance_criteria>
    - Dockerfile contains "uvicorn nebula.main:app --host 0.0.0.0 --port 8000"
    - .dockerignore contains ".venv" and ".planning"
    - docker-compose.selfhosted.yml contains "nebula:", "postgres:", and "qdrant:"
    - docker-compose.selfhosted.yml contains "pg_isready" and "deploy/selfhosted.env.example"
  </acceptance_criteria>
  <done>Container packaging and the self-hosted compose topology exist and render with `docker compose config`.</done>
</task>

<task type="auto">
  <name>Task 2: Add a dedicated self-hosted environment template and deployment runbook</name>
  <files>deploy/selfhosted.env.example, docs/self-hosting.md, README.md</files>
  <read_first>README.md, .env.example, .planning/phases/01-self-hosted-foundation/01-CONTEXT.md, .planning/phases/01-self-hosted-foundation/01-RESEARCH.md</read_first>
  <action>Create `deploy/selfhosted.env.example` as the premium-first deployment template with concrete keys for `NEBULA_ENV=production`, `NEBULA_PREMIUM_PROVIDER=openai_compatible`, `NEBULA_PREMIUM_BASE_URL`, `NEBULA_PREMIUM_MODEL`, `NEBULA_QDRANT_URL=http://qdrant:6333`, and placeholder secrets for admin/bootstrap access. Create `docs/self-hosting.md` with exactly one supported path: copy the env template, fill secrets, run `docker compose -f docker-compose.selfhosted.yml up -d`, and verify the stack with `/health`. Update `README.md` to add a "Self-Hosted Deployment" section that points to `docs/self-hosting.md` instead of duplicating a second deployment flow.</action>
  <verify>rg -n "docker compose -f docker-compose.selfhosted.yml up -d|NEBULA_PREMIUM_PROVIDER=openai_compatible|Self-Hosted Deployment" deploy/selfhosted.env.example docs/self-hosting.md README.md</verify>
  <acceptance_criteria>
    - deploy/selfhosted.env.example contains "NEBULA_ENV=production"
    - deploy/selfhosted.env.example contains "NEBULA_PREMIUM_PROVIDER=openai_compatible"
    - docs/self-hosting.md contains "docker compose -f docker-compose.selfhosted.yml up -d"
    - README.md contains "Self-Hosted Deployment" and "docs/self-hosting.md"
  </acceptance_criteria>
  <done>The repo has one operator-facing deployment guide and one dedicated self-hosted env template.</done>
</task>

<task type="auto">
  <name>Task 3: Expose the self-hosted workflow through Makefile helpers</name>
  <files>Makefile</files>
  <read_first>Makefile, docker-compose.selfhosted.yml, docs/self-hosting.md</read_first>
  <action>Add phony targets `selfhost-up`, `selfhost-down`, and `selfhost-logs` that wrap `docker compose -f docker-compose.selfhosted.yml` with `up -d`, `down`, and `logs -f nebula`. Keep the existing local-dev targets unchanged; the new helpers should only provide the supported self-hosted command surface for operators and demos.</action>
  <verify>rg -n "^selfhost-up:|^selfhost-down:|^selfhost-logs:" Makefile</verify>
  <acceptance_criteria>
    - Makefile contains "selfhost-up:"
    - Makefile contains "selfhost-down:"
    - Makefile contains "selfhost-logs:"
    - Makefile contains "docker compose -f docker-compose.selfhosted.yml"
  </acceptance_criteria>
  <done>The self-hosted deployment path is available through named Makefile commands.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose -f docker-compose.selfhosted.yml config` succeeds
- [ ] `rg -n "Self-Hosted Deployment|docker compose -f docker-compose.selfhosted.yml up -d" README.md docs/self-hosting.md` finds the expected documentation
- [ ] `rg -n "^selfhost-(up|down|logs):" Makefile` finds the helper targets
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo has one supported self-hosted runtime story, not multiple equal deployment options
  </success_criteria>

<output>
After completion, create `.planning/phases/01-self-hosted-foundation/01-01-SUMMARY.md`
</output>
