---
phase: 01-self-hosted-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/nebula/core/config.py
  - src/nebula/core/container.py
  - src/nebula/main.py
  - src/nebula/services/runtime_health_service.py
  - src/nebula/services/governance_store.py
  - src/nebula/services/semantic_cache_service.py
  - src/nebula/services/embeddings_service.py
  - tests/test_settings.py
  - tests/test_health.py
  - deploy/selfhosted.env.example
  - docs/self-hosting.md
  - README.md
autonomous: true
requirements:
  - PLAT-02
  - PLAT-04
user_setup: []
must_haves:
    truths:
      - "Nebula refuses to start outside local mode when dev secrets or the mock premium provider are still configured."
      - "Operators can distinguish liveness from readiness through separate health endpoints."
      - "Readiness reports governance-store readiness separately from degraded cache and local-optimization states."
    artifacts:
      - path: src/nebula/core/config.py
        provides: Runtime guardrails for non-local deployment
        contains: "premium_first"
      - path: src/nebula/main.py
        provides: Liveness and readiness endpoints
        contains: "/health/ready"
      - path: tests/test_health.py
        provides: Readiness and degraded-mode coverage
        contains: "health/ready"
    key_links:
      - from: src/nebula/main.py
        to: src/nebula/services/runtime_health_service.py
        via: FastAPI endpoints calling the health service
        pattern: "runtime_health"
      - from: src/nebula/services/runtime_health_service.py
        to: src/nebula/services/governance_store.py
        via: dependency health inspection
        pattern: "governance_store"
---

<objective>
Harden runtime configuration and expose deployment-safe health/readiness signals.

Purpose: Make Nebula reject unsafe production-ish defaults and give operators enough runtime status detail to trust the deployment.
Output: Stronger config validation, a runtime health service, readiness endpoints, degraded-mode reporting, and tests.
</objective>

<execution_context>
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-self-hosted-foundation/01-CONTEXT.md
@.planning/phases/01-self-hosted-foundation/01-RESEARCH.md
@.planning/phases/01-self-hosted-foundation/01-VALIDATION.md
@.planning/phases/01-self-hosted-foundation/01-01-SUMMARY.md
@src/nebula/core/config.py
@src/nebula/core/container.py
@src/nebula/main.py
@src/nebula/services/governance_store.py
@src/nebula/services/semantic_cache_service.py
@src/nebula/services/embeddings_service.py
@tests/test_settings.py
@tests/test_health.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce explicit non-local runtime configuration and secret safety</name>
  <files>src/nebula/core/config.py, deploy/selfhosted.env.example, docs/self-hosting.md</files>
  <read_first>src/nebula/core/config.py, deploy/selfhosted.env.example, docs/self-hosting.md, README.md, .planning/phases/01-self-hosted-foundation/01-RESEARCH.md</read_first>
  <action>Add `NEBULA_RUNTIME_PROFILE` in `src/nebula/core/config.py` with allowed values `local_dev` and `premium_first`, defaulting to `local_dev`. Extend the settings validator so that when `NEBULA_ENV` is not `local`, startup raises `ValueError` if `NEBULA_RUNTIME_PROFILE` is still `local_dev`, `NEBULA_ADMIN_API_KEY` is `nebula-admin-key`, `NEBULA_BOOTSTRAP_API_KEY` is `nebula-dev-key`, or `NEBULA_PREMIUM_PROVIDER` is `mock`. Mirror the new runtime-profile field and non-default-secret instructions in `deploy/selfhosted.env.example` and `docs/self-hosting.md` with the literal premium-first value.</action>
  <verify>./.venv/bin/pytest tests/test_settings.py -q</verify>
  <acceptance_criteria>
    - src/nebula/core/config.py contains "NEBULA_RUNTIME_PROFILE"
    - src/nebula/core/config.py contains "premium_first"
    - src/nebula/core/config.py contains "nebula-admin-key" and "nebula-dev-key"
    - deploy/selfhosted.env.example contains "NEBULA_RUNTIME_PROFILE=premium_first"
  </acceptance_criteria>
  <done>Unsafe non-local defaults are rejected and the self-hosted env template documents the supported runtime profile.</done>
</task>

<task type="auto">
  <name>Task 2: Add component-level readiness and degraded dependency reporting</name>
  <files>src/nebula/services/runtime_health_service.py, src/nebula/core/container.py, src/nebula/main.py, src/nebula/services/governance_store.py, src/nebula/services/semantic_cache_service.py, src/nebula/services/embeddings_service.py</files>
  <read_first>src/nebula/core/container.py, src/nebula/main.py, src/nebula/services/governance_store.py, src/nebula/services/semantic_cache_service.py, src/nebula/services/embeddings_service.py, .planning/phases/01-self-hosted-foundation/01-RESEARCH.md</read_first>
  <action>Create `src/nebula/services/runtime_health_service.py` that builds a response with `status`, `runtime_profile`, and dependency entries for `gateway`, `governance_store`, `semantic_cache`, and `local_ollama`. Add lightweight health methods that return structured status dictionaries from `GovernanceStore`, `SemanticCacheService`, and the Ollama embedding path. Wire the new service through `ServiceContainer` and expose `GET /health/ready` plus `GET /health/dependencies` from `src/nebula/main.py`. Keep `GET /health` as the liveness endpoint; return HTTP 200 from `/health/ready` when required dependencies are ready, including the case where cache or local Ollama are degraded, and return HTTP 503 only when the gateway or governance store are not ready.</action>
  <verify>./.venv/bin/pytest tests/test_health.py -q</verify>
  <acceptance_criteria>
    - src/nebula/main.py contains '@app.get("/health/ready"'
    - src/nebula/main.py contains '@app.get("/health/dependencies"'
    - src/nebula/services/runtime_health_service.py contains '"governance_store"' and '"semantic_cache"'
    - src/nebula/services/runtime_health_service.py contains '"local_ollama"'
  </acceptance_criteria>
  <done>Operators can see liveness, readiness, and degraded dependency detail through separate endpoints.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage and operator docs for guardrails and readiness</name>
  <files>tests/test_settings.py, tests/test_health.py, README.md, docs/self-hosting.md</files>
  <read_first>tests/test_settings.py, tests/test_health.py, README.md, docs/self-hosting.md, src/nebula/main.py, src/nebula/core/config.py</read_first>
  <action>Extend `tests/test_settings.py` with explicit non-local validation cases for the default admin key, default bootstrap key, mock premium provider, and missing `premium_first` runtime profile. Extend `tests/test_health.py` to assert `/health` stays a simple liveness probe, `/health/ready` reports `ready` or `degraded` correctly, and optional dependency failures do not force HTTP 503. Update `README.md` and `docs/self-hosting.md` to document the new readiness endpoints and what `degraded` means for cache and local optimization.</action>
  <verify>./.venv/bin/pytest tests/test_settings.py tests/test_health.py -q</verify>
  <acceptance_criteria>
    - tests/test_settings.py contains "test_non_local_runtime_requires_premium_first_profile"
    - tests/test_settings.py contains "test_non_local_runtime_rejects_default_admin_key"
    - tests/test_health.py contains "test_readiness_reports_degraded_optional_dependencies"
    - docs/self-hosting.md contains "/health/ready" and "/health/dependencies"
  </acceptance_criteria>
  <done>Runtime safety rules and readiness behavior are documented and covered by focused tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./.venv/bin/pytest tests/test_settings.py tests/test_health.py -q` succeeds
- [ ] `rg -n "/health/ready|/health/dependencies" src/nebula/main.py docs/self-hosting.md README.md` finds the new endpoint wiring and docs
- [ ] `rg -n "premium_first|nebula-admin-key|nebula-dev-key" src/nebula/core/config.py` confirms the explicit guardrails
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Nebula can distinguish required readiness from optional degraded dependencies in a way an operator can act on
  </success_criteria>

<output>
After completion, create `.planning/phases/01-self-hosted-foundation/01-02-SUMMARY.md`
</output>
