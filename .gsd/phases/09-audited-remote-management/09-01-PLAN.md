---
phase: 09-audited-remote-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/nebula/models/deployment.py
  - src/nebula/db/models.py
  - src/nebula/services/enrollment_service.py
  - src/nebula/api/routes/admin.py
  - migrations/versions/20260322_0005_remote_actions.py
  - tests/test_remote_management_api.py
autonomous: true
requirements:
  - RMGT-01
  - RMGT-03

must_haves:
  truths:
    - "Hosted admin API can queue exactly one allowlisted remote action named rotate_deployment_credential for an active linked deployment"
    - "Queueing a duplicate live rotation action returns the existing queued or in_progress action record instead of creating a second one"
    - "Each queued action persists operator note, requested_at, expires_at, and status so the hosted plane has a durable audit trail"
  artifacts:
    - path: "src/nebula/models/deployment.py"
      provides: "Remote action request, record, status, and summary Pydantic contracts"
      exports: ["RemoteActionType", "RemoteActionStatus", "RemoteActionRecord", "RemoteActionQueueRequest", "RemoteActionSummary"]
    - path: "src/nebula/db/models.py"
      provides: "DeploymentRemoteActionModel ORM table for hosted-side queue and audit history"
      exports: ["DeploymentRemoteActionModel"]
    - path: "migrations/versions/20260322_0005_remote_actions.py"
      provides: "Alembic migration creating deployment_remote_actions and the live-action uniqueness index"
    - path: "tests/test_remote_management_api.py"
      provides: "Queue-contract coverage for success, duplicate, and invalid-state cases"
  key_links:
    - from: "src/nebula/api/routes/admin.py"
      to: "src/nebula/services/enrollment_service.py"
      via: "queue_rotate_deployment_credential and list_remote_actions"
      pattern: "queue_rotate_deployment_credential|list_remote_actions"
    - from: "src/nebula/services/enrollment_service.py"
      to: "src/nebula/db/models.py"
      via: "DeploymentRemoteActionModel row creation and live-action lookup"
      pattern: "DeploymentRemoteActionModel"
    - from: "migrations/versions/20260322_0005_remote_actions.py"
      to: "src/nebula/db/models.py"
      via: "deployment_remote_actions table and unique live-action index"
      pattern: "deployment_remote_actions|uq_remote_actions_live"
---

<objective>
Define the first remote-management contract on the hosted side: typed queue models, durable persistence, admin queue/history endpoints, and backend tests for the one allowlisted action.

Purpose: Phase 9 cannot build polling or local apply behavior until the hosted plane has a concrete, durable queue contract for `rotate_deployment_credential`.
Output: Hosted queue models, DB table + migration, admin endpoints for queue/history, and regression coverage for RMGT-01.
</objective>

<execution_context>
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-audited-remote-management/09-CONTEXT.md
@.planning/phases/09-audited-remote-management/09-RESEARCH.md
@.planning/phases/09-audited-remote-management/09-VALIDATION.md
@src/nebula/models/hosted_contract.py
@src/nebula/models/deployment.py
@src/nebula/db/models.py
@src/nebula/services/enrollment_service.py
@src/nebula/api/routes/admin.py

<interfaces>
From src/nebula/models/deployment.py:
```python
DeploymentEnvironment = Literal["production", "staging", "development"]
EnrollmentState = Literal["pending", "active", "revoked", "unlinked"]

class DeploymentRecord(BaseModel):
    id: str
    display_name: str
    environment: str
    enrollment_state: EnrollmentState
    nebula_version: str | None
    capability_flags: list[str]
    last_seen_at: str | None = None
    freshness_status: str | None = None
```

From src/nebula/db/models.py:
```python
class DeploymentModel(Base):
    __tablename__ = "deployments"
    id: Mapped[str] = mapped_column(String(255), primary_key=True)
    enrollment_state: Mapped[str] = mapped_column(String(32), nullable=False)
    credential_hash: Mapped[str | None] = mapped_column(String(64), nullable=True, unique=True)
    credential_prefix: Mapped[str | None] = mapped_column(String(32), nullable=True)
    last_seen_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
```

From src/nebula/services/enrollment_service.py:
```python
class EnrollmentService:
    def create_deployment_slot(self, display_name: str, environment: str) -> DeploymentRecord: ...
    def generate_enrollment_token(self, deployment_id: str) -> EnrollmentTokenResponse: ...
    def list_deployments(self) -> list[DeploymentRecord]: ...
    def get_deployment(self, deployment_id: str) -> DeploymentRecord | None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define remote-action models, ORM state, and migration</name>
  <files>
    src/nebula/models/deployment.py,
    src/nebula/db/models.py,
    migrations/versions/20260322_0005_remote_actions.py
  </files>
  <read_first>
    src/nebula/models/deployment.py,
    src/nebula/models/hosted_contract.py,
    src/nebula/db/models.py,
    migrations/versions/20260322_0004_credential_raw.py
  </read_first>
  <behavior>
    - Test 1: a queued rotate_deployment_credential action serializes with action_type, status, note, requested_at, expires_at, and deployment_id
    - Test 2: deployment records can carry a remote_action_summary with queued/applied/failed counts and last_action_at
    - Test 3: the schema contains a unique live-action guard so only one queued or in_progress rotation exists per deployment
  </behavior>
  <action>
    Extend `src/nebula/models/deployment.py` with exact remote-action contracts:
    `RemoteActionType = Literal["rotate_deployment_credential"]`,
    `RemoteActionStatus = Literal["queued", "in_progress", "applied", "failed"]`,
    `RemoteActionFailureReason = Literal["unauthorized_local_policy", "expired", "unsupported_capability", "invalid_state", "apply_error"]`,
    `RemoteActionSummary` with `queued`, `applied`, `failed`, and `last_action_at`,
    `RemoteActionQueueRequest` with `note: str`,
    `RemoteActionRecord` with `id`, `deployment_id`, `action_type`, `status`, `note`, `requested_at`, `expires_at`, `started_at`, `finished_at`, `failure_reason`, `failure_detail`, and `result_credential_prefix`.

    Extend `DeploymentRecord` to include `remote_action_summary: dict[str, int | str | None] | None` or an equivalent typed summary field that serializes the four summary keys exactly as `queued`, `applied`, `failed`, and `last_action_at`.

    Add `DeploymentRemoteActionModel` to `src/nebula/db/models.py` with these exact columns:
    `id` string primary key,
    `deployment_id` FK to `deployments.id`,
    `action_type` string(64),
    `status` string(32),
    `note` string(280),
    `requested_at` timestamptz,
    `expires_at` timestamptz,
    `started_at` timestamptz nullable,
    `finished_at` timestamptz nullable,
    `failure_reason` string(64) nullable,
    `failure_detail` string(255) nullable,
    `result_credential_prefix` string(32) nullable,
    `created_at` timestamptz,
    `updated_at` timestamptz.

    Create `migrations/versions/20260322_0005_remote_actions.py` that creates `deployment_remote_actions` and a unique live-action index named `uq_remote_actions_live` on `(deployment_id, action_type)` for rows where `status IN ('queued', 'in_progress')`. Keep the migration idempotent by checking whether the table and index already exist before creating them.
  </action>
  <verify>
    <automated>pytest tests/test_remote_management_api.py -k "models or schema" -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/nebula/models/deployment.py` contains `RemoteActionType = Literal["rotate_deployment_credential"]`
    - `src/nebula/models/deployment.py` contains `class RemoteActionRecord`
    - `src/nebula/db/models.py` contains `class DeploymentRemoteActionModel`
    - `migrations/versions/20260322_0005_remote_actions.py` contains `uq_remote_actions_live`
  </acceptance_criteria>
  <done>
    The repo has a concrete remote-action data contract and a durable hosted-side queue table for the one allowlisted action.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement hosted queue/history APIs and queue-contract tests</name>
  <files>
    src/nebula/services/enrollment_service.py,
    src/nebula/api/routes/admin.py,
    tests/test_remote_management_api.py
  </files>
  <read_first>
    src/nebula/services/enrollment_service.py,
    src/nebula/api/routes/admin.py,
    src/nebula/db/models.py,
    src/nebula/models/deployment.py,
    tests/test_enrollment_api.py,
    tests/test_heartbeat_api.py
  </read_first>
  <behavior>
    - Test 1: POST queue route for an active deployment returns 201 with a queued rotate_deployment_credential action and the submitted note
    - Test 2: POST queue route for a pending, revoked, or unlinked deployment returns 409 invalid_state and does not create a row
    - Test 3: POST queue route called twice for the same deployment while the first action is queued returns the original action id instead of inserting a duplicate
    - Test 4: GET history route returns deployment-scoped remote actions ordered newest first
  </behavior>
  <action>
    Implement hosted queue behavior in `src/nebula/services/enrollment_service.py` with exact service methods:
    `queue_rotate_deployment_credential(deployment_id: str, note: str) -> RemoteActionRecord`,
    `list_remote_actions(deployment_id: str, limit: int = 10) -> list[RemoteActionRecord]`,
    and `_to_remote_action_record(model: DeploymentRemoteActionModel) -> RemoteActionRecord`.

    In `queue_rotate_deployment_credential`, enforce these exact contract rules:
    the target deployment must exist,
    `deployment.enrollment_state` must equal `"active"`,
    the queued action must use `action_type="rotate_deployment_credential"`,
    `status="queued"`,
    `note` trimmed to 1-280 characters,
    `requested_at = now`,
    `expires_at = now + timedelta(minutes=15)`.
    Use a `SELECT ... FOR UPDATE` lookup against `DeploymentRemoteActionModel` to find an existing live row with the same `deployment_id`, `action_type`, and `status IN ("queued", "in_progress")`; if found, return that row instead of creating a new one.

    Add these admin routes to `src/nebula/api/routes/admin.py`:
    `POST /v1/admin/deployments/{deployment_id}/remote-actions/rotate-credential` accepting `RemoteActionQueueRequest`,
    `GET /v1/admin/deployments/{deployment_id}/remote-actions` returning `list[RemoteActionRecord]`.
    Map missing deployments to 404, invalid deployment state to 409 with `detail="Deployment is not in an active linked state."`, and validation errors for an empty note to 422.

    Create `tests/test_remote_management_api.py` with coverage for the four behaviors above using the same ASGI transport and configured-app pattern already used in `tests/test_enrollment_api.py` and `tests/test_heartbeat_api.py`.
  </action>
  <verify>
    <automated>pytest tests/test_remote_management_api.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/nebula/services/enrollment_service.py` contains `def queue_rotate_deployment_credential(`
    - `src/nebula/api/routes/admin.py` contains `/remote-actions/rotate-credential`
    - `src/nebula/api/routes/admin.py` contains `def list_remote_actions(`
    - `pytest tests/test_remote_management_api.py -x` exits 0
  </acceptance_criteria>
  <done>
    Hosted-side queue creation and history retrieval work for the one allowlisted action and reject out-of-scope deployment states.
  </done>
</task>

</tasks>

<verification>
Run `pytest tests/test_remote_management_api.py -x` and confirm the queue contract covers success, duplicate suppression, and invalid-state failures for hosted admin requests.
</verification>

<success_criteria>
- An active linked deployment can receive a queued `rotate_deployment_credential` action through the hosted admin API
- Duplicate live queue requests return the same remote action record instead of creating two live rows
- Remote actions persist note, TTL, and status data required for later polling and audit history
</success_criteria>

<output>
After completion, create `.planning/phases/09-audited-remote-management/09-01-SUMMARY.md`.
</output>
