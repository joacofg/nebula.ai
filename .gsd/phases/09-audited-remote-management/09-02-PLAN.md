---
phase: 09-audited-remote-management
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/nebula/core/config.py
  - src/nebula/models/deployment.py
  - src/nebula/services/enrollment_service.py
  - src/nebula/services/gateway_enrollment_service.py
  - src/nebula/services/remote_management_service.py
  - src/nebula/api/routes/remote_management.py
  - src/nebula/core/container.py
  - src/nebula/main.py
  - tests/test_remote_management_service.py
autonomous: true
requirements:
  - RMGT-02
  - RMGT-03

must_haves:
  truths:
    - "A linked deployment polls outbound for queued remote actions using its existing deployment credential"
    - "Local configuration and allowlist checks decide whether a queued action is applied or failed closed"
    - "Applying rotate_deployment_credential updates the local hosted-link credential and the hosted-side deployment credential without touching serving-path policy or provider state"
    - "Queued actions record terminal applied or failed outcomes with stable failure reasons"
  artifacts:
    - path: "src/nebula/services/remote_management_service.py"
      provides: "Gateway-side polling, local authorization, and apply orchestration"
      exports: ["RemoteManagementService"]
    - path: "src/nebula/api/routes/remote_management.py"
      provides: "Deployment-authenticated poll and completion endpoints"
      exports: ["remote_management_router"]
    - path: "src/nebula/core/config.py"
      provides: "Remote management settings for enablement, allowlist, and polling interval"
    - path: "tests/test_remote_management_service.py"
      provides: "Integration coverage for poll/apply/failed flows"
  key_links:
    - from: "src/nebula/services/remote_management_service.py"
      to: "src/nebula/api/routes/remote_management.py"
      via: "poll and completion HTTP calls using X-Nebula-Deployment-Credential"
      pattern: "X-Nebula-Deployment-Credential|/remote-actions/"
    - from: "src/nebula/services/remote_management_service.py"
      to: "src/nebula/services/gateway_enrollment_service.py"
      via: "replace_deployment_credential after applied completion"
      pattern: "replace_deployment_credential"
    - from: "src/nebula/main.py"
      to: "src/nebula/services/remote_management_service.py"
      via: "lifespan start and stop"
      pattern: "remote_management_service\\.start|remote_management_service\\.stop"
---

<objective>
Implement the deployment-side remote-management execution path: outbound polling, local authorization, apply/completion handshake, lifespan wiring, and backend tests.

Purpose: The hosted queue created in Plan 09-01 only becomes Phase 9 product value once linked deployments can poll, enforce local rules, rotate the hosted-link credential safely, and record applied or failed outcomes.
Output: Remote management settings, gateway polling/apply service, deployment-authenticated hosted routes, and backend test coverage for RMGT-02.
</objective>

<execution_context>
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/workflows/execute-plan.md
@/Users/joaquinfernandezdegamboa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-audited-remote-management/09-CONTEXT.md
@.planning/phases/09-audited-remote-management/09-RESEARCH.md
@.planning/phases/09-audited-remote-management/09-VALIDATION.md
@.planning/phases/09-audited-remote-management/09-01-SUMMARY.md
@src/nebula/core/config.py
@src/nebula/services/heartbeat_service.py
@src/nebula/services/gateway_enrollment_service.py
@src/nebula/main.py

<interfaces>
From src/nebula/services/heartbeat_service.py:
```python
class HeartbeatService:
    def start(self) -> None: ...
    async def stop(self) -> None: ...
    async def _send_once(self) -> None: ...
```

From src/nebula/services/gateway_enrollment_service.py:
```python
class GatewayEnrollmentService:
    def get_deployment_credential(self) -> str | None: ...
    def get_local_identity(self) -> LocalHostedIdentityModel | None: ...
```

From src/nebula/models/deployment.py after Plan 09-01:
```python
class RemoteActionRecord(BaseModel):
    id: str
    deployment_id: str
    action_type: Literal["rotate_deployment_credential"]
    status: Literal["queued", "in_progress", "applied", "failed"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deployment-authenticated poll and completion handshake</name>
  <files>
    src/nebula/models/deployment.py,
    src/nebula/services/enrollment_service.py,
    src/nebula/services/gateway_enrollment_service.py,
    src/nebula/services/remote_management_service.py,
    src/nebula/api/routes/remote_management.py
  </files>
  <read_first>
    src/nebula/models/deployment.py,
    src/nebula/services/enrollment_service.py,
    src/nebula/services/gateway_enrollment_service.py,
    src/nebula/api/routes/heartbeat.py,
    src/nebula/services/heartbeat_ingest_service.py,
    src/nebula/db/models.py
  </read_first>
  <behavior>
    - Test 1: polling with a valid deployment credential returns the next queued rotate_deployment_credential action and marks it in_progress
    - Test 2: local authorization failure completes the action as failed with failure_reason unauthorized_local_policy
    - Test 3: successful completion returns a new deployment credential in the completion response and the gateway persists it locally
    - Test 4: failed completion keeps the old credential active and records failure_reason invalid_state or apply_error
  </behavior>
  <action>
    Extend `src/nebula/models/deployment.py` with exact deployment-authenticated remote-management payloads:
    `RemoteActionPollResponse` containing `action: RemoteActionRecord | None`,
    `RemoteActionCompletionRequest` containing `status: Literal["applied", "failed"]`, `failure_reason: RemoteActionFailureReason | None`, and `failure_detail: str | None`,
    `RemoteActionCompletionResponse` containing `acknowledged: bool` and `new_deployment_credential: str | None`.

    In `src/nebula/services/enrollment_service.py`, add exact hosted-side methods:
    `claim_next_remote_action(raw_credential: str) -> RemoteActionRecord | None`,
    `complete_remote_action(raw_credential: str, action_id: str, payload: RemoteActionCompletionRequest) -> RemoteActionCompletionResponse | None`.
    `claim_next_remote_action` must hash `raw_credential`, resolve the active deployment, find the oldest queued unexpired remote action for that deployment, set `status="in_progress"` and `started_at=now`, and return the claimed record.

    `complete_remote_action` must enforce these exact rules:
    for `status="failed"`, set `finished_at=now`, persist `failure_reason` and `failure_detail`, and return `new_deployment_credential=None`;
    for `status="applied"` on `rotate_deployment_credential`, mint a new raw credential using the existing `nbdc_` token format, update `DeploymentModel.credential_hash` and `credential_prefix`, store the prefix in `result_credential_prefix`, set `finished_at=now`, and return the raw new credential in `new_deployment_credential`.

    Add `replace_deployment_credential(new_credential: str) -> None` to `src/nebula/services/gateway_enrollment_service.py` that updates the active `LocalHostedIdentityModel` row in place with the new raw credential, SHA-256 hash, and first-12-character prefix.

    Create `src/nebula/api/routes/remote_management.py` with these exact deployment-authenticated routes:
    `POST /v1/remote-actions/poll` returning `RemoteActionPollResponse`,
    `POST /v1/remote-actions/{action_id}/complete` accepting `RemoteActionCompletionRequest` and returning `RemoteActionCompletionResponse`.
    Authenticate both routes with the existing `X-Nebula-Deployment-Credential` header model used by `src/nebula/api/routes/heartbeat.py`; invalid credentials return 401 with `detail="Invalid or unknown deployment credential."`.

    Create `src/nebula/services/remote_management_service.py` with `poll_and_apply_once()` and `_authorize(action: RemoteActionRecord) -> tuple[bool, RemoteActionFailureReason | None, str | None]`. For `rotate_deployment_credential`, local apply must be exactly:
    fetch action,
    require local identity present,
    require local authorization to pass,
    send completion request with `status="applied"` using the old credential,
    receive `new_deployment_credential`,
    call `replace_deployment_credential(new_credential)`.
    If authorization fails, send completion with `status="failed"` and `failure_reason="unauthorized_local_policy"` without mutating local identity.
  </action>
  <verify>
    <automated>pytest tests/test_remote_management_service.py -k "poll or apply or failed" -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/nebula/api/routes/remote_management.py` contains `@remote_management_router.post("/poll")`
    - `src/nebula/api/routes/remote_management.py` contains `@remote_management_router.post("/{action_id}/complete")`
    - `src/nebula/services/gateway_enrollment_service.py` contains `def replace_deployment_credential(`
    - `src/nebula/services/enrollment_service.py` contains `def claim_next_remote_action(`
  </acceptance_criteria>
  <done>
    The hosted plane and gateway can execute the one allowed remote action through an outbound polling and completion handshake.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire local authorization settings, polling lifecycle, and integration tests</name>
  <files>
    src/nebula/core/config.py,
    src/nebula/services/remote_management_service.py,
    src/nebula/core/container.py,
    src/nebula/main.py,
    tests/test_remote_management_service.py
  </files>
  <read_first>
    src/nebula/core/config.py,
    src/nebula/core/container.py,
    src/nebula/main.py,
    src/nebula/services/heartbeat_service.py,
    tests/test_heartbeat_api.py,
    tests/test_enrollment_api.py
  </read_first>
  <behavior>
    - Test 1: when remote management is disabled, the background poller starts but poll_and_apply_once does nothing
    - Test 2: when the allowlist does not include rotate_deployment_credential, a queued action is completed as failed with unauthorized_local_policy
    - Test 3: when enabled and allowlisted, the service advertises remote_credential_rotation capability and rotates credentials successfully
  </behavior>
  <action>
    Add exact settings to `src/nebula/core/config.py`:
    `remote_management_enabled: bool = False`,
    `remote_management_allowed_actions: list[str] = Field(default_factory=list)`,
    `remote_management_poll_interval_seconds: int = 60`.

    In `src/nebula/services/remote_management_service.py`, implement the same lifecycle pattern as `HeartbeatService`:
    `start()` creates an asyncio task,
    `stop()` cancels it,
    `_loop()` sleeps for `settings.remote_management_poll_interval_seconds`,
    `_poll_once()` silently returns when `hosted_plane_url` is missing, when no local deployment credential exists, or when `remote_management_enabled` is false.

    Wire `RemoteManagementService` into `src/nebula/core/container.py` and `src/nebula/main.py`. Start it unconditionally during lifespan after enrollment and heartbeat setup, and stop it during shutdown. Do not create a second scheduler or CLI hook.

    Ensure the gateway advertises the exact capability flag `remote_credential_rotation` whenever `remote_management_enabled` is true and `rotate_deployment_credential` is present in `remote_management_allowed_actions`; update the existing capability derivation used for enrollment exchange and heartbeat payloads so the hosted UI can fail closed on unsupported deployments.

    Build `tests/test_remote_management_service.py` using ASGI transport and lifespan-managed app setup. Cover disabled polling, allowlist failure, successful rotation returning a new credential, and follow-up poll requests authenticating with the rotated credential.
  </action>
  <verify>
    <automated>pytest tests/test_remote_management_service.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/nebula/core/config.py` contains `remote_management_enabled`
    - `src/nebula/core/config.py` contains `remote_management_allowed_actions`
    - `src/nebula/main.py` contains `remote_management_service.start()`
    - `pytest tests/test_remote_management_service.py -x` exits 0
  </acceptance_criteria>
  <done>
    Local authorization and background polling are wired into the gateway lifecycle with backend coverage for success and fail-closed behavior.
  </done>
</task>

</tasks>

<verification>
Run `pytest tests/test_remote_management_service.py -x` and confirm the gateway-side poll/apply flow covers disabled, unauthorized, and successful rotation paths.
</verification>

<success_criteria>
- Linked deployments poll outbound for remote actions with deployment credential auth
- Local config and allowlist rules determine whether the action applies or fails closed
- Successful rotation updates both hosted and local hosted-link credentials without touching serving-path state
</success_criteria>

<output>
After completion, create `.planning/phases/09-audited-remote-management/09-02-SUMMARY.md`.
</output>
