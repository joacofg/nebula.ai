# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001 | arch | v3.0 API strategy | Hybrid: OpenAI-compatible inference plus Nebula-native control surfaces | A pure Nebula-native API increases adoption friction; a pure compatibility layer undersells Nebula’s differentiated value | No | collaborative |
| D002 | M001 | scope | v3.0 primary emphasis | Focus v3.0 on adoption path clarity, migration proof, and day-1 value visibility rather than broad new infrastructure | Nebula is already reasonably strong as infrastructure; the weak spot is how teams adopt and understand it | No | collaborative |
| D003 | M001 | scope | Compatibility promise | Keep the initial OpenAI-compatible promise tight and reliable around common chat-completions flows | Broad compatibility would expand surface area and maintenance risk before the contract is proven | Yes — if repeated adoption demand proves a wider surface is required | collaborative |
| D004 | M001 | pattern | Reference proof style | Use a realistic migration integration rather than a toy demo as the main adoption proof | Platform-minded teams trust believable migration proof more than greenfield showcase apps | Yes — if a stronger representative reference target emerges during execution | collaborative |
| D005 | M001 | pattern | Production structuring model in v3.0 | Make tenant / app / workload / operator boundaries explicit in M001, but keep enforcement lightweight unless adoption proof demands more | The model needs to be understandable now, but a heavy product-surface redesign would distract from the adoption milestone | Yes — if lightweight modeling proves insufficient during implementation | collaborative |
| D006 | M001/S01 | api-contract | How to enforce the minimum public request invariant for chat completions | Enforce at least one user message at the ChatCompletionRequest schema boundary and rely on FastAPI's native 422 validation envelope for invalid request shapes. | Keeping the invariant in request-schema validation narrows the adoption contract at the public boundary, preserves consistent framework-native 422 behavior, and prevents downstream service logic from becoming the first line of contract enforcement. | Yes | agent |
| D007 | M001/S01 | documentation | How to document the public compatibility surface without compatibility sprawl | Treat docs/adoption-api-contract.md as the single canonical source for the public POST /v1/chat/completions adoption boundary, and explicitly separate it from the admin Playground contract. | A single test-backed contract document reduces drift between README, architecture docs, and runtime behavior while making the public client path and admin operator path clearly non-equivalent. | Yes | agent |
| D008 | M001/S02 | documentation | How to structure adoption guidance without reopening contract ambiguity | Use one canonical quickstart (`docs/quickstart.md`) for the supported self-hosted happy path, one canonical production-model doc (`docs/production-model.md`) for tenant/operator/app/workload framing, and keep `docs/adoption-api-contract.md` as the only public API contract. | Separating deployment flow, operating-model guidance, and public compatibility rules reduces doc drift while giving downstream slices one stable adoption story to reuse. | Yes | agent |
| D009 | M001/S03 | reference-integration | How to structure the canonical S03 reference migration proof | Keep the reference migration proof narrowly focused on real caller changes and dual-sided verification: base URL plus X-Nebula-API-Key are the default changes, X-Nebula-Tenant-ID is added only for intentionally ambiguous multi-tenant keys, and success is proved by correlating X-Request-ID/X-Nebula-* response headers with GET /v1/admin/usage/ledger. | A narrow, executable migration path is more credible than a broader demo because it matches the real public contract, avoids reopening setup or compatibility scope from S01/S02, and gives downstream slices a reusable observability pattern for operator-visible proof. | Yes | agent |
| D010 | M001/S04 | documentation | How to present Nebula's day-1 value proof without reopening the public compatibility boundary | Use a single canonical docs/day-1-value.md walkthrough rooted in the public POST /v1/chat/completions request and X-Request-ID/header correlation, while treating Playground and Observability strictly as operator corroboration surfaces and reusing existing admin API data for console proof surfaces. | This keeps the public adoption target stable, prevents the admin Playground from being mistaken for the migration path, and makes routing, policy, observability, and provider abstraction legible on day 1 without widening backend scope or duplicating contract/setup docs. | Yes | agent |
| D011 | M002/S01 | console-copy | How operator truth surfaces should present runtime scope and evidence semantics | Derive all operator-facing scope and evidence framing from existing runtime/admin contracts: API key scope from tenant_id plus allowed_tenant_ids, Playground as admin-only non-streaming corroboration, and Observability as persisted request evidence plus dependency-health context. | This removes production-structuring ambiguity without inventing backend fields or pseudo-entities, keeps the console aligned to AuthService/admin route truth, and lets focused tests catch future wording drift at the real operator trust boundary. | Yes | agent |
| D012 | M002/S02 | console-copy | How tenant surfaces should explain app/workload structuring without fake product objects | Teach operators to map apps and workloads onto real tenants and API keys, and frame tenant metadata only as optional notes/runbook context rather than an enforced schema or pseudo-entity. | This gives larger teams actionable production-structuring guidance on the Tenants page and editor drawer while preserving Nebula's runtime truth: tenants and API keys are enforced, but app/workload remain conceptual conventions until real implementation exists. Focused vitest assertions can then lock that boundary against future wording drift. | Yes | agent |
| D013 | M002/S03 | documentation | How to close the production-structuring milestone with one integrated operator proof | Make docs/integrated-adoption-proof.md the composition-first assembly doc that starts from tenant/API-key runtime truth, preserves the public request → headers → usage ledger → Playground → Observability order, and lock console corroboration surfaces with focused positive and negative wording assertions plus a concrete S03 UAT script. | The milestone risk was final-story drift across docs and operator surfaces, not missing new runtime features. A composition-first walkthrough avoids duplicating contract/setup canonicals, while focused Vitest checks and tailored UAT keep Playground and Observability in corroboration-only roles and prevent pseudo-entity drift from reappearing. | Yes | agent |
| D014 | M003 | scope | How to widen the v3 adoption surface | Add one new public surface only — embeddings — and keep M003 focused on adoption proof rather than a broader parity expansion. | Embeddings is the strongest next high-demand surface and the repo already has internal prior art, but widening beyond one surface would reopen scope and maintenance risk. | Yes — if repeated demand later justifies another narrow compatibility surface | collaborative |
| D015 | M003 | api-contract | What public embeddings boundary M003 should promise | Support only the strict happy-path embeddings contract: `POST /v1/embeddings` with `input` + `model`, string input and simple batch input, and the standard float embedding response shape. | A narrow contract preserves credibility and keeps M003 from accidentally committing Nebula to broader embeddings-option parity before the basic adoption story is proven. | Yes — if real adoption demand later proves a specific additional parameter is necessary | collaborative |
| D016 | M003 | pattern | What kind of proof should anchor the embeddings milestone | Use a realistic generic caller migration proof and require durable backend/operator evidence, while treating console expansion and helper ergonomics as optional only if a concrete proof gap forces them. | The milestone is about believable adoption, not feature breadth. A generic caller swap is the clearest proof that Nebula can replace direct provider usage with minimal change, and backend-first evidence is enough unless implementation shows a real trust gap. | Yes | collaborative |
| D017 | M003/S01 | api-contract | How S01 should expose embeddings publicly without widening Nebula's orchestration or evidence model | Add a dedicated authenticated /v1/embeddings route that calls the embeddings service directly, keeps the request contract to model plus string-or-flat-list input, and reuses the existing generic usage ledger with embeddings-specific metadata keyed by X-Request-ID. | This preserves the narrow public compatibility promise, avoids coupling embeddings into chat orchestration, and provides durable metadata-only correlation proof without adding a new ledger schema or storing raw inputs/vectors. | Yes | agent |
| D018 | M003/S02 | documentation | How to document Nebula's public embeddings compatibility boundary without creating a second drifting source of truth | Make docs/embeddings-adoption-contract.md the single canonical public POST /v1/embeddings contract, keep it grounded in route/tests/ledger evidence, and update README.md plus docs/architecture.md to link to it instead of duplicating detailed request/response semantics. | The embeddings milestone widens adoption only if the compatibility line stays narrow and inspectable. One canonical document backed by focused tests and runtime evidence reduces drift, makes unsupported/deferred edges explicit, and preserves the milestone guardrail against broader parity promises. | Yes | agent |
| D019 | M003/S03 | reference-integration | How S03 should prove the embeddings migration path credibly without widening scope | Anchor the embeddings migration proof in one executable public-route test plus a pointer-only human guide, and require dual-sided verification through public X-Request-ID/X-Nebula-* headers and metadata-only usage-ledger correlation. | A believable adoption proof needs to show the real caller diff and the real evidence path, not a toy demo or a second drifting contract document. Reusing the public /v1/embeddings route, existing OpenAI-style client shape, and existing usage-ledger correlation proves minimal-change migration while preserving the milestone guardrails against helper-stack, parity, or persistence-scope expansion. | Yes | agent |
| D020 | M003/S04 | observability | How embeddings evidence should appear in operator surfaces | Reuse the existing usage-ledger/admin API path and make embeddings intentionally discoverable by extending shared Observability filter, table, and request-detail surfaces with persisted metadata-only fields rather than adding embeddings-specific storage, payload capture, or a new API family. | This satisfies the durable-evidence goal with the smallest scope increase, keeps Observability subordinate to the public X-Request-ID plus usage-ledger proof path, preserves the metadata-only trust boundary, and avoids widening M003 into bespoke console or backend infrastructure. | Yes | agent |
| D021 | M003/S05 | documentation | How to assemble the final embeddings adoption story without creating a second contract or promoting admin corroboration above the public proof path | Make `docs/embeddings-integrated-adoption-proof.md` a composition-first, pointer-only walkthrough that preserves the order `POST /v1/embeddings` → `X-Request-ID`/`X-Nebula-*` → `GET /v1/admin/usage/ledger?request_id=...` → Observability, and keep README/architecture discoverability links pointer-only. | Final assembly should help reviewers find and follow the joined proof while protecting the canonical contract, migration guide, and metadata-only evidence model from duplication or scope drift. Preserving Observability as subordinate corroboration keeps the public adoption path authoritative. | Yes | agent |
| D022 | M005 | scope | What the first v4 milestone should optimize for | Focus v4 on decisioning quality and operator actionability: adaptive routing, policy simulation, hard budget guardrails, cache tuning visibility, and recommendation-grade feedback, rather than broad API parity, SDK expansion, or new hosted authority. | The current product already proves adoption, evidence, and hosted reinforcement. The most obvious remaining weakness is not missing surfaces; it is the quality and safety of the decisions Nebula makes and helps operators make. | Yes — if implementation shows the chosen decisioning wedge is too broad or insufficiently differentiated | collaborative |
| D023 | M005/S01 | observability | How adaptive route signals should appear in operator surfaces | Render route decision metadata as stable labeled fields in the ledger detail drawer and expose route score as an additive response header while keeping override routes signal-free | S01 established an interpretable routing vocabulary that operators can inspect without reading raw JSON, preserved existing header semantics, and kept non-heuristic override paths free of misleading synthetic signals. | Yes | agent |
| D024 | M005/S01 | requirement | R039 | Keep R039 active | S01 delivers explicit decision signals, score/header exposure, ledger persistence, UI visibility, and stable vocabulary, but the router still relies on token-count heuristics plus simple policy cues rather than broader outcome-aware adaptive scoring. The slice materially advances R039 without fully validating the requirement. | Yes | agent |
| D025 | M005/S02 | requirement | R040 | validated | M005/S02 added a replay-only policy simulation service, a non-mutating admin `POST /v1/admin/tenants/{tenant_id}/policy/simulate` endpoint, and console preview-before-save UX that replays recent ledger-backed tenant traffic against a candidate policy. Focused verification passed in `./.venv/bin/pytest tests/test_service_flows.py -k simulation -x`, `./.venv/bin/pytest tests/test_governance_api.py -k simulation -x`, and `npm --prefix console run test -- --run src/components/policy/policy-form.test.tsx src/components/policy/policy-page.test.tsx`, establishing that operators can simulate routing/policy changes against real recent traffic before applying them. | Yes | agent |
| D026 | M005/S03 | architecture | How hard cumulative budget guardrails should be enforced in M005/S03 | Implement hard cumulative budget guardrails as explicit tenant-policy fields enforced centrally in PolicyService with downgrade-to-local when compatible and exact denial when premium is required, then expose the resulting evidence through existing ledger/simulation/operator surfaces rather than a separate budget engine. | The codebase already shares runtime and simulation policy evaluation through PolicyService and already has spend aggregation plus operator evidence paths. Centralizing the new semantics there keeps runtime and simulation aligned, preserves S01's route-vocabulary/override discipline, and advances R041/R044 without widening Nebula into billing, analytics, or new hosted authority surfaces. | Yes | agent |
| D027 |  | requirement | R041 | validated | M005/S03 added explicit hard cumulative budget policy fields, centralized runtime/simulation enforcement in PolicyService with deterministic downgrade-versus-deny behavior, and exposed operator-readable budget evidence in admin APIs, console request detail, and docs. Focused verification passed in `./.venv/bin/pytest tests/test_governance_runtime_hardening.py tests/test_service_flows.py -k "budget or simulation or runtime_policy" -x`, `./.venv/bin/pytest tests/test_governance_api.py -k "guardrail or policy_denied or simulation" -x`, and `npm --prefix console run test -- --run src/components/policy/policy-form.test.tsx src/components/policy/policy-page.test.tsx src/components/ledger/ledger-request-detail.test.tsx`, establishing enforceable hard spend guardrails with graceful downgrade behavior beyond soft budget signals alone. | Yes | agent |
| D028 |  | requirement | R042 | validated | M005/S04 delivered bounded recommendation-grade feedback grounded in recent ledger-backed traffic plus supporting runtime cache health through a typed backend RecommendationBundle, an admin-only tenant-scoped recommendations endpoint, and console observability rendering that shows explicit next-best-action cards without black-box optimization claims. Verification passed in `./.venv/bin/pytest tests/test_service_flows.py -k "recommendation or cache" -x`, `./.venv/bin/pytest tests/test_governance_api.py -k "recommendation or cache" -x`, and `npm --prefix console run test -- --run src/app/'(console)'/observability/page.test.tsx src/app/'(console)'/observability/observability-page.test.tsx src/components/policy/policy-form.test.tsx src/components/policy/policy-page.test.tsx`. | Yes | agent |
| D029 |  | requirement | R043 | validated | M005/S04 made semantic cache behavior tunable and inspectable per tenant by adding explicit policy knobs for `semantic_cache_similarity_threshold` and `semantic_cache_max_entry_age_hours`, persisting them through governance storage and migration, exposing a bounded cache summary with runtime status, estimated hit rate, avoided premium cost, and cache insights, and rendering those controls in the existing policy save/preview flow plus observability context. Verification passed in `./.venv/bin/pytest tests/test_service_flows.py -k "recommendation or cache" -x`, `./.venv/bin/pytest tests/test_governance_api.py -k "recommendation or cache" -x`, and `npm --prefix console run test -- --run src/app/'(console)'/observability/page.test.tsx src/app/'(console)'/observability/observability-page.test.tsx src/components/policy/policy-form.test.tsx src/components/policy/policy-page.test.tsx`. | Yes | agent |
| D030 |  | requirement | R044 | validated | M005/S05 assembled the v4 decisioning story as a pointer-only integrated proof in docs/v4-integrated-proof.md, added discoverability links from README.md and docs/architecture.md, and reverified the shipped runtime/admin/console seams with passing focused coverage: ./.venv/bin/pytest tests/test_service_flows.py -k "simulation or recommendation or cache or runtime_policy or budget" -x, ./.venv/bin/pytest tests/test_governance_api.py -k "simulation or recommendation or guardrail or policy_options or policy_denied" -x, npm --prefix console run test -- --run src/app/'(console)'/observability/page.test.tsx src/app/'(console)'/observability/observability-page.test.tsx src/components/policy/policy-form.test.tsx src/components/policy/policy-page.test.tsx src/components/ledger/ledger-request-detail.test.tsx, plus file/link checks for docs/v4-integrated-proof.md. The assembled proof stays narrow and operator-driven: no new public APIs, no autonomous optimization, no new hosted authority, and no SDK/app-platform expansion. | Yes | agent |
| D031 | M005/S05 | documentation | v4 integrated proof composition style | Use a pointer-only integrated walkthrough plus top-level discoverability links instead of duplicating v4 contracts or adding prose-snapshot doc tests. | S05 needed to assemble simulation replay, hard guardrails, recommendations, cache controls, Observability corroboration, and benchmark evidence into one reviewable path without widening scope. Keeping docs/v4-integrated-proof.md composition-first and discoverability-only links in README.md and docs/architecture.md preserves canonical sources, makes drift obvious, and lets verification rely on focused runtime/UI tests plus explicit file/link checks rather than brittle prose snapshots. | Yes | agent |
| D032 | M006 | operability | How much operator control M006 should introduce for calibrated routing | Add only a small tenant-scoped calibrated-routing control as a rollout and safety valve, not a broader tuning surface. | A bounded tenant control gives operators a reversible guardrail for rollout and degraded-mode handling without expanding M006 into a calibration-tuning product. | Yes | collaborative |
| D033 | M006 | operability | How much operator control M006 should introduce for calibrated routing | Add only a small tenant-scoped calibrated-routing control as a rollout and safety valve, not a broader tuning surface. | A bounded tenant control gives operators a reversible guardrail for rollout and degraded-mode handling without expanding M006 into a calibration-tuning product. | Yes | collaborative |
| D034 | M006/S01 planning | architecture | How S01 should stage calibrated routing work to preserve replay parity and scope discipline | Implement S01 in three steps: shared router contract first, policy/runtime propagation second, bounded tenant rollout control last. | Replay currently depends on a small persisted signal subset and runtime already propagates route score/signals end to end. Locking the shared calibrated contract before policy wiring and persistence changes reduces parity drift risk and keeps the tenant control bounded instead of turning router work into a cross-stack redesign. | Yes | agent |
| D035 | M006/S01 | requirement | R039 | active | M006/S01 replaced duplicated router branches with a shared calibrated scoring contract, propagated calibrated route score/mode metadata through runtime headers and usage-ledger evidence, added the bounded tenant `calibrated_routing_enabled` rollout valve across governance APIs and the console policy editor, and reverified the focused backend/console suites. This materially advances outcome-aware routing evidence and operator control, but ledger-backed calibration summaries, replay parity proof, and integrated close-out evidence are still pending in later M006 slices, so the requirement remains active. | Yes | agent |
| D036 | M006/S02 planning | M006/S02 architecture | How ledger-backed calibration evidence should be surfaced in M006/S02 | Define one bounded tenant-scoped calibration summary contract in the governance/admin layer and reuse it in policy simulation and existing Observability/request-detail surfaces instead of adding a separate analytics subsystem or broad new API family. | S01 already shipped the calibrated/degraded per-request contract. S02's missing piece is deterministic tenant-scoped evidence derived from existing usage-ledger metadata. A shared typed summary keeps runtime, replay, and operator rendering aligned; preserves the milestone constraint against analytics drift; and lets later slices inherit one vocabulary instead of re-implementing evidence logic in simulation and UI. | Yes | agent |
| D037 | M006/S04 | console-observability | How M006/S04 should expose calibrated routing inspection in operator surfaces | Keep the selected ledger row authoritative and render calibration, recommendation, cache, and dependency cards only as supporting context; reuse the existing calibrated/degraded/rollout-disabled/unscored vocabulary and allow duplicate labels when they are scoped to bounded cards. | S04 implemented request-detail routing inspection directly from persisted route_signals and reframed Observability around a selected-request-first flow. Focused Vitest coverage passed for calibrated additive-score rows, degraded rows, rollout-disabled/null-mode rows, unscored partial-signal rows, and page-level framing, showing operators can inspect the persisted request evidence without inventing a new inspector or analytics surface. | Yes | agent |
| D038 | M006/S05/T01 | documentation | How M006 close-out proof should be assembled | Use a pointer-only integrated walkthrough plus discoverability links in README.md and docs/architecture.md instead of duplicating calibrated-routing, replay, or operator-surface contracts. | S05 needs one reviewable calibrated-routing close-out path that joins route vocabulary, public headers, request-id-to-ledger correlation, replay parity, and selected-request-first Observability without widening Nebula into a second contract or a broader analytics/tuning story. Keeping docs/m006-integrated-proof.md composition-first preserves canonical sources and lets verification stay mechanical and code-backed. | Yes | agent |
| D039 |  | requirement | R049 | validated | M006/S05 assembled the calibrated-routing close-out as a pointer-only integrated proof in docs/m006-integrated-proof.md, added discoverability links in README.md and docs/architecture.md, and reverified the shipped seams with passing focused checks: ./.venv/bin/python mechanical file/content validation for docs/m006-integrated-proof.md plus README.md/docs/architecture.md, rg proof-token checks, ./.venv/bin/pytest tests/test_response_headers.py -x, ./.venv/bin/pytest tests/test_governance_api.py -k "simulation and calibrated" -x, and npm --prefix console run test -- --run src/app/'(console)'/observability/page.test.tsx src/app/'(console)'/observability/observability-page.test.tsx src/components/policy/policy-page.test.tsx. The assembled proof stays narrow and operator-readable: no new admin routes, no new public API family, no analytics-product drift, no broader tuning surface, and no hosted-authority expansion. | Yes | agent |
| D040 | M007 | console-product | What the primary roles of the M007 operator surfaces should be | Observability is request investigation first, request detail remains the authoritative persisted evidence record, and policy preview is a save/don’t-save decision-review surface. | These role decisions directly address the current page identity blur and give the milestone a concrete product spine. They also preserve Nebula’s existing wedge by keeping durable evidence primary and supporting context visibly secondary. | Yes | collaborative |
| D041 | M007/S01 | console-product | M007/S01 operator-surface evidence hierarchy | Observability leads with selected-request investigation, ledger request detail remains the authoritative persisted evidence seam, and policy preview stays explicitly preview-before-save with supporting context rendered as secondary. | S01 reordered Observability so selected request evidence is the first major evidence section after filters, tightened request-detail copy to identify the persisted ledger row as authoritative, fixed preview rendering for undefined route scores, and locked the hierarchy with 32 passing focused Vitest assertions across Observability, request detail, and policy surfaces. | Yes | agent |
| D042 | M007/S02 | console-product | M007/S02 supporting evidence and preview seam shape | Keep policy options and policy preview bounded to runtime-enforced cache controls plus comparison-first replay evidence, and keep Observability supporting cards explicitly subordinate to the selected request. | T01 aligned the admin policy-options contract with live semantic-cache controls and tightened bounded simulation evidence at the admin boundary rather than widening preview payloads. T02 then consumed that contract by clarifying baseline-vs-draft preview wording and making recommendation, calibration, cache, and dependency sections point back to the authoritative selected ledger row, all verified by passing focused pytest and Vitest suites. | Yes | agent |
| D043 |  | requirement | R055 | validated | M007/S05 delivered the primary requirement close-out: `docs/m007-integrated-proof.md` now assembles selected-request-first Observability, authoritative request detail, compare-before-save policy preview, and explicit anti-drift boundaries into one pointer-only operator proof path; `README.md` and `docs/architecture.md` link to it; and the six-file focused Vitest bundle passed unchanged (6/6 files, 39/39 tests), proving page identity is clarified without widening Nebula into a dashboard, routing studio, analytics product, redesign-sprawl effort, or parallel operator workflow. | Yes | agent |
| D044 | M008 bootstrap repair | maintenance | How to recover GSD database bootstrap after verification_evidence duplicate rows blocked DB open | Dedupe verification_evidence rows by (task_id, slice_id, milestone_id, command, verdict) after taking a timestamped gsd.db backup, keeping the lowest-id row in each duplicate group. | The SQLite file passed integrity_check, but the GSD engine failed to open in degraded mode because bootstrap/migration hit duplicate verification_evidence groups that violate the stricter uniqueness expectation. Removing only duplicate groups restored DB-backed tool access without changing substantive project data. | Yes — if the GSD engine later ships an idempotent bootstrap fix, prefer that over manual DB repair. | agent |
| D045 | M008/S01 | governance | How M008/S01 should expose evidence governance without widening the trust boundary | Keep evidence governance as typed runtime-enforced tenant policy fields plus per-row governance markers on the existing usage ledger, and render them through existing policy and request-detail surfaces rather than introducing raw payload capture or separate governance products. | Task summaries show S01 completed by extending the current tenant-policy, ledger, and operator surfaces with retention/minimization controls and historically truthful row markers. This preserves Nebula’s bounded evidence story, keeps hosted export metadata-only, and gives downstream slices one shared governance vocabulary for persistence, deletion, and request-detail explanation. | Yes | agent |
