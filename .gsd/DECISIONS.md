# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001 | arch | v3.0 API strategy | Hybrid: OpenAI-compatible inference plus Nebula-native control surfaces | A pure Nebula-native API increases adoption friction; a pure compatibility layer undersells Nebula’s differentiated value | No | collaborative |
| D002 | M001 | scope | v3.0 primary emphasis | Focus v3.0 on adoption path clarity, migration proof, and day-1 value visibility rather than broad new infrastructure | Nebula is already reasonably strong as infrastructure; the weak spot is how teams adopt and understand it | No | collaborative |
| D003 | M001 | scope | Compatibility promise | Keep the initial OpenAI-compatible promise tight and reliable around common chat-completions flows | Broad compatibility would expand surface area and maintenance risk before the contract is proven | Yes — if repeated adoption demand proves a wider surface is required | collaborative |
| D004 | M001 | pattern | Reference proof style | Use a realistic migration integration rather than a toy demo as the main adoption proof | Platform-minded teams trust believable migration proof more than greenfield showcase apps | Yes — if a stronger representative reference target emerges during execution | collaborative |
| D005 | M001 | pattern | Production structuring model in v3.0 | Make tenant / app / workload / operator boundaries explicit in M001, but keep enforcement lightweight unless adoption proof demands more | The model needs to be understandable now, but a heavy product-surface redesign would distract from the adoption milestone | Yes — if lightweight modeling proves insufficient during implementation | collaborative |
| D006 | M001/S01 | api-contract | How to enforce the minimum public request invariant for chat completions | Enforce at least one user message at the ChatCompletionRequest schema boundary and rely on FastAPI's native 422 validation envelope for invalid request shapes. | Keeping the invariant in request-schema validation narrows the adoption contract at the public boundary, preserves consistent framework-native 422 behavior, and prevents downstream service logic from becoming the first line of contract enforcement. | Yes | agent |
| D007 | M001/S01 | documentation | How to document the public compatibility surface without compatibility sprawl | Treat docs/adoption-api-contract.md as the single canonical source for the public POST /v1/chat/completions adoption boundary, and explicitly separate it from the admin Playground contract. | A single test-backed contract document reduces drift between README, architecture docs, and runtime behavior while making the public client path and admin operator path clearly non-equivalent. | Yes | agent |
| D008 | M001/S02 | documentation | How to structure adoption guidance without reopening contract ambiguity | Use one canonical quickstart (docs/quickstart.md) for the supported self-hosted happy path, one canonical production-model doc (docs/production-model.md) for tenant/operator/app/workload framing, and keep docs/adoption-api-contract.md as the only public API contract. | Separating deployment flow, operating-model guidance, and public compatibility rules reduces doc drift while giving downstream slices one stable adoption story to reuse. | Yes | agent |
| D009 | M001/S03 | reference-integration | How to structure the canonical S03 reference migration proof | Keep the reference migration proof narrowly focused on real caller changes and dual-sided verification: base URL plus X-Nebula-API-Key are the default changes, X-Nebula-Tenant-ID is added only for intentionally ambiguous multi-tenant keys, and success is proved by correlating X-Request-ID/X-Nebula-* response headers with GET /v1/admin/usage/ledger. | A narrow, executable migration path is more credible than a broader demo because it matches the real public contract, avoids reopening setup or compatibility scope from S01/S02, and gives downstream slices a reusable observability pattern for operator-visible proof. | Yes | agent |
| D010 | M001/S04 | documentation | How to present Nebula's day-1 value proof without reopening the public compatibility boundary | Use a single canonical docs/day-1-value.md walkthrough rooted in the public POST /v1/chat/completions request and X-Request-ID/header correlation, while treating Playground and Observability strictly as operator corroboration surfaces and reusing existing admin API data for console proof surfaces. | This keeps the public adoption target stable, prevents the admin Playground from being mistaken for the migration path, and makes routing, policy, observability, and provider abstraction legible on day 1 without widening backend scope or duplicating contract/setup docs. | Yes | agent |
| D011 | M002/S01 | console-copy | How operator truth surfaces should present runtime scope and evidence semantics | Derive all operator-facing scope and evidence framing from existing runtime/admin contracts: API key scope from tenant_id plus allowed_tenant_ids, Playground as admin-only non-streaming corroboration, and Observability as persisted request evidence plus dependency-health context. | This removes production-structuring ambiguity without inventing backend fields or pseudo-entities, keeps the console aligned to AuthService/admin route truth, and lets focused tests catch future wording drift at the real operator trust boundary. | Yes | agent |
| D012 | M002/S02 | console-copy | How tenant surfaces should explain app/workload structuring without fake product objects | Teach operators to map apps and workloads onto real tenants and API keys, and frame tenant metadata only as optional notes/runbook context rather than an enforced schema or pseudo-entity. | This gives larger teams actionable production-structuring guidance on the Tenants page and editor drawer while preserving Nebula's runtime truth: tenants and API keys are enforced, but app/workload remain conceptual conventions until real implementation exists. Focused vitest assertions can then lock that boundary against future wording drift. | Yes | agent |
| D013 | M002/S03 | documentation | How to close the production-structuring milestone with one integrated operator proof | Make docs/integrated-adoption-proof.md the composition-first assembly doc that starts from tenant/API-key runtime truth, preserves the public request → headers → usage ledger → Playground → Observability order, and lock console corroboration surfaces with focused positive and negative wording assertions plus a concrete S03 UAT script. | The milestone risk was final-story drift across docs and operator surfaces, not missing new runtime features. A composition-first walkthrough avoids duplicating contract/setup canonicals, while focused Vitest checks and tailored UAT keep Playground and Observability in corroboration-only roles and prevent pseudo-entity drift from reappearing. | Yes | agent |
| D014 | M003 | scope | How to widen the v3 adoption surface | Add one new public surface only — embeddings — and keep M003 focused on adoption proof rather than a broader parity expansion. | Embeddings is the strongest next high-demand surface and the repo already has internal prior art, but widening beyond one surface would reopen scope and maintenance risk. | Yes — if repeated demand later justifies another narrow compatibility surface | collaborative |
| D015 | M003 | api-contract | What public embeddings boundary M003 should promise | Support only the strict happy-path embeddings contract: POST /v1/embeddings with input + model, string input and simple batch input, and the standard float embedding response shape. | A narrow contract preserves credibility and keeps M003 from accidentally committing Nebula to broader embeddings-option parity before the basic adoption story is proven. | Yes — if real adoption demand later proves a specific additional parameter is necessary | collaborative |
| D016 | M003 | pattern | What kind of proof should anchor the embeddings milestone | Use a realistic generic caller migration proof and require durable backend/operator evidence, while treating console expansion and helper ergonomics as optional only if a concrete proof gap forces them. | The milestone is about believable adoption, not feature breadth. A generic caller swap is the clearest proof that Nebula can replace direct provider usage with minimal change, and backend-first evidence is enough unless implementation shows a real trust gap. | Yes | collaborative |
| D017 | M003/S01 | api-contract | How S01 should expose embeddings publicly without widening Nebula's orchestration or evidence model | Add a dedicated authenticated /v1/embeddings route that calls the embeddings service directly, keeps the request contract to model plus string-or-flat-list input, and reuses the existing generic usage ledger with embeddings-specific metadata keyed by X-Request-ID. | This preserves the narrow public compatibility promise, avoids coupling embeddings into chat orchestration, and provides durable metadata-only correlation proof without adding a new ledger schema or storing raw inputs/vectors. | Yes | agent |
| D018 | M003/S02 | documentation | How to document Nebula's public embeddings compatibility boundary without creating a second drifting source of truth | Make docs/embeddings-adoption-contract.md the single canonical public POST /v1/embeddings contract, keep it grounded in route/tests/ledger evidence, and update README.md plus docs/architecture.md to link to it instead of duplicating detailed request/response semantics. | The embeddings milestone widens adoption only if the compatibility line stays narrow and inspectable. One canonical document backed by focused tests and runtime evidence reduces drift, makes unsupported/deferred edges explicit, and preserves the milestone guardrail against broader parity promises. | Yes | agent |
| D019 | M003/S03 | reference-integration | How S03 should prove the embeddings migration path credibly without widening scope | Anchor the embeddings migration proof in one executable public-route test plus a pointer-only human guide, and require dual-sided verification through public X-Request-ID/X-Nebula-* headers and metadata-only usage-ledger correlation. | A believable adoption proof needs to show the real caller diff and the real evidence path, not a toy demo or a second drifting contract document. Reusing the public /v1/embeddings route, existing OpenAI-style client shape, and existing usage-ledger correlation proves minimal-change migration while preserving the milestone guardrails against helper-stack, parity, or persistence-scope expansion. | Yes | agent |
| D020 | M003/S04 | observability | How embeddings evidence should appear in operator surfaces | Reuse the existing usage-ledger/admin API path and make embeddings intentionally discoverable by extending shared Observability filter, table, and request-detail surfaces with persisted metadata-only fields rather than adding embeddings-specific storage, payload capture, or a new API family. | This satisfies the durable-evidence goal with the smallest scope increase, keeps Observability subordinate to the public X-Request-ID plus usage-ledger proof path, preserves the metadata-only trust boundary, and avoids widening M003 into bespoke console or backend infrastructure. | Yes | agent |
| D021 | M003/S05 | documentation | How to assemble the final embeddings adoption story without creating a second contract or promoting admin corroboration above the public proof path | Make docs/embeddings-integrated-adoption-proof.md a composition-first, pointer-only walkthrough that preserves the order POST /v1/embeddings → X-Request-ID/X-Nebula-* → GET /v1/admin/usage/ledger?request_id=... → Observability, and keep README/architecture discoverability links pointer-only. | Final assembly should help reviewers find and follow the joined proof while protecting the canonical contract, migration guide, and metadata-only evidence model from duplication or scope drift. Preserving Observability as subordinate corroboration keeps the public adoption path authoritative. | Yes | agent |
| D022 | M004 | scope | What kind of hosted improvement M004 should prioritize | Prioritize hosted console fleet-reading UX improvements over new hosted mechanics, and treat docs/proof as reinforcement that keeps the UX inside the trust boundary. | The repo already has hosted prior art — deployment inventory, trust-boundary copy, freshness states, and bounded remote actions — but the hosted experience still reads more like inventory plus details than a clear fleet posture surface. UX reinforcement closes the bigger adoption gap without expanding authority. | Yes | collaborative |
| D023 | M004 | product-framing | Who M004 should optimize for first | Optimize the hosted experience for multi-deployment evaluators and operators first; single-deployment pilots should benefit as a simpler subset. | Multi-deployment ambiguity is where hosted fleet understanding and operator confidence matter most. A well-designed fleet posture surface still works for single-deployment use, but the reverse tends to leave evaluators stitching meaning together manually. | Yes | agent |
| D024 | M004 | trust-boundary | What language and semantics M004 should use for hosted reinforcement | Use fleet posture and confidence as the primary framing, keep any readiness language narrow and secondary, and prefer composition over new hosted authority or broad metadata expansion. | Fleet posture is a descriptive, metadata-backed frame that helps operators interpret linked, stale, offline, and blocked states without making the hosted plane sound like it is certifying runtime truth. This keeps local enforcement authoritative while still allowing the hosted UX to become more useful. | Yes | agent |
| D025 | M004/S01 | trust-boundary | How hosted reinforcement wording should be controlled across hosted and deployment-management surfaces in M004 | Keep hosted reinforcement vocabulary in console/src/lib/hosted-contract.ts as the single shared seam, assert required phrases there, and require trust-boundary plus deployment-management surfaces to reuse that seam instead of maintaining local wording islands. | S01's main risk was semantic drift into hosted authority rather than missing backend capability. A shared schema-backed wording seam with fail-fast phrase assertions keeps fleet-posture, confidence, and bounded-action language descriptive and metadata-backed across public and deployment-facing surfaces while giving downstream slices one contract to preserve. | Yes | agent |
| D026 | M004/S02 | ui-architecture | How hosted fleet posture and bounded remote-action semantics should be derived in the deployments console | Use a shared pure helper seam in console/src/components/deployments/fleet-posture.ts to derive fleet counts, per-row posture labels, and bounded-action availability, and have the page summary, deployment table, and RemoteActionCard consume that seam instead of duplicating conditions. | S02's main risk was semantic drift between the page summary, scan table, and bounded-action detail surface. A single helper keeps hosted posture descriptive, metadata-backed, and fail-closed while letting focused tests catch derivation drift separately from rendering drift. | Yes | agent |
